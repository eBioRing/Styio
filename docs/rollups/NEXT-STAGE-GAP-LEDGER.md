# Next-Stage Gap Ledger

**Purpose:** Provide the active, evidence-based phase summary for repository-wide unfinished work so maintainers can split the next stage into checkpoint-sized, multi-team deliveries without creating parallel truths.

**Last updated:** 2026-06-28

**Status:** Active collaboration ledger. This file distinguishes:

1. what the `styio` repository has already delivered,
2. what is still missing inside `styio`,
3. what is intentionally owned by `styio-spio` or another adjacent repository.

## 1. How to Use This Ledger

1. Start from [CURRENT-STATE.md](./CURRENT-STATE.md), then use this file to decide next-stage ownership and sequencing.
2. Treat each gap below as a coordination item, not as a free-form backlog note. Every implementation checkpoint should point back to one or more ledger items.
3. When a gap changes status, update this file, the owning runbook, and the relevant SSOT or handoff document in the same checkpoint.
4. Do not use this file to redefine language semantics, package-manager product scope, or IDE public behavior. Those still belong to the owning SSOTs.
5. Treat the industrial-maturity decision register in §5.7 as the required stop line for broad compiler-language hardening work: an unfinished capability may remain open only when it is tied to a named pending decision, a mature reference architecture, and the next artifact that would close or reject it.

## 2. Current Baseline That Is Real

1. Project priorities and decision order remain fixed by [../specs/PRINCIPLES-AND-OBJECTIVES.md](../specs/PRINCIPLES-AND-OBJECTIVES.md).
2. language feature acceptance suites remain the frozen language/runtime acceptance baseline; current implementation is still expected to preserve those accepted behaviors.
3. The hand-written nightly compiler parser is the accepted grammar authority for parser/toolchain execution.
4. `styio` already provides a bootstrap nano package producer/verifier contract, including `--nano-create`, `--nano-publish`, static repository materialization, and `--machine-info=json`.
5. The IDE stack is real and usable for completion, hover, definition, references, symbols, semantic tokens, incremental edits, and semantic query caching, but it is not feature-complete or operationally closed yet.

## 3. Executive Summary

| Stream | Current reality | Next-stage pressure |
|--------|-----------------|---------------------|
| Frontend / Parser | Nightly parser is the accepted grammar authority; unsupported syntax now fails closed instead of passing through legacy fallback | Keep future syntax additions on the no-fallback contract |
| Sema / IR | Direct unsupported AST lowering now fails closed or uses explicit `SGNoOp`; remaining debt is feature semantics and empty visitor follow-up, not silent integer placeholder execution | Highest technical debt concentration; blocks language/runtime completion |
| Codegen / Runtime | Multi-stream zip and stream-driver combinations are only partially lowered | stream-processing remains incomplete end-to-end |
| CLI / Nano | Bootstrap nano contract exists; full package lifecycle does not | Keep `styio` limited to compiler contracts and handoff surfaces |
| IDE / LSP | Core semantic services exist, but stdio runtime drain and several LSP methods are still absent | Close operational gaps before expanding host-facing promises |
| Tests / Quality | Core suites exist, state-resource active acceptance now uses resource topology syntax, resource-topology safety coverage is registered, and broad maturity gaps are now decision-tracked in §5.7, but negative-path package and next-stage migration coverage still need expansion | Coverage closure must run in parallel with implementation closure |

## 4. Responsibility Split: `styio` vs `styio-spio`

| Area | Owning repo | Status in `styio` | Notes |
|------|-------------|-------------------|-------|
| Nano package materialization, static local registry consume/publish, compiler capability reporting | `styio` | Delivered baseline | See [../external/for-spio/Styio-Nano-Spio-Coordination.md](../external/for-spio/Styio-Nano-Spio-Coordination.md) |
| Full package-manager UX: `install`, `use`, `search`, `vendor`, `pin`, dependency resolution, lockfiles, remote registry protocol | `styio-spio` | Out of scope here | See [../specs/REPOSITORY-MAP.md](../specs/REPOSITORY-MAP.md) |
| `spio build/check/run/test` live compile-plan handoff | `styio` producer contract, `styio-spio` consumer integration | Delivered baseline in `styio` | `--machine-info=json` advertises `compile_plan:[resolved-request]`; compile-plan resolved-request consumer materializes artifacts / receipt / `diag_dir` diagnostics |
| Remote registry service semantics, auth/signing/trust, channel aliasing, package listing APIs | `styio-spio` | Not owned here | `styio` should not absorb this scope |

## 5. Detailed Gap Ledger

### 5.1 Frontend / Parser

| Gap | Severity | Current evidence | Owning teams | Next checkpoint intent |
|-----|----------|------------------|--------------|------------------------|
| Parser-authority contract is closed for accepted grammar | Medium | Public syntax check rejects non-authoritative parser engines, nightly parser fallback points fail closed, and the contract is recorded in [IM-D2-PARSER-AUTHORITY-INVENTORY.md](./IM-D2-PARSER-AUTHORITY-INVENTORY.md) | Frontend, Test Quality, IDE / LSP | Keep future syntax additions on the same rule: accepted by nightly with no fallback, or rejected by stable diagnostic |
| resource topology resource declaration syntax is still not the complete running compiler path | High | Target design now uses `@name : Type|n|`, `@name : Type|..n|`, `T..` / `T...`, `list[T]`, and `expr -> @name`; bounded selector semantics are partially executable because `@name[-n]` is scalar/latest while `@name[-n..]` and `@name[...]` materialize `list[i64]` for `i64` resources, `list[f64]` for `f64` resources, `list[bool]` for `bool` resources, `list[char]` for `char` resources, `list[string]` for `string` resources, `list[list[T]]` snapshots for bounded list-valued resources, `list[dict[K,V]]` snapshots for bounded dict-valued resources, and `list[matrix]` snapshots for bounded matrix-valued resources; list/dict/matrix selector rings clone handle values on write/read and release owned ring handles on overwrite or scope cleanup; bounded selector iterator tails such as `@name[...] >> #(v)` now execute through the materialized runtime-list path while scalar latest selector iterators stay rejected; `snapshot << @name[-n..]` and `snapshot << @name[...]` now copy materialized bounded scalar/string/char/bool/f64/i64 snapshots plus bounded list/dict/matrix handle snapshots; materialized `list`/`dict`/`matrix` handles now deep-clone through `copy << source`, while `copy <- source` fails closed; the remaining parser/type/lowering migration is tracked by [../design/Styio-Resource-Topology.md](../design/Styio-Resource-Topology.md) | Frontend, Sema / IR, Docs / Ecosystem | Treat remaining resource topology work as dedicated migration checkpoints, especially unsupported tuple selector history, unbounded snapshots, broader file/topology-resource `<<` copy/clone semantics, and broader resource-family semantics |
| Handle / capability / failure-type unification is still target design rather than active compiler behavior | Medium | Design doc explicitly says the model is not fully implemented and keeps remaining capability/protocol special cases in [../design/Styio-Handle-Capability-Type-System.md](../design/Styio-Handle-Capability-Type-System.md); typed stdin ingestion is no longer evidence of an active AST split; IM-D4 decisions are now fixed in [IM-D4-RESOURCE-MANAGEMENT-INVENTORY.md](./IM-D4-RESOURCE-MANAGEMENT-INVENTORY.md) | Frontend, Sema / IR, Codegen / Runtime | Implement the fixed IM-D4 model instead of adding more local special-case patches |

### 5.2 Sema / IR

| Gap | Severity | Current evidence | Owning teams | Next checkpoint intent |
|-----|----------|------------------|--------------|------------------------|
| Placeholder lowering contract is closed for direct AST lowering | Medium | Direct unsupported AST families now throw `StyioTypeError` or lower to explicit `SGNoOp`; tuple function return annotations now fail closed instead of mapping tuple metadata to `i64`; remaining `SGConstInt(0)` uses are documented generated/default values in [IM-D1-STYIOIR-CONTRACT-INVENTORY.md](./IM-D1-STYIOIR-CONTRACT-INVENTORY.md) | Sema / IR, Codegen / Runtime, Test Quality | Keep future syntax additions on the same contract: real lowering, explicit no-op, or typed rejection |
| Type inference coverage remains structurally incomplete | High | Empty visitors remain classified in [IM-D1-STYIOIR-CONTRACT-INVENTORY.md](./IM-D1-STYIOIR-CONTRACT-INVENTORY.md); `FmtStrAST` infers embedded expressions and returns `string`; `CharAST` now reports `char`; `RangeAST` now validates integer expression bounds/step and materializes expression-bound ranges as `list[i64]`; accepted `MatchCasesAST` / function match sugar now infer scrutinee, integer patterns, branch bodies, branch-local scopes, and `i64`/`f64`/`bool`/`char`/`string` tail result kinds before lowering, including source-reachable single-return and statement-preface resource method bodies, while list/dict/matrix/tuple match results remain future work; block-form ordinary function result tails now feed returned function calls in value-returning resource methods when the called function has a value-producing final tail or explicit `<| expr`; compiler-owned `TypeConvertAST` scalar promotions now carry their value through `SGCast`; `StyioIR::is_active()` and the verifier give the IR layer an explicit active-surface marker | Sema / IR, Test Quality | Convert implementation-debt visitors into real sema only when the owning syntax is accepted; keep unsupported forms fail-closed |
| State inline clone path still has unsupported-node fallthrough | Medium | Accepted scalar leaf values, `char` literals, format strings, receiver-scoped property postfix such as `@file.path`, scalar/string match expressions, dynamic range literals, value-producing `ResourceEffectAST` return expressions, block-form function-call return expressions, local list/dict/matrix container-return resource methods, and accepted value-returning resource methods that return materialized list index/list-slice, inline dict-index, ordered dict value-slice, or matrix cell/row/range expressions now parse through resource-method definition bodies and `StateExprCloneVisitor` clones `IntAST`/`BoolAST`/`FloatAST`/`StringAST`, `CharAST`, `FmtStrAST`, `ResourceReceiverAST`/`AttrAST`, `MatchCasesAST`, `RangeAST`, `ResourceEffectAST`, `FuncCallAST`, `ListAST`/`ListOpAST`, and `DictAST` type metadata, with focused tests for inline execution, guarded scalar/fmt-string/receiver-property/match/function-call/local-list-dict-matrix-container value paths, invalid multi-byte char rejection, malformed format-string rejection, top-level receiver-property rejection, returned container match rejection, returned statement-only function-call rejection, returned resource-effect fallback/handler value paths, returned resource-effect discard rejection, format-string fallback type mismatch, non-integer range-bound rejection, dynamic range lowering, and returned list/dict/matrix `bounds` recovery. Unsupported AST fallback remains in [src/StyioLowering/AstToStyioIR.cpp](../../src/StyioLowering/AstToStyioIR.cpp) for other accepted source-reachable forms until each is covered. | Sema / IR, Test Quality | Continue shrinking the unsupported clone surface until state-helper inlining is total for accepted language forms |

### 5.3 Codegen / Runtime

| Gap | Severity | Current evidence | Owning teams | Next checkpoint intent |
|-----|----------|------------------|--------------|------------------------|
| stream-processing processing is not complete end-to-end | High | `IterSeqAST` exists in parser output in [src/StyioParser/Parser.cpp](../../src/StyioParser/Parser.cpp), but hash-tag route semantics are not defined in active syntax; it now fails closed with a typed diagnostic instead of silently lowering to `SGConstInt(0)` | Frontend, Sema / IR, Codegen / Runtime | **Closed decision IM-D5-P1:** retire hash-tag routes completely, keep stable migration diagnostics, and carry only accepted stream-processing slices through parser, sema, lowering, runtime, and feature tests |
| Zip lowering still supports only a narrow source set | High | List literal/file-backed zip paths remain supported, non-file materialized `list[T]` handles have runtime-list zip coverage for `i64`, `string`, `f64`, `bool`, and `char`, mixed `@file` / materialized-list pairs run in both directions, bounded Topology selector snapshots feed finite zip as already-materialized `list[T]` values, bounded matrix selector snapshots have finite `list[matrix]` zip evidence, and `@stdin` now zips with materialized lists or `@file` streams in both source orders while `@stdin & @stdin` fails closed by owner decision until an explicit tee/buffer or duplicate-driver design is accepted; raw/latest matrix selector handles and broader stream-source combinations still reject before codegen can reinterpret them as list handles | Codegen / Runtime, Sema / IR, Test Quality | Expand supported combinations according to stream-processing acceptance order, especially true snapshot joins, pressure/timeout policy, EOF/failure policy beyond plain files, and merge/conflict behavior; do not implement implicit stdin duplication or sequential double reads |
| Some accepted runtime-oriented syntax still depends on special-case routing rather than a unified protocol | Medium | This is reflected both in current parser/analyzer shape and in the still-target-only capability design [../design/Styio-Handle-Capability-Type-System.md](../design/Styio-Handle-Capability-Type-System.md) | Codegen / Runtime, Sema / IR | Use next-stage runtime work to reduce parser-shape-driven behavior branching |

### 5.4 CLI / Nano / `spio` Handoff

| Gap | Severity | Current evidence | Owning teams | Next checkpoint intent |
|-----|----------|------------------|--------------|------------------------|
| `compile_plan` live handoff baseline exists, but contract hardening is still narrower than a full release matrix | Medium | `--machine-info=json` now reports `compile_plan:[resolved-request]`; compile-plan success paths write artifacts / receipt / `diag_dir` diagnostics, and invalid intent / generated_by mismatch / unsupported target kind / relative-path guards / missing-outputs fallback, malformed explicit `profile.build_mode`, malformed package entries, and missing entry-package consistency now also have explicit machine-readable coverage in [src/main.cpp](../../src/main.cpp), [src/StyioServices/StyioConfig/CompilePlanContract.cpp](../../src/StyioServices/StyioConfig/CompilePlanContract.cpp), and [tests/styio_test.cpp](../../tests/styio_test.cpp) | CLI / Nano, Docs / Ecosystem, `styio-spio` coordination | Keep the current resolved-request boundary stable while expanding compatibility-edge coverage and broader malformed-input matrices without pulling package-manager lifecycle into `styio` |
| Full package-manager lifecycle is not implemented here by design | High | CLI surface only exposes nano producer/verifier options in [src/main.cpp](../../src/main.cpp); repo boundary doc assigns package management to `styio-spio` in [../specs/REPOSITORY-MAP.md](../specs/REPOSITORY-MAP.md) | CLI / Nano, Docs / Ecosystem | Preserve this boundary; do not let compiler CLI accumulate `install/use/search/vendor` responsibilities |
| Remote publish protocol is still intentionally absent | Medium | `--nano-publish` rejects HTTP(S) roots and only accepts local path or `file://` repository roots in [src/main.cpp](../../src/main.cpp) | CLI / Nano, `styio-spio` coordination | Keep publish local/static here; any remote protocol must be defined on the `styio-spio` side |
| Edge-path nano validation baseline covers the current compiler-side contract | Medium | Happy-path bundle/create/publish tests exist in [tests/styio_test.cpp](../../tests/styio_test.cpp), and the current negative-path baseline rejects invalid repository markers, blob SHA256 mismatches, blob size mismatches, malformed repository entry schemas, malformed cloud manifests, HTTP(S) publish roots, create/publish mutual exclusion, missing nano mode selection, and create-only/publish-only option mixups without adding package-manager lifecycle behavior | CLI / Nano, Test Quality | Keep every future nano handoff branch paired with explicit negative-path coverage; do not expand this into package-manager UX |

### 5.5 IDE / LSP

| Gap | Severity | Current evidence | Owning teams | Next checkpoint intent |
|-----|----------|------------------|--------------|------------------------|
| LSP surface is still intentionally incomplete | Medium | Current limits still list local-only, single-workspace behavior and missing `rename`, `codeAction`, and `inlayHint` in [../external/for-ide/LSP.md](../external/for-ide/LSP.md); server capabilities stop at completion/hover/definition/references/symbols/semantic tokens in [src/StyioServices/StyioLSP/Server.cpp](../../src/StyioServices/StyioLSP/Server.cpp) | IDE / LSP, Docs / Ecosystem | Treat `rename` as the next public-method checkpoint, but only after runtime drain, semantic identity/freshness, workspace index, and diagnostics publication stay stable under tests |
| Perf budget enforcement is split between unit and dedicated Release harnesses | Low | `StyioIdePerf.EnforcesFrozenLatencyBudgets` skips non-Release runs in [tests/ide/styio_ide_test.cpp](../../tests/ide/styio_ide_test.cpp), with operational guidance in [../teams/PERF-STABILITY-RUNBOOK.md](../teams/PERF-STABILITY-RUNBOOK.md) | IDE / LSP, Perf / Stability | Preserve the dedicated Release gate, but keep the distinction visible so teams do not mistake Debug green for perf closure |

### 5.6 Tests / Quality / Perf

| Gap | Severity | Current evidence | Owning teams | Next checkpoint intent |
|-----|----------|------------------|--------------|------------------------|
| Next-stage migration tests need to stay tied to active syntax | Medium | [../../workflows/TEST-CATALOG.md](../../workflows/TEST-CATALOG.md) now treats state-resource positive coverage as resource topology resource syntax and keeps retired state-family spellings only as negative migration diagnostics | Test Quality, Frontend, Sema / IR | Add new positive coverage only for active syntax; keep retired spellings in negative tests with stable migration diagnostics |
| Package and contract negative-path testing is current for nano handoff branches | Medium | Nano static repository coverage now rejects invalid repository markers, blob SHA256 mismatches, blob size mismatches, malformed entry schemas, malformed cloud package manifests, remote publish roots, create/publish mutual exclusion, packaging arguments without a selected mode, and create-only/publish-only option mixups in [tests/styio_test.cpp](../../tests/styio_test.cpp) | Test Quality, CLI / Nano | Treat contract-edge coverage as release-blocking for any future nano handoff changes |
| Broadened ignore baselines can still hide future tracked repro fixtures outside the frozen negate roots | Low | Root ignore rules now absorb cache, `tmp/`, `build-*`, and `*.tmp` / `*.log` style paths in [../../.gitignore](../../.gitignore), but `docs/**` and `tests/**` now have explicit negate rules and are checked by [../../scripts/repo-hygiene-gate.py](../../scripts/repo-hygiene-gate.py) | Docs / Ecosystem, Test Quality | Keep the shared ignore baseline, extend explicit negate rules before adding new tracked repro roots outside `docs/**` or `tests/**`, and do not rely on review memory alone |

### 5.7 Industrial Maturity Decision Register

This register is the active answer to broad requests to make Styio a mature compiler-language engineering project. It does not replace the feature ledger above. It records the decisions that must be resolved before a gap can be considered industrially closed, ties each item to a mature public architecture reference instead of local preference alone, and orders items by descending priority: `IM-D1` is the highest-priority maturity blocker, and larger numbers are lower priority.

| Decision ID | Capability gap | Current state | Mature reference architecture | Decision required before implementation closure | Stop condition |
|-------------|----------------|---------------|-------------------------------|-----------------------------------------------|----------------|
| IM-D1 | Sema and lowering completeness | `StyioIR` is already the canonical typed mid-level IR. The contract slice now has `StyioIR::is_active()`, explicit `SGNoOp`, an independent verifier pass, codegen verifier gates, fail-closed unsupported AST lowering, `char` lowering/type closure, integer expression-bound `RangeAST` list materialization, value-carrying scalar cast closure for compiler-owned `TypeConvertAST`, tuple-return-annotation fail-closed behavior, accepted match/function-match result-family closure for `i64`/`f64`/`bool`/`char`/`string`, and a recorded empty-visitor inventory in [IM-D1-STYIOIR-CONTRACT-INVENTORY.md](./IM-D1-STYIOIR-CONTRACT-INVENTORY.md) | rustc lowers AST to HIR and then MIR; MIR is a simplified CFG with explicit types for flow-sensitive safety checks, optimization, and code generation | Decisions are fixed: lowering feeds an independent StyioIR verifier before codegen; optimization stays separate; no-op is explicit `SGNoOp`; unsupported syntax is rejected at the earliest stage that can prove it; resource/handle capability state is maintained by verifier-side state instead of bloating every IR node; codegen must run only after verification | The contract stop condition is met for direct lowering: no active AST lowers to `SGConstInt(0)` as a placeholder, remaining empty visitors are classified, tuple return metadata no longer becomes an `i64` function type, match expression/function match sugar `i64`/`f64`/`bool`/`char`/`string` result families keep their Sema/lowering/codegen shape, and codegen gates active verified StyioIR. Follow-on feature work remains for tuple/set/null/source-level-cast/closure/resource-path semantics and broader match result families. |
| IM-D2 | Parser authority and accepted grammar completeness | The hand-written nightly compiler parser is now the accepted grammar authority. Public syntax check rejects non-authoritative engines, top-level and internal nightly fallback points fail closed, IDE semantic analysis uses strict compiler parse facts, and the contract inventory lives in [IM-D2-PARSER-AUTHORITY-INVENTORY.md](./IM-D2-PARSER-AUTHORITY-INVENTORY.md) | Clang separates reusable source/diagnostic/file infrastructure in its Basic library and keeps diagnostics as first-class IDs, locations, severities, ranges, and consumers; rustc separates AST, HIR, type checking, MIR, borrow checking, optimization, and code generation in the compiler overview | Decisions are fixed: hand-written nightly parser is the only grammar authority; accepted grammar must be no-fallback; IDE/editor syntax snapshots are non-authoritative and must not maintain a separate accepted grammar; unsupported syntax is rejected instead of treated as a subset gap | The contract stop condition is met: syntax-check is nightly-only, legacy fallback is not an accepted grammar path, strict IDE semantic parsing rejects malformed source without recovery facts, and future syntax must land with no-fallback parser evidence or stable rejection diagnostics. |
| IM-D3 | Typed diagnostics and parser/sema failure contract | The public diagnostic baseline is implemented in [IM-D3-DIAGNOSTIC-CONTRACT-INVENTORY.md](./IM-D3-DIAGNOSTIC-CONTRACT-INVENTORY.md): public codes use `STYIO_<PHASE>_<ERROR_FAMILY>`, phases are stable public enums, JSON/JSONL diagnostics carry code/phase/notes, exit codes stay coarse, and IDE/LSP maps compiler/service diagnostics instead of inventing compiler codes. Implemented family refinements now map immutable/final binding mutation to `STYIO_SEMA_IMMUTABLE_BINDING`, unresolved function/resource references to `STYIO_SEMA_UNDECLARED_SYMBOL`, user function and resource-method arity mismatches to `STYIO_SEMA_CALL_ARITY_MISMATCH`, resource capability mismatches to `STYIO_SEMA_RESOURCE_CAPABILITY_MISMATCH`, user function and resource-method argument type mismatches to `STYIO_TYPE_CALL_ARGUMENT_MISMATCH`, invalid matrix literal shape/context failures to `STYIO_TYPE_MATRIX_LITERAL_INVALID`, resource-effect fallback type mismatches to `STYIO_TYPE_RESOURCE_EFFECT_FALLBACK_MISMATCH`, unsupported statement-only called-function returns or local resource-binding resource method bodies to `STYIO_SEMA_RESOURCE_METHOD_UNSUPPORTED_BODY`, unsupported tuple function return annotations to `STYIO_TYPE_UNSUPPORTED_TUPLE_RETURN`, undefined hash-tag stream routes to `STYIO_TYPE_STREAM_HASH_TAG_ROUTE_UNSUPPORTED`, stream zip non-iterable inputs to `STYIO_TYPE_STREAM_ZIP_UNSUPPORTED_SOURCE`, duplicate stdin zip drivers to `STYIO_TYPE_STREAM_DUPLICATE_DRIVER_UNSUPPORTED`, ordinary iterator non-iterable inputs to `STYIO_TYPE_ITERATION_UNSUPPORTED_SOURCE`, and unsupported typed stdin scalar/list targets to `STYIO_TYPE_STDIN_UNSUPPORTED_TARGET`. | Clang diagnostics use unique IDs, severity mapping, source locations, ranges, notes, and consumer rendering; Clang also keeps diagnostic and AST dump tests as a normal compiler testing pattern | Decisions are fixed and the baseline implementation is complete: shared taxonomy, syntax-check codes, compile-plan/runtime/type JSONL envelopes, IDE diagnostic code/phase fields, LSP `Diagnostic.code` / `data.phase` mapping, and feature-owned sema/type diagnostics where focused families own the condition | The IM-D3 baseline stop condition is met. Remaining work is family refinement for broader sema, IR verifier, codegen, runtime fallback, and native diagnostics when those feature checkpoints own narrower error families. |
| IM-D4 | Resource management model | Resource syntax and current RTG checks exist, but the implementation is not yet closed: [IM-D4-RESOURCE-MANAGEMENT-INVENTORY.md](./IM-D4-RESOURCE-MANAGEMENT-INVENTORY.md) records the accepted resource-subject, capability, typestate, cleanup, and failure semantics. The accepted capability vocabulary is fixed as `pull`, `iter`, `push`, `index`, `slice`, `sized`, `collect`, `clone`, and `close`; `failed` is fixed as a transient typed effect/result rather than a long-lived typestate; cleanup failure is fixed as a typed resource effect with an independent `ResourceCleanupFailure` family; resource fallback is fixed as `?\| resource_operation \| fallback`; named handlers such as `?\| resource_operation \| backpressure => handler` are fixed as typed effect-specific recovery; `?\| resource_operation \| ...` is fixed as a statement-only audited discard that settles resource state, produces no value, and continues to the next statement; `?\| resource_operation` without fallback settles at that site and raises the structured error immediately; fallback recovery participates in normal type inference; `?=` remains ordinary value/pattern matching instead of an implicit resource-effect handler; single-resource write backpressure is fixed as non-failure `ResourceBackpressure` pressure signal that may escalate to `ResourceBackpressureFailure`; resource families may expose pressure observer streams for explicit side-effect recovery logic; clone is fixed as a deep copy that allocates a fresh resource/container without reference counting; `expr -> @name` uses pending writes with the latest safe commit boundary; every block-entry form, including `>>`, `=>`, `?=`, active `\|\|>`, and reserved `\|>` when activated, runs resource effects on a snapshot and commits the snapshot at block exit; host-provided anchors allow explicit user resource operations through their declared capabilities | RAII-style deterministic cleanup, typestate/protocol-state checking, operation-capability checking, single-resource-subject semantics without reference counting, and typed effect/result models for fallible operations | Decisions are fixed; remaining work is implementation and tests for each accepted resource family | Every accepted resource operation has an explicit capability, resource-state, block-entry snapshot/commit, cleanup, pressure-observation, `?\|` fallback/named-handler/discard behavior, commit-barrier, and failure rule; unsupported or retired resource forms are rejected by named migration diagnostics instead of implicit fallbacks |
| IM-D5 | Stream runtime, concurrency, and cross-stream synchronization | Multi-stream zip and stream-driver combinations are partially lowered; cross-stream sync and pulse-frame locking are not fully active; [IM-D5-STREAM-CONCURRENCY-INVENTORY.md](./IM-D5-STREAM-CONCURRENCY-INVENTORY.md) records the accepted deterministic pulse-frame model, zip barrier sync, snapshot-join split, pressure scheduling contract, happens-before rules, and Git-like parallel resource worktree merge model | Go memory model documents happens-before, synchronization operations, channel communication, and race-free sequential consistency; rustc MIR provides explicit CFG structure for control/dataflow checks; Git's branch/merge model is the reference analogy for isolated writer worktrees and deterministic merge/conflict handling | Decisions are fixed: Styio stream concurrency is a single-machine deterministic pulse-frame model; `&` zip is barrier synchronization; snapshot joins read latest committed state without being zip; unequal rates settle through declared queue/pressure/timeout/EOF behavior; multiple writers fork isolated worktrees and commit through deterministic merge or typed `ResourceMergeConflict`; scheduler order must not define observable results | Each accepted stream combination has parser/sema/lowering/runtime tests, or the combination is rejected by a stable diagnostic tied to this decision; frame, snapshot, commit, zip, pressure, happens-before, and merge/conflict facts survive into StyioIR/verifier-visible metadata |
| IM-D6 | Release, conformance, and regression matrix | Local gates are strong, but cross-platform release, sanitizer/fuzz/perf, package negative paths, and conformance matrices are not complete; [IM-D6-RELEASE-CONFORMANCE-INVENTORY.md](./IM-D6-RELEASE-CONFORMANCE-INVENTORY.md) records the accepted L0/L1/L2/L3 gate tiers, lane status vocabulary, block policy vocabulary, affected-area matrix, nightly/release matrix, and package-boundary rule | Clang keeps diagnostic and AST dump tests; Go modules define compatibility through module graph selection rules; rustc exposes staged IRs and query boundaries for compiler validation | Decisions are fixed: Linux x86_64 is the initial required platform; L0 commit gates protect hygiene/docs/runbook/runtime-surface drift; L1 push/PR gates use delivery/checkpoint/audit entrypoints; L2 gates are affected-area required; L3 nightly/release gates own sanitizer, fuzz, perf, soak, conformance, and cross-platform pending/advisory lanes; unavailable lanes must be `pending` or `skip-with-reason`, not silent success | A promotion is blocked by any failed required lane; a nightly may pass with explicit pending debt only when no required lane failed and each missing lane has owner/reason; release-candidate records must list lane status, command, result, artifact path, skipped lanes, pending owners, and final decision |
| IM-D7 | Native interop ABI contract | Explicit `# name := @ extern(...) { ... }` binding is the preferred path; [IM-D7-NATIVE-INTEROP-ABI-INVENTORY.md](./IM-D7-NATIVE-INTEROP-ABI-INVENTORY.md) records the accepted host-compiler delegation, explicit binding, native signature extraction, symbol resolution, C-priority/Linux-native use, C++ support policy, toolchain evidence, and native-effect contract | Clang owns C/C++ front-end source handling and target abstraction; LLVM LangRef is the stable IR contract beneath native codegen; POSIX/Linux C APIs are the primary native integration target for system-facing interop | Decisions are fixed: Styio orchestrates native C/C++ compilation instead of reimplementing those languages; every explicit binding must have an extractable native signature and resolvable callable symbol; C/POSIX/Linux interop is the priority path; C++ internals are allowed when the final callable export is provable; C ABI wrappers are the portable baseline but not the only possible supported path; unsupported signatures, ambiguous C++ symbols, missing exports, or unstable ABI mappings fail closed | Each native fixture states ABI, source route, explicit binding list, extracted signatures, visible and hidden symbols, runtime/artifact expectation, and negative diagnostics; JIT and `styio build` paths expose the same bound functions or reject the fixture with a stable native diagnostic |
| IM-D8 | Standard library and domain library maturity | Core fixtures cover language/runtime slices, but the thick-library model remains a large investment before practical broad use; [IM-D8-STDLIB-DOMAIN-LIBRARY-INVENTORY.md](./IM-D8-STDLIB-DOMAIN-LIBRARY-INVENTORY.md) records the accepted three-layer model and external ownership boundaries | Go standard packages and module tooling show a language-level distribution model; Rust/Clang keep core compiler semantics separate from libraries and toolchain integration | Decisions are fixed: retained compiler-repo layers are language-core, compiler-intrinsic, and standard library; language-core is basically the type system and minimal semantic facts; compiler-intrinsic admission requires compiler-owned typing/lowering/optimization/resource-effect/frame participation or performance evidence; standard library stays temporarily in this repo but should become official package content managed through Spio / Styio-Platform and trimmed during builds; examples, benchmarks, and domain libraries are external projects | A capability is either language-core, compiler-intrinsic, standard-library contract, delegated to `styio-example`, delegated to `styio-benchmark`, delegated to a domain package distributed through Spio / Styio-Platform, or explicitly deferred |
| IM-D9 | IDE/LSP completeness and service contracts | LSP core exists and Vityo already has a richer first-party product-owned adapter shape; [IM-D9-IDE-LSP-SERVICE-CONTRACT-INVENTORY.md](./IM-D9-IDE-LSP-SERVICE-CONTRACT-INVENTORY.md) records the accepted boundary across compiler facts, editor snapshots, semantic cache, workspace indexes, public LSP, and first-party adapters | Clang exposes diagnostics through consumers; rustc query-style demand-driven organization supports cached semantic facts; Go workspace behavior defines how tools scope multiple modules | Decisions are fixed: StyioServices owns the shared service facts; public LSP is one editor-neutral transport; Vityo / Spio may use deep first-party convenience adapters when they reuse shared facts; local fallback is allowed only as marked derived/fallback behavior; syntax validity, compiler diagnostics, and semantic truth remain compiler-owned | Public LSP capabilities match documented server capabilities and tests; missing methods remain intentionally absent with documented prerequisites; first-party adapters may be deeply integrated, but must not become separate grammar, diagnostic, or semantic authorities; service payloads carry capability state, freshness, parser/grammar evidence, and workspace/config identity |
| IM-D10 | Package, module, and release compatibility model | `styio` owns nano producer/verifier, source-build, machine-info, syntax-check, and compile-plan contracts; [IM-D10-PACKAGE-MODULE-COMPATIBILITY-INVENTORY.md](./IM-D10-PACKAGE-MODULE-COMPATIBILITY-INVENTORY.md) records the compiler-side boundary and the external Spio / Styio-Platform confirmations that cannot be inferred from stale local checkouts | Go modules define module graphs, module selection, graph pruning, lazy loading, workspaces, and repository identity mapping | Keep compile-plan as a resolved compiler input, not a dependency-resolution request; confirm manifest, lockfile, resolver, registry, trust, hosted workspace, standard-library package, and compatibility-matrix ownership with the active Spio and Styio-Platform maintainers | `styio` exposes only compiler-side contracts with negative-path tests; package lifecycle and platform-hosted registry items are either confirmed by their owning repos or tracked as external confirmation requirements without being implemented in `styio` |

Closed sub-decisions that are below the IM-D register level:

| Decision ID | Boundary | Current owner | Closed answer / stop condition |
|-------------|----------|---------------|------------------------------|
| IM-D5-P1 | Hash-tag stream route syntax and `IterSeqAST` ownership | Frontend + Sema / IR + Codegen / Runtime | Retire the syntax from active docs, keep stable migration diagnostics, and do not implement lowering/runtime behavior unless a future owner decision reopens the syntax through the stream design SSOT. |

Reference links used by the register:

1. Clang CFE Internals Manual: <https://clang.llvm.org/docs/InternalsManual.html>
2. LLVM Language Reference Manual: <https://llvm.org/docs/LangRef.html>
3. rustc development guide overview: <https://rustc-dev-guide.rust-lang.org/overview.html>
4. rustc MIR guide: <https://rustc-dev-guide.rust-lang.org/mir/index.html>
5. rustc query system guide: <https://rustc-dev-guide.rust-lang.org/query.html>
6. Go Modules Reference: <https://go.dev/ref/mod>
7. Go Memory Model: <https://go.dev/ref/mem>

### 5.8 Closed Since Previous Ledger

Pressure observer syntax now has a narrow parser/Sema/diagnostic boundary:
`resource.pressure >> #(p) => { ... }` reaches Sema through the nightly parser,
and current resource families such as `@stdout` or topology resource bindings
fail closed with `STYIO_SEMA_RESOURCE_PRESSURE_OBSERVER_UNSUPPORTED`. This is
not pressure payload typing, observer execution, or runtime pressure-stream
support; those remain open IM-D4/IM-D5 implementation work.

| Closed item | Evidence | Verification |
|-------------|----------|--------------|
| Matrix literal invalid diagnostic refinement | Invalid matrix literal shape/context failures now report `STYIO_TYPE_MATRIX_LITERAL_INVALID` instead of the broad `STYIO_TYPE_ERROR` code while staying in the TypeError exit family. `StyioDiagnostics.MatrixBindingRaggedLiteralReportsTypeCode` covers direct typed matrix binding with inconsistent row lengths, and `StyioDiagnostics.MatrixFunctionReturnFlatListFailsBeforeRuntime` covers explicit `matrix` function returns whose flat-list tail is rejected before runtime. This is diagnostic refinement only; it does not broaden matrix literal acceptance, matrix return compatibility, matrix operators, resource-method matrix bodies, or matrix runtime behavior. | `ctest --test-dir build -R '^StyioDiagnostics\.(MatrixBindingRaggedLiteralReportsTypeCode\|MatrixFunctionReturnFlatListFailsBeforeRuntime)$' --output-on-failure` passed on 2026-05-31 |
| `f32` dtype mapping and nearby numeric-promotion regression | `f32` maps to internal name `f32` in [src/StyioToken/Token.hpp](../../src/StyioToken/Token.hpp), and focused coverage exists in [tests/styio_test.cpp](../../tests/styio_test.cpp) (`StyioTypes.F32BuiltinMappingUsesF32InternalName`, `StyioTypes.GetMaxTypeNumericPromotionByBitWidth`) | `ctest --test-dir build-codex -R '^(StyioTypes\.F32BuiltinMappingUsesF32InternalName\|StyioTypes\.GetMaxTypeNumericPromotionByBitWidth)$' --output-on-failure` passed on 2026-04-21 |
| final-binding positive smoke coverage in the automated feature matrix | [tests/CMakeLists.txt](../../tests/CMakeLists.txt) registers final-binding positive fixtures via `styio_feature_stdout_golden_test(final_bindings "t*.styio" final_bindings)`, and [../../workflows/TEST-CATALOG.md](../../workflows/TEST-CATALOG.md) lists `final_bindings_t01_bounded_final_bind`, `final_bindings_t02_bounded_read`, and `final_bindings_t14_flex_other_var_after_final_ok` | `ctest --test-dir build/default -R '^(final_bindings_t01_bounded_final_bind|final_bindings_t02_bounded_read|final_bindings_t14_flex_other_var_after_final_ok)$' --output-on-failure` |
| `stdio semantic drain request-loop integration` | `Server::run()` now drains runtime diagnostics on each request and `styio_ide_test` asserts the serialized loop output matches `handle + drain_runtime()` (`StyioLspServer.RunDrainsRuntimeDiagnostics`) | `ctest --test-dir build-codex --tests-regex 'StyioLspServer.RunDrainsRuntimeDiagnostics' --output-on-failure` passed on 2026-04-21 |
| `CP-B0.2 runtime scheduling freeze` | Request-loop runtime diagnostics are budgeted in `Server::run()` (`kRuntimeDrainBudgetPerLoop = 1`), `IdeService::run_idle_tasks()` drains semantic diagnostics before budgeted background work, and stale/late updates are dropped by snapshot-version sequencing in `IdeService` | `ctest --test-dir build-codex -L ide --tests-regex 'StyioLspRuntime.RuntimeDrainCanBeBudgetedForScheduling|StyioLspRuntime.IdleSliceDrainsSemanticBeforeBackgroundWork|StyioLspRuntime.RunAdvancesBackgroundWorkAsRequestDrivenFallback|StyioLspServer.RunDrainsRuntimeDiagnostics' --output-on-failure` passed on 2026-04-22 |
| Format strings (`$"..."`) parser/sema/lowering closure | [src/StyioParser/Parser.cpp](../../src/StyioParser/Parser.cpp) parses token-level format strings for both parser engines, [src/StyioSema/TypeInfer.cpp](../../src/StyioSema/TypeInfer.cpp) infers embedded expressions as `string`, [src/StyioLowering/AstToStyioIR.cpp](../../src/StyioLowering/AstToStyioIR.cpp) lowers through existing string concatenation, and [tests/features/stdio_output/t06_stdout_fmtstr.styio](../../tests/features/stdio_output/t06_stdout_fmtstr.styio) now exercises real `$"..."` syntax | `ctest --test-dir build/default -R '^(StyioParserEngine\.LegacyAndNightlyMatchOnStdioOutputFmtStringSample|stdio_output_t06_stdout_fmtstr)$' --output-on-failure` passed on 2026-05-10 |
| Hash-tag iterator sequence fail-closed | `IterSeqAST` no longer silently executes as `SGConstInt(0)`; sema/lowering paths reject undefined hash-tag routing with `STYIO_TYPE_STREAM_HASH_TAG_ROUTE_UNSUPPORTED`, and [tests/styio_test.cpp](../../tests/styio_test.cpp) covers the regression. This remains a fail-closed boundary after the IM-D5-P1 retirement decision; no hash-tag route semantics are implemented. | `ctest --test-dir build -R '^StyioDiagnostics\.IteratorSequenceHashTagRoutingReportsFeatureCode$' --output-on-failure` passed on 2026-05-30 |
| Fuzz session lifetime and parser recovery leaks | Tokenizer accumulation, legacy main-block parsing, hash-function parts, return-type parsing, parenthesized expressions, and call arguments now retain RAII ownership until handoff, fuzz leak seeds are backflowed under [tests/fuzz/corpus/](../../tests/fuzz/corpus/), and [tests/security/styio_security_test.cpp](../../tests/security/styio_security_test.cpp) covers the lexer/parser paths under `CompilationSession` | `ASAN_OPTIONS=detect_leaks=1:detect_container_overflow=0 ctest --test-dir build/asan-ubsan -R '^StyioSecurity(Lexer\.UnterminatedStringAfterOwnedTokensStaysExceptionSafe\|ParserContext\.HashFunctionFuzzSeedStaysExceptionSafe)$' --output-on-failure` passed on 2026-05-12 |
| Internal operator fallback fail-closed | Unknown comparison, list, logical, and binary IR operator values now throw `StyioTypeError` instead of lowering/emitting equality, constant zero, or left-operand fallback in [src/StyioLowering/AstToStyioIR.cpp](../../src/StyioLowering/AstToStyioIR.cpp) and [src/StyioCodeGen/CodeGenG.cpp](../../src/StyioCodeGen/CodeGenG.cpp) | `ctest --test-dir build/default -R '^StyioSecurityNightlyCodegen\.(LogicalNotAndXorLowerWithoutLeftOperandFallback\|UnsupportedInternalBinaryOperatorFailsClosed\|UnsupportedInternalLoweringOperatorsFailClosed)$' --output-on-failure` passed on 2026-05-12 |
| IM-D1 StyioIR contract slice | Direct unsupported AST lowering now fails closed, intentional empty forms lower to `SGNoOp`, `char` lowering/type/codegen is real, compiler-owned scalar casts lower to value-carrying `SGCast`, tuple function return annotations fail closed instead of becoming `i64`, accepted match cases now run Sema over scrutinee/patterns/arm bodies, keep branch-local bindings isolated, preserve `i64`/`f64`/`bool`/`char`/`string` branch result families through `SGMatch` and LLVM PHI widths, and reject container match result families before lowering. The independent StyioIR verifier gates codegen, and the remaining zero/default uses plus empty sema visitors are recorded in [IM-D1-STYIOIR-CONTRACT-INVENTORY.md](./IM-D1-STYIOIR-CONTRACT-INVENTORY.md). | `ctest --test-dir build -R '^(StyioDiagnostics\.(FunctionMatchSugarAndTailExpressionsReturnValues\|MatchBoolAndCharTailValuesPreserveScalarFamilies\|MatchBranchLocalTailValuesRemainExecutable)\|StyioSecurityNightlySemantics\.(MatchBoolAndCharLowerToNativeScalarWidths\|RejectsUnsupportedContainerMatchResultBeforeLowering\|RejectsUndefinedMatchArmTailValue\|MatchArmBindingsDoNotLeakAcrossBranches\|FunctionMatchSugarInfersArmBodiesOnCall\|FunctionMatchSugarArmBindingsStayBranchScoped))$' --output-on-failure` passed on 2026-05-30; prior scalar/inferred return and tuple fail-closed coverage passed on 2026-05-26, and prior `StyioIRContract`/`StyioSecurityNightlyCodegen` gates passed on 2026-05-22 |
| Resource method helper inline-clone slices | Resource method definition bodies now accept single-quoted `char` literals, token-level format strings, scalar leaf values, scalar local `=` / `:=` prefaces or local list/dict/matrix `=` / `:=` prefaces before a final scalar/string or local list/dict/matrix container returned expression, dynamic range literals, block-form function calls with value-producing called-function tails, and value-producing resource-effect expressions through the parser path used by resource method bodies, and state/resource-method inline cloning copies `IntAST`/`BoolAST`/`FloatAST`/`StringAST`, `CharAST`, `FmtStrAST`, scalar local `FlexBindAST`/`FinalBindAST` prefaces plus local list/dict/matrix `FlexBindAST`/`FinalBindAST` prefaces that return scalar/string tails or local container handles, `RangeAST`, `FuncCallAST`, and `ResourceEffectAST` result metadata instead of falling through to the unsupported clone diagnostic. `@file::marker = () => { >_('x') }`, `@file::summary = () => { $"value={1 + 2}" -> @stdout }`, `@file::flag = () => { <| true }`, `@file::summary = (x: int) => { <| $"value={x + 1}" }`, `@file::answer = () => { x = 41 <| x + 1 }`, `@file::answer = () => { x := 41 <| x + 1 }`, `@file::list_answer = () => { xs := [41,42] <| xs }`, `@file::dict_answer = () => { d := dict{"a": 40, "b": 2} <| d }`, `@file::matrix_answer = () => { m: matrix := [[40,2],[1,3]] <| m }`, `@file::matrix_cell = () => { m: matrix := [[42,2],[1,3]] <| m[0][0] }`, `@file::span = (start: int, stop: int, step: int) => { <| [start..stop..step] }`, `# plus_one := (x: i64) => { <| x + 1 }` plus `@file::score = (x: i64) => { <| plus_one(x) }`, and `@file::read_or = () => { <| ?| (<< @file("data.txt")) | io => 8 | 7 }` inline and execute after resource method calls or under `?| method() | fallback`, while invalid multi-byte char literals, malformed format strings, local resource binding prefaces, lexical/global captures, returned container match results, returned statement-only function calls, returned resource-effect discard, format-string fallback type mismatches, and non-integer range bounds fail closed. Remaining state inline clone coverage is still open for other accepted AST families that have not been proven through source-reachable resource/state helper bodies. | `ctest --test-dir build -R '^(StyioDiagnostics\.ResourceMethod(CharLiteralBodyRunsAfterInlineClone\|FmtStringBodyRunsAfterInlineClone\|RangeBodyRunsAfterInlineClone\|InvalidCharLiteralFailsClosed\|InvalidFmtStringFailsClosed\|InvalidRangeBoundFailsClosed)\|StyioResourceEffects\.(ValueResourceMethod(Local(FlexBind\|FinalBind)ReturnsFromDirectAndResourceEffectCalls\|Local(ListDictMatrixBindsReturnScalarValues\|ContainerReturnsFromDirectAndFallbackCalls)\|ScalarFamiliesReturnFromDirectAndFallbackCalls\|Returned(BlockFunctionCallsInferResultFamilies\|ResourceEffectRecoversInsideMethod))\|ResourceMethod(FmtStringValueFallbackTypeMismatchReportsTypeCode\|ReturnedStatementOnlyFunctionReportsSemaCode))\|StyioSecurityNightlyParserStmt\.(ParsesResourceMethodLocal(FlexBind\|FinalBind)ReturnExpression\|ParsesResourceMethodLocalListDictMatrixBindReturnExpressions\|ParsesResourceMethodReturned(Range\|ScalarFmt\|ResourceEffect\|BlockFunctionCall)FallbackExpression\|RejectsResourceMethodReturnedResourceEffectDiscardExpression)\|StyioSecurityNightlySemantics\.(ResourceMethodLocalContainerReturnsLowerThroughValueScope\|RejectsResourceMethodLocalResourceBindBeforeResourceValueScopeSupport\|RejectsResourceMethodGlobalMatrixCaptureBeforeLexicalCaptureSupport\|RejectsResourceMethodReturnedStatementOnlyFunctionCall))$' --output-on-failure` passed on 2026-05-31 |
| IM-D2 parser-authority contract slice | Syntax check is locked to authoritative nightly parsing, non-authoritative parser engines are rejected, nightly fallback points fail closed, and IDE semantic analysis no longer recovers later facts from malformed source | `ctest --test-dir build/default -R '^(StyioDiagnostics\.SyntaxCheckRejectsNonAuthoritativeParserEngine|StyioSemanticBridge\.RejectsMalformedInputWithoutRecovery|StyioSyntaxDrift\.CorpusMatchesApprovedEnvelope)$' --output-on-failure` |
| IM-D3 diagnostic contract baseline | Shared diagnostic taxonomy, syntax-check codes, main CLI JSONL diagnostic envelopes, runtime helper primary codes, sema/type JSONL baseline, IDE diagnostic code/phase fields, and LSP diagnostic code mapping are recorded in [IM-D3-DIAGNOSTIC-CONTRACT-INVENTORY.md](./IM-D3-DIAGNOSTIC-CONTRACT-INVENTORY.md) | `ctest --test-dir build -R '^(StyioDiagnostics\.(SyntaxCheckJsonReportsParseFailure|SyntaxCheckJsonReportsMultipleRecoveredParseFailures|SyntaxCheckJsonReportsLexFailure|SyntaxCheckRejectsNonAuthoritativeParserEngine|CompilePlanFailureWritesJsonlDiagnosticIntoDiagDir|CompilePlanInvalidIntentReportsCliDiagnosticAndWritesDiagDir|CompilePlanInvalidBuildModeReportsCliDiagnosticAndWritesDiagDir|CompilePlanCliConflictReportsCliDiagnosticAndWritesDiagDir|CompilePlanInvalidJsonReportsMachineReadableCliDiagnostic|RuntimeHelperErrorEmitsJsonlRuntimeDiagnostic|RuntimeWriteHelperErrorEmitsJsonlRuntimeDiagnostic|InvalidNumericStdinArgumentEmitsJsonlRuntimeDiagnostic|CompoundAssignOnImmutableBindingReportsSemaCode|TupleFunctionReturnAnnotationReportsTypeCode|StreamZipUnsupportedSourceReportsFeatureCode|IteratorSequenceHashTagRoutingReportsFeatureCode)|StyioResourceEffects\.(ResourceMethodValueFallbackTypeMismatchReportsTypeCode|ResourceMethodReturnedStatementOnlyFunctionReportsSemaCode)|StyioSemanticBridge\.RejectsMalformedInputWithoutRecovery|StyioLspServer\.PublishDiagnosticsCarriesStyioCodeAndPhase)$' --output-on-failure` |
| IM-D3 immutable binding diagnostic refinement | Immutable/final binding mutation failures no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` now maps stable immutable/final binding mutation messages to `STYIO_SEMA_IMMUTABLE_BINDING`, and focused JSONL coverage proves compound assignment to a final binding keeps the TypeError exit family while reporting public phase `sema`. | `ctest --test-dir build -R '^StyioDiagnostics\.CompoundAssignOnImmutableBindingReportsSemaCode$' --output-on-failure` passed on 2026-05-30 |
| IM-D3 tuple return diagnostic refinement | Tuple function return annotations no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` now maps the stable tuple-value-IR message to `STYIO_TYPE_UNSUPPORTED_TUPLE_RETURN`, and focused JSONL coverage proves the public phase stays `type` while tuple return annotations remain fail-closed until tuple value IR exists. | `ctest --test-dir build -R '^StyioDiagnostics\.TupleFunctionReturnAnnotationReportsTypeCode$' --output-on-failure` passed on 2026-05-30 |
| IM-D3 hash-tag route diagnostic refinement | Undefined hash-tag iterator sequence routes no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` now maps the stable fail-closed message to `STYIO_TYPE_STREAM_HASH_TAG_ROUTE_UNSUPPORTED`, and focused JSONL coverage proves the public phase stays `type` after IM-D5-P1 retired hash-tag route semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.IteratorSequenceHashTagRoutingReportsFeatureCode$' --output-on-failure` passed on 2026-05-30 |
| IM-D3 stream zip unsupported-source diagnostic refinement | Non-iterable/scalar stream zip inputs no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` now maps the stable stream zip unsupported-source message to `STYIO_TYPE_STREAM_ZIP_UNSUPPORTED_SOURCE`, and focused JSONL tests cover both a scalar binding input and a scalar resource selector input without changing stream semantics. | `ctest --test-dir build -R '^(StyioDiagnostics\.StreamZipUnsupportedSourceReportsFeatureCode|StyioStreamZip\.ResourceScalarSelectorFailsClosedAsZipInput)$' --output-on-failure` passed on 2026-05-30 |
| IM-D3 duplicate stdin zip diagnostic refinement | Duplicate `@stdin & @stdin` stream zip inputs no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` now maps the stable duplicate-driver decision-boundary message to `STYIO_TYPE_STREAM_DUPLICATE_DRIVER_UNSUPPORTED`, and focused JSONL coverage proves the public phase stays `type` while duplicate external-input consumption remains fail-closed by owner decision until an explicit tee/buffer or duplicate-driver design is accepted. | `ctest --test-dir build -R '^StyioDiagnostics\.DuplicateStdinZipReportsFeatureCode$' --output-on-failure` passed on 2026-05-31 |
| IM-D3 iterator unsupported-source diagnostic refinement | Ordinary iterator sources that are not iterable no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` now maps the stable iterator unsupported-source message to `STYIO_TYPE_ITERATION_UNSUPPORTED_SOURCE`, and focused JSONL coverage proves ordinary scalar iterators and scalar latest selector iterators remain fail-closed without changing iterator, selector, or stream-driver semantics. | `ctest --test-dir build -R '^Styio(Diagnostics\.IteratorUnsupportedSourceReportsFeatureCode|ResourceTopology\.ResourceScalarSelectorIteratorFailsClosed)$' --output-on-failure` passed on 2026-05-31 |
| IM-D3 typed stdin unsupported-target diagnostic refinement | Unsupported explicit-target stdin scalar/list families such as `bool` and `list[bool]` no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` now maps the stable typed stdin unsupported-target messages to `STYIO_TYPE_STDIN_UNSUPPORTED_TARGET`, and focused JSONL tests cover both scalar and list target routes without changing supported stdin target families. | `ctest --test-dir build -R '^StyioDiagnostics\.UnsupportedTypedStdin(ListTarget\|Target)ReportsTypeCode$' --output-on-failure` passed on 2026-05-31 |
| IM-D3 undeclared-symbol diagnostic refinement | Unresolved user function calls and unknown Topology resource references no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` maps the stable unknown function/resource messages to `STYIO_SEMA_UNDECLARED_SYMBOL`, and focused JSONL tests prove the public phase is `sema`, the TypeError exit family remains unchanged, and following output does not execute. This is diagnostic-only and does not broaden symbol resolution, hidden native symbol visibility, import behavior, or resource lookup semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.(UnknownFunctionReportsSemaUndeclaredSymbolCode|UnknownResourceReportsSemaUndeclaredSymbolCode)$' --output-on-failure` passed on 2026-05-30 |
| IM-D3 call-arity diagnostic refinement | User function and resource-method call arity mismatches no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` maps stable `expects N argument(s), got M` messages to `STYIO_SEMA_CALL_ARITY_MISMATCH`, and focused JSONL tests prove the public phase is `sema`, the TypeError exit family remains unchanged, and following output does not execute. This is diagnostic-only and does not broaden function calling or resource-method dispatch semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.(UserFunctionArityReportsSemaCallArityMismatchCode|ResourceMethodArityReportsSemaCallArityMismatchCode)$' --output-on-failure` passed on 2026-05-30 |
| IM-D3 call argument-type diagnostic refinement | User function and resource-method argument type mismatches no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` maps stable call argument mismatch messages to `STYIO_TYPE_CALL_ARGUMENT_MISMATCH`, and focused JSONL tests prove the public phase is `type`, the TypeError exit family remains unchanged, and following output does not execute. This is diagnostic-only and does not broaden function calling, implicit argument adaptation, native ABI behavior, or resource-method dispatch semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.(UserFunctionArgumentMismatchReportsTypeCallArgumentMismatchCode|ResourceMethodArgumentMismatchReportsTypeCallArgumentMismatchCode)$' --output-on-failure` passed on 2026-05-30 |
| IM-D3 resource-effect value diagnostic refinement | Resource-effect fallback type mismatches no longer report only the broad `STYIO_TYPE_ERROR`; `DiagnosticContract.hpp` maps the stable fallback mismatch message to `STYIO_TYPE_RESOURCE_EFFECT_FALLBACK_MISMATCH`. Unsupported resource-method bodies such as statement-only called-function returns and local resource-binding method bodies now map to `STYIO_SEMA_RESOURCE_METHOD_UNSUPPORTED_BODY`; local list/dict/matrix container returns are no longer part of this fail-closed diagnostic slice. Focused JSONL coverage proves the public phases stay `type` and `sema` respectively, the TypeError exit family remains unchanged, and execution stops before following output. This is diagnostic-only and does not broaden resource-effect recovery or unsupported resource-method body semantics. | `ctest --test-dir build -R '^StyioResourceEffects\.(ResourceMethodValueFallbackTypeMismatchReportsTypeCode|ResourceMethodReturnedStatementOnlyFunctionReportsSemaCode)$' --output-on-failure` passed on 2026-05-31 |
| IM-D3 native interop diagnostic refinement | Native `@extern` referenced-source, explicit binding/signature, unsupported-signature, host compiler rejection, native artifact load failure, missing exported symbol, and unavailable toolchain configuration failures no longer report only broad type/native fallback codes. `DiagnosticContract.hpp` maps missing referenced sources to `STYIO_NATIVE_SOURCE_READ_FAILED`, explicit binding misses to `STYIO_NATIVE_SIGNATURE_NOT_FOUND`, unsupported signature shapes such as aggregate parameters or variadic signatures to `STYIO_NATIVE_UNSUPPORTED_SIGNATURE`, host compiler rejections to `STYIO_NATIVE_HOST_COMPILE_FAILED`, native artifact load failures to `STYIO_NATIVE_LOAD_FAILED`, missing exported symbols to `STYIO_NATIVE_SYMBOL_MISSING`, and unavailable toolchain configuration to `STYIO_NATIVE_TOOLCHAIN_UNAVAILABLE`; focused JSONL coverage proves the public phase is `native_interop`, the TypeError exit family remains unchanged, and following output does not execute. This is diagnostic-only and does not broaden native ABI support, signature support, artifact loading, symbol visibility, C++ symbol mapping, or host toolchain behavior. | `ctest --test-dir build -R '^StyioDiagnostics\.(NativeExternMissingSourceReportsNativeCode\|NativeExternMissingBindingReportsNativeCode\|NativeExternUnsupportedSignatureReportsNativeCode\|NativeExternVariadicSignatureReportsNativeCode\|NativeExternHostCompileFailureReportsNativeCode\|NativeExternSymbolMissingReportsNativeCode\|NativeExternToolchainUnavailableReportsNativeCode\|NativeExternLoadFailureReportsNativeCode)$' --output-on-failure` passed on 2026-05-31 |
| Nano static repository negative-path baseline | [tests/styio_test.cpp](../../tests/styio_test.cpp) covers invalid static repository marker rejection, blob SHA256 mismatch rejection, blob size mismatch rejection, malformed entry-schema rejection, malformed cloud manifest rejection, HTTP(S) publish-root rejection, and create/publish CLI guard rejection without adding package-manager lifecycle behavior to the compiler CLI | `ctest --test-dir build -R '^StyioNanoPackage\.(CloudRepositoryRejectsMalformedEntrySchema|CloudManifestRejectsMalformedPackageFile|CliRejectsMutuallyExclusiveAndMisdirectedOptions)$' --output-on-failure` passed on 2026-05-26; prior marker/blob/remote-publish coverage remains in the same test group |
| Range literal integer expression bounds | The nightly list parser accepts `[start..end]` and `[start..end..step]` with expression operands, Sema requires all range operands to infer integer types, lowering preserves constant ranges as list literals and lowers expression-bound ranges through `__styio_list_range_i64`, and codegen expands that internal helper to a `styio_list_new_i64` / `styio_list_push_i64` runtime loop tracked as an owned list handle. Non-integer bounds fail closed before LLVM emission. | `ctest --test-dir build -R '^(StyioParserEngine\.(RangeLiteralIteratesInclusivelyAndIgnoresDotCount\|RangeLiteralExpressionBoundsMaterializeList\|RangeLiteralRejectsNonIntegerExpressionBounds)\|StyioSecurityNightlyCodegen\.DynamicRangeLiteralLowersToRuntimeListLoop)$' --output-on-failure` passed on 2026-05-26 |
| Bounded resource topology selector slice/snapshot semantics for `i64`/`f64`/`bool`/`char`/`string`/`list`/`dict` | `ResourceRefAST` type inference now distinguishes scalar/latest offset selectors from bounded `i64`/`f64`/`bool`/`char`/`string`/`list`/`dict` slice/snapshot selectors; lowering materializes `@name[-n..]` and `@name[...]` as list literals from explicit history reads instead of collapsing them to whole-resource `SGResId`; bounded char rings store `i8` values and materialize `list[char]` snapshots, bounded string rings clone stored values and release ring-owned cstrs on overwrite and scope cleanup, and bounded list/dict rings clone runtime handles on write/read while releasing ring-owned handles on overwrite or cleanup; `snapshot << @name[-n..]` and `snapshot << @name[...]` now bind scalar/string/char/bool/f64/i64 materialized lists and list/dict handle snapshot lists through the explicit-copy surface; [tests/features/state_resources/t06_topology_selector_snapshot.styio](../../tests/features/state_resources/t06_topology_selector_snapshot.styio), [tests/features/state_resources/t07_topology_selector_snapshot_f64.styio](../../tests/features/state_resources/t07_topology_selector_snapshot_f64.styio), [tests/features/state_resources/t08_topology_selector_snapshot_bool.styio](../../tests/features/state_resources/t08_topology_selector_snapshot_bool.styio), [tests/features/state_resources/t09_topology_selector_explicit_copy.styio](../../tests/features/state_resources/t09_topology_selector_explicit_copy.styio), [tests/features/state_resources/t10_topology_selector_snapshot_string.styio](../../tests/features/state_resources/t10_topology_selector_snapshot_string.styio), [tests/features/state_resources/t11_topology_selector_snapshot_char.styio](../../tests/features/state_resources/t11_topology_selector_snapshot_char.styio), [tests/features/state_resources/e05_selector_slice_exceeds_bound.styio](../../tests/features/state_resources/e05_selector_slice_exceeds_bound.styio), [tests/features/state_resources/e06_selector_snapshot_unbounded_matrix_unsupported.styio](../../tests/features/state_resources/e06_selector_snapshot_unbounded_matrix_unsupported.styio), [tests/features/state_resources/e07_selector_copy_scalar_unsupported.styio](../../tests/features/state_resources/e07_selector_copy_scalar_unsupported.styio), `StyioResourceTopology.ResourceSelectorHandleSnapshotsCloneListAndDictValues`, `StyioResourceTopology.ResourceSelectorHandleSnapshotCopiesStayMaterialized`, and `StyioSecurityResourceTopology.LowersBoundedListAndDictResourceSelectors` cover positive and fail-closed behavior | `ctest --test-dir build -L state_resources --output-on-failure` passed on 2026-05-26; focused list/dict selector handle and handle-copy tests passed on 2026-05-30; focused char selector and adjacent matrix-boundary tests passed on 2026-05-30 |
| Materialized list/dict/matrix explicit clone slice | `HandleAcquireAST` now keeps `<-` for external resource acquire/receive and task pulls: bound cloneable resources such as materialized `list`/`dict`/`matrix` handles fail closed with `copy <- source` and must use `copy << source`. The accepted `copy << source` path lowers through `SCListClone` / `SCDictClone` / `SCMatrixClone`, emits `styio_list_clone` / `styio_dict_clone` / `styio_matrix_clone_i64` / `styio_matrix_clone_f64`, and runtime coverage proves source mutations do not alias the copied list, nested list element, dict handle, or matrix handle. File, topology-resource, and broader resource-family clone/copy semantics remain open. | `ctest --test-dir build -R '^(StyioResourceCopy\.Explicit(ListAndDictCloneAreIndependent\|MatrixCloneIsIndependent)\|StyioSecurityNightlySemantics\.(Rejects(BoundResourceCopyByLeftArrow\|MatrixCopyByLeftArrow)\|AllowsExplicit(CloneFormAndIndexedMutation\|MatrixCloneForm)\|AllowsDictIndexingAttrsAndClone))$' --output-on-failure` passed on 2026-05-26 |
| File resource default statement settlement slice | Plain file resource operations outside a `?|` recovery wrapper now emit the default runtime guard at the covered operation boundary: `f <- @file(missing)`, `"x" >> @file(missing-dir/out)`, direct `@file(missing).close()`, and direct `@file(missing) >> #(line)` iteration produce JSONL runtime diagnostics and stop before following statements; successful direct `@file(data).close()` continues to the next statement. The operation-local guard is suppressed while `SIOResourceEffect` dispatches catch-all fallback or named handlers, so explicit recovery continues to own `?| ... | fallback` behavior. This is a file acquire/write/release/iterator statement-boundary slice only; arbitrary value-producing resource-operation recovery beyond the instant-pull slices, source-level fallback recovery for implicit cleanup and non-file reassignment cleanup, pressure observers, and broader resource-family settlement remain open. | `ctest --test-dir build -R '^(StyioDiagnostics\.RuntimeFile(Acquire\|Write\|IteratorOpen\|Release)FailureStopsBeforeNextStatement\|StyioResourceLifecycle\.DirectFileReleaseSuccessContinues\|StyioResourceEffects\.(FallbackRunsAfterFileWriteFailureAndContinues\|NamedIoHandlerRunsForFileWriteFailure\|NamedCleanupHandlerRunsForFileCloseFailure))$' --output-on-failure` passed on 2026-05-26 |
| File resource flex-rebind lifecycle and cleanup settlement slice | A successful flex resource rebind now represents a fresh occupant after a consuming `.close()`: Sema clears the destroyed-receiver mark for accepted `name = @file(...)`, Codegen releases a tracked prior file handle before RHS acquire/overwrite, explicit release keeps named slots zero for idempotent cleanup, and same-path singleton slots are reopened when a later acquire finds the slot closed. The default no-fallback cleanup-failure boundary is also covered: file flex-rebind closes the old handle, checks `styio_runtime_has_error`, reports `STYIO_RUNTIME_FILE_CLEANUP_FAILURE` when close fails, and stops before opening the replacement resource or executing following statements. Statement-shaped file rebind now also enters the explicit resource-effect route: `?| f = @file(missing) | fallback` and matched `io` handlers recover replacement open failures, scalar flex binds remain rejected, value-required rebind expressions remain rejected, and codegen branches prior-handle cleanup failure to `SIOResourceEffect` before replacement open. This covers the source-reachable file rebind edge only; source-level fallback recovery for implicit cleanup, non-file reassignment cleanup, non-file cleanup families, and arbitrary value-producing resource recovery beyond the instant-pull slices remain open. | `ctest --test-dir build -R '^(StyioSecurityNightlyCodegen\.RebindRunsFileCleanupBeforeNextAcquire\|StyioResourceLifecycle\.(FlexFileRebindReleasesOldFileAndContinues\|FlexFileRebindCleanupFailureStopsBeforeOverwrite\|FlexFileRebindAfterCloseReopensSamePath\|FileAliasUseAfterCloseFailsFast)\|StyioResourceEffects\.(NamedCleanupHandlerRunsForFileCloseFailure\|IoHandlerDoesNotCatchFileCloseCleanupFailure))$' --output-on-failure` passed on 2026-05-29; focused file-rebind resource-effect coverage `ctest --test-dir build -R '^(StyioResourceEffects\.FlexRebind(Fallback|NamedIoHandler)RecoversFileOpenFailure|StyioSecurityNightlyParserStmt\.(ParsesResourceEffectFileRebindStatement|RejectsScalarFlexBindResourceEffectStatement|RejectsFileRebindResourceEffectExpression))$' --output-on-failure` passed on 2026-05-30; `ctest --test-dir build -R '^(stream_processing_t07_singleton\|parser_shadow_gate_stream_processing_zero_(fallback\|internal_bridges)\|parser_shadow_gate_file_resources_dual_zero_expected_nonzero)$' --output-on-failure` passed on 2026-05-26 |
| File scope-exit, explicit-return, and loop-exit cleanup settlement slice | Tracked file handles now close on ordinary scope-pop cleanup, on explicit `<| return` before the LLVM `ret`, and on loop `^` break / standalone `>>` continue branches before jumping to the loop target. Codegen separates the post-cleanup runtime-error guard from the ordinary guard so cleanup failures are settled after cleanup without recursively running cleanup again, keeps file handle slots entry-allocated so same-path singleton reuse across loop branches satisfies LLVM dominance, and function-body codegen saves/restores file/dynamic resource scope state so function-local cleanup stacks do not leak into later codegen. Top-level break/continue remain fail-closed. This covers tracked file handles only; source-level fallback recovery for implicit cleanup and non-file reassignment cleanup, release/commit hooks, non-file cleanup families, and pressure observers remain open. | `ctest --test-dir build -R '^(StyioSecurityNightlyCodegen\.(BreakRunsFileScopeCleanupBeforeLoopExitBranch\|ContinueRunsFileScopeCleanupBeforeLoopStepBranch\|RejectsBreakAndContinueOutsideLoop\|ReturnRunsFileScopeCleanupBeforeRet)\|StyioResourceLifecycle\.(LoopBreakAndContinueRunFileScopeCleanupSmoke\|FunctionReturnRunsFileScopeCleanupSmoke\|FlexFileRebindAfterCloseReopensSamePath\|FileAliasUseAfterCloseFailsFast))$' --output-on-failure` passed on 2026-05-29; `ctest --test-dir build -R '^control_flow_' --output-on-failure` passed on 2026-05-29 |
| File iterator closed-alias settlement slice | File iterator lowering now distinguishes runtime failure from normal EOF: `styio_file_read_line(0)` / `styio_file_rewind(0)` materialize `STYIO_RUNTIME_INVALID_FILE_HANDLE`, and `SIOFileLineIter` checks the runtime error channel before treating a null line as EOF. A shared same-path alias used after another alias closes the slot now fails fast with the `closed` family instead of silently ending the iterator. This covers source-reachable file alias invalidation for iterator use only; source-level fallback recovery for implicit cleanup and non-file reassignment cleanup, non-file cleanup families, pressure observers, and arbitrary value-producing resource recovery beyond the instant-pull slices remain open. | `ctest --test-dir build -R '^(StyioResourceLifecycle\.FileAliasUseAfterCloseFailsFast\|StyioSafetyRuntime\.(ZeroFileHandleReadSetsRuntimeError\|InvalidHandleOperationsAreSafe\|FirstErrorWinsAcrossMultipleRuntimeFailures)\|StyioFiveLayerPipeline\.(P04_read_file\|P06_zip_files\|P09_full_pipeline\|P10_auto_detect_read\|P11_pipe_func)\|stream_processing_t07_singleton)$' --output-on-failure` passed on 2026-05-26 |
| Resource-effect statement fallback and named-handler slice | `ResourceEffectAST` and `SIOResourceEffect` now wrap non-task `?| resource_operation` statements, carry ordered named handlers, reject unknown or duplicate handler names, reject `effect => @()`, and dispatch matched current runtime subcode families through `styio_runtime_error_matches_effect`. Parser, sema, lowering, verifier, optimizer, codegen, runtime, and ORC registration accept catch-all `?| resource_operation | fallback`, statement discard, `?| resource_operation | effect => handler`, handler chains, a final catch-all fallback after unmatched handlers, statement-shaped file handle acquire such as `?| f <- @file("data.txt") | fallback`, statement-shaped file rebind such as `?| f = @file("next.txt") | fallback`, direct file iterators such as `?| @file("data.txt") >> #(line) => { ... } | fallback`, direct file release such as `?| @file("data.txt") -> @() | fallback`, and statement-shaped resource method calls such as `?| @file("data.txt").close() | fallback`. Runtime smoke coverage proves file-write fallback and named `io` handlers run only after the materialized error is cleared, successful writes skip recovery, unmatched `backpressure` for file-open failure remains fail-fast without catch-all fallback, handler chains can fall through to final catch-all fallback, and no-fallback settlement stops before the following statement. File handle acquire coverage proves missing files recover through catch-all fallback or matched `io` handlers after clearing `STYIO_RUNTIME_FILE_OPEN_READ`, successful opens skip fallback and leave the acquired file handle usable by a following iterator, later resource-effect close method, later direct release to `@()`, later guarded `f.write(...)` write method, or later value-producing `?| (<< f) | fallback` instant-pull expression, no-fallback acquire settlement fails fast, value-required acquire expressions stay rejected, later close or direct release consumes the receiver for topology use-after-destroy checks, and later iterator, write-method, or instant-pull use after a recovered failed acquire reports `STYIO_RUNTIME_INVALID_FILE_HANDLE` instead of treating the zeroed slot as EOF or recreating the file by path. Direct file iterator coverage proves success skips fallback, missing-file open failures recover through catch-all fallback or matched `io` handlers after clearing `STYIO_RUNTIME_FILE_OPEN_READ`, no-fallback iterator settlement fails fast before the following statement, and non-file iterators such as list iteration stay rejected under `?|`. Direct file release coverage proves success skips recovery, missing-file open failures recover through catch-all fallback or matched `io` handlers after clearing `STYIO_RUNTIME_FILE_OPEN_READ`, no-fallback release settlement fails fast, acquired-handle release lowers through `SIOHandleRelease`, and value-required release expressions stay rejected. Direct file close method coverage proves success skips recovery, missing files recover through catch-all fallback or matched `io` handlers after clearing `STYIO_RUNTIME_FILE_OPEN_READ`, no-fallback close settlement fails fast, and non-resource member calls stay rejected under `?|`. File rebind coverage proves `?| f = @file(missing) | fallback` recovers replacement open failures, matched `io` handlers recover the same failure family, scalar flex binds stay fail-closed, value-required rebind expressions stay rejected, and prior-handle cleanup failures branch to wrapper dispatch before replacement open. File-close cleanup failure now reports `STYIO_RUNTIME_FILE_CLEANUP_FAILURE`, matches `cleanup`, and stays distinct from `io`; source-level fallback recovery for implicit cleanup and non-file reassignment cleanup, non-file cleanup families, non-failure `ResourceBackpressure` events, pressure observers, broader post-acquire resource operations beyond the covered file iterator, close-method, direct-release, write-method, and acquired-handle instant-pull paths, failing value-producing resource methods beyond returned file/stdin instant pulls, returned local list/dict/matrix container handles, and returned list/dict/matrix bounds slices, local resource binding value-producing resource method bodies, resource-method lexical/global captures, and arbitrary value-producing recovery beyond the covered value slices remain open. | `ctest --test-dir build -R '^(StyioSecurityNightlyParserStmt\.(ParsesResourceEffect(DiscardStatement\|FallbackStatement\|NamedHandlerStatement\|ResourceMethodStatement\|HandleAcquireStatement\|FileIteratorStatement)\|ResourceEffectHandleAcquireFeedsLater(Iterator\|CloseMethod\|InstantPull)\|Rejects(UnknownResourceEffectNamedHandler\|DuplicateResourceEffectNamedHandler\|EmptyResourceNamedHandlerBody)\|RejectsHandleAcquireResourceEffectExpression)\|StyioSecurityNightlySemantics\.(RejectsNon(ResourceMethod\|FileIterator)ResourceEffectStatement\|ResourceEffectAcquireThenCloseConsumesReceiver)\|StyioResourceEffects\.(FallbackRunsAfterFileWriteFailureAndContinues\|FallbackIsSkippedAfterSuccessfulFileWrite\|FileIterator(FallbackRecoversOpenFailureAndContinues\|NamedIoHandlerRecoversOpenFailure\|NoFallbackStopsOpenFailure\|SuccessSkipsFallback)\|HandleAcquire(FallbackRecoversFileOpenFailure\|NamedIoHandlerRecoversFileOpenFailure\|NoFallbackStopsFileOpenFailure\|SuccessSkipsFallbackAndKeepsHandleUsable\|SuccessFeedsLaterResourceEffectClose\|SuccessFeedsLaterResourceEffectWrite\|SuccessFeedsLaterResourceEffectInstantPull\|FallbackThenUseFailsClosed\|FallbackThenWriteHandlerRecoversClosedHandle\|FallbackThenWriteNoFallbackStopsClosedHandle\|FallbackThenInstantPullHandlerRecoversClosedHandle\|FallbackThenInstantPullNoFallbackStopsClosedHandle)\|ResourceMethodClose(SuccessSkipsFallback\|FallbackRecoversFileOpenFailure\|NamedIoHandlerRecoversFileOpenFailure\|NoFallbackStopsFileOpenFailure)\|NamedIoHandlerRunsForFileWriteFailure\|NamedCleanupHandlerRunsForFileCloseFailure\|IoHandlerDoesNotCatchFileCloseCleanupFailure\|UnmatchedNamedHandlerFailsFastWithoutCatchAllFallback\|HandlerChainFallsBackAfterUnmatchedEffects\|MissingFallbackStopsAtSettlementSite)\|StyioSafetyRuntime\.FileCloseFailureIsCleanupRuntimeEffect)$' --output-on-failure` passed on 2026-05-26 for the earlier slice; focused acquired-handle write-method coverage `ctest --test-dir build -R '^StyioResourceEffects\.HandleAcquire(SuccessFeedsLaterResourceEffectWrite\|FallbackThenWriteHandlerRecoversClosedHandle\|FallbackThenWriteNoFallbackStopsClosedHandle)$' --output-on-failure` passed on 2026-05-29; focused acquired-handle instant-pull coverage `ctest --test-dir build -R '^(StyioSecurityNightlyParserStmt\.ResourceEffectHandleAcquireFeedsLaterInstantPull\|StyioSecurityNightlySemantics\.RejectsNonFileHandleInstantPullResourceEffectValue\|StyioSafetyRuntime\.ZeroFileHandleReadSetsRuntimeError\|StyioResourceEffects\.HandleAcquire(SuccessFeedsLaterResourceEffectInstantPull\|FallbackThenInstantPullHandlerRecoversClosedHandle\|FallbackThenInstantPullNoFallbackStopsClosedHandle))$' --output-on-failure` passed on 2026-05-29; `python3 scripts/runtime-surface-gate.py` passed on 2026-05-29; focused file-rebind resource-effect coverage `ctest --test-dir build -R '^(StyioResourceEffects\.FlexRebind(Fallback|NamedIoHandler)RecoversFileOpenFailure|StyioSecurityNightlyParserStmt\.(ParsesResourceEffectFileRebindStatement|RejectsScalarFlexBindResourceEffectStatement|RejectsFileRebindResourceEffectExpression))$' --output-on-failure` passed on 2026-05-30; focused direct file-iterator resource-effect coverage `ctest --test-dir build -R '^(StyioResourceEffects\.FileIterator(FallbackRecoversOpenFailureAndContinues\|NamedIoHandlerRecoversOpenFailure\|NoFallbackStopsOpenFailure\|SuccessSkipsFallback)\|StyioSecurityNightlyParserStmt\.ParsesResourceEffectFileIteratorStatement\|StyioSecurityNightlySemantics\.RejectsNonFileIteratorResourceEffectStatement)$' --output-on-failure` passed on 2026-05-31; focused direct file-release resource-effect coverage `ctest --test-dir build -R '^(StyioResourceEffects\.DirectFileRelease(SuccessSkipsFallback\|FallbackRecoversFileOpenFailure\|NamedIoHandlerRecoversFileOpenFailure\|NoFallbackStopsFileOpenFailure)|StyioSecurityNightlyParserStmt\.(ParsesResourceEffectDirectFileReleaseStatement\|ResourceEffectHandleAcquireFeedsLaterDirectRelease)|StyioSecurityNightlySemantics\.(RejectsDirectFileReleaseResourceEffectExpression\|ResourceEffectAcquireThenDirectReleaseConsumesReceiver))$' --output-on-failure` passed on 2026-05-31 |
| File/stdin instant-pull value-producing resource-effect slice | `ResourceEffectAST` / `SIOResourceEffect` now carry an expression-value flag for non-task `?|` uses. The nightly parser accepts `result = ?| (<< @file("data.txt")) | fallback`, acquired file-handle instant pulls such as `result = ?| (<< f) | fallback` after a preceding `?| f <- @file(...) | ...`, canonical stdin `result = ?| (<- @stdin) | fallback`, explicit-target stdin forms such as `result: f64 = ?| (<- @stdin) | fallback`, and named value handlers. Sema requires the operation to produce a value, requires acquired-handle instant-pull names to resolve to file handles, lets explicit target types drive stdin `SIOStdStreamPull::result_type`, and keeps fallback/handler values assignable to the successful operation type; lowering preserves the value-required flag; codegen returns a PHI over the success, fallback, or matched-handler value only for expression contexts. Runtime coverage proves successful file and stdin instant pulls return values, acquired file handles read `i64` through the same value path, missing files recover through fallback or matched `io` handlers after clearing file-open read failures, recovered failed acquires followed by `?| (<< f) | closed => value` recover `STYIO_RUNTIME_INVALID_FILE_HANDLE`, no-fallback acquired-handle pulls stop before the following statement, stdin numeric parse failures recover through fallback or matched `parse` handlers after clearing `STYIO_RUNTIME_NUMERIC_PARSE`, typed stdin `f64` and `list[i64]` parse failures recover through fallback/handler or stop with `STYIO_RUNTIME_LIST_PARSE` when no fallback exists, and typed stdin `string` pulls return cloned stdin lines. Adjacent negative coverage keeps `?| op | ...` statement-only, rejects statement-shaped write operations where a value is required, rejects non-file names as acquired-handle instant-pull sources, rejects unsupported typed stdin targets such as `bool` and `list[bool]` with `STYIO_TYPE_STDIN_UNSUPPORTED_TARGET`, and rejects mismatched fallback values. This is not arbitrary value-producing recovery for all resource families. | `ctest --test-dir build -R '^(StyioResourceEffects\.(ValueFallbackRecovers(FileInstantPull\|StdinInstantPullParseFailure)\|ValueNamedHandlerRecovers(FileInstantPull\|StdinInstantPullParseFailure)\|ValueNoFallbackStops(StdinInstantPullParseFailure\|FileInstantPull)\|ValueTyped(F64FallbackAndHandlerRecoverStdinInstantPullParseFailure\|StringReturnsStdinInstantPull\|List(ReturnsStdinInstantPull\|FallbackRecoversStdinInstantPullParseFailure\|NoFallbackStopsStdinInstantPullParseFailure))\|HandleAcquire(SuccessFeedsLaterResourceEffectInstantPull\|FallbackThenInstantPullHandlerRecoversClosedHandle\|FallbackThenInstantPullNoFallbackStopsClosedHandle))\|StyioSecurityNightlyParserStmt\.(ParsesResourceEffectValue(FallbackExpression\|StdinPullFallbackExpression\|TypedStdinPullFallbackExpressions)\|ResourceEffectHandleAcquireFeedsLaterInstantPull\|RejectsResourceEffectDiscardExpression)\|StyioSecurityNightlySemantics\.(RejectsStatementShapedResourceEffectExpression\|RejectsResourceEffectValueFallbackTypeMismatch\|RejectsUnsupportedTypedStdinResourceEffectValue\|RejectsNonFileHandleInstantPullResourceEffectValue)\|StyioDiagnostics\.UnsupportedTypedStdin(ListTarget\|Target)ReportsTypeCode)$' --output-on-failure` passed on 2026-05-31 for typed-stdin unsupported-target diagnostics, on 2026-05-29 for the acquired-handle instant-pull subset, and on 2026-05-26 for the earlier file/stdin subset; `ctest --test-dir build -R '^(parser_shadow_gate_\|parser_legacy_entry_audit)' --output-on-failure` passed on 2026-05-26 |
| Materialized container-bounds value-producing resource-effect slice | The first non-instant-pull value-producing resource-operation paths are executable for materialized list index/slice reads, dict index/value-slice reads, matrix index reads, matrix row reads, and matrix row-range slice reads. The parser admits `result = ?| xs[i] | fallback`, `slice = ?| xs[0..] | fallback`, `result = ?| d[key] | fallback`, `values = ?| d[start..end] | fallback`, `result = ?| m[row][col] | fallback`, `row = ?| m[row] | fallback`, and `rows = ?| m[start..end] | fallback` as value-required `ResourceEffectAST` forms; Sema limits this slice to single-index/slice `list[T]`, single-key and ordered value-slice `dict[K,V]`, and matrix operations; lowering reuses `SCListGet`, `SCListSlice`, `SCDictGet`, `SCDictValues`, `SCMatrixGet`, `SCMatrixRow`, and `SCMatrixRowsSlice`; codegen suppresses the operation-local default guard under `SIOResourceEffect` so catch-all fallback and matched `bounds` handlers can recover from `STYIO_RUNTIME_LIST_INDEX`, `STYIO_RUNTIME_DICT_KEY`, and `STYIO_RUNTIME_MATRIX_INDEX`, while plain index/slice expressions outside `?|` emit the default runtime guard before following statements. Runtime coverage proves success skips fallback, catch-all fallback and `bounds => handler` recover list/dict/matrix bounds failures, no-fallback expression settlement fails fast, plain list/dict/matrix bounds failures outside `?|` stop before later statements, fallback type mismatches fail closed, and ordered dict value slices reuse `d.values` plus the `SCListSlice` / `STYIO_RUNTIME_LIST_INDEX` bounds path. Source-level fallback recovery for implicit cleanup and non-file reassignment cleanup, non-file cleanup families, pressure observers, and arbitrary value-producing recovery beyond the covered paths remain open. | `ctest --test-dir build -R '^(StyioResourceEffects\.(ValueList(Index\|Slice)(SuccessSkipsFallback\|FallbackRecoversBoundsFailure\|NamedBoundsHandlerRecoversBoundsFailure\|NoFallbackStopsBoundsFailure)\|ValueDict(Index\|Slice)(SuccessSkipsFallback\|FallbackRecoversBoundsFailure\|NamedBoundsHandlerRecoversBoundsFailure\|NoFallbackStopsBoundsFailure)\|ValueMatrix(Index\|Slice)(SuccessSkipsFallback\|FallbackRecoversBoundsFailure\|NamedBoundsHandlerRecoversBoundsFailure\|NoFallbackStopsBoundsFailure)\|ValueMatrixRowFallbackRecoversBoundsFailure)\|StyioDiagnostics\.Runtime(List(Index\|Slice)\|Dict(Key\|Slice)\|Matrix(Index\|Slice))FailureStopsBeforeNextStatement\|StyioSecurityNightlyParserStmt\.ParsesResourceEffectValue(List(Index\|Slice)\|Dict(Index\|Slice)\|Matrix(Index\|Slice))FallbackExpression\|StyioSecurityNightlySemantics\.RejectsResourceEffectValue(List(Index\|Slice)\|Dict(Index\|Slice)\|MatrixIndex)FallbackTypeMismatch)$' --output-on-failure` passed on 2026-05-30 |
| Simple value-producing resource method slice | User-defined resource methods with a single `<| expr` body, an accepted statement-only preface followed by a final `<| expr`, or scalar local `=` / `:=` prefaces or local list/dict/matrix `=` / `:=` prefaces followed by a final scalar/string or local list/dict/matrix container `<| expr`, now record the inferred result type and lower direct calls or `SIOResourceEffect` value expressions through the returned expression rather than through an expression-context `SGReturn`. Direct `log.answer()` and `result = ?| log.answer() | fallback` preserve the value family for scalar/bool/f64/char/string, format strings, scalar/string match results, returned block-form function calls including explicit matrix-return functions, returned value-producing `ResourceEffectAST`, returned file/stdin instant pulls, returned list/dict/matrix bounds reads, and local list/dict/matrix container handles. Scalar local-flex/final methods and local list/dict/matrix flex/final methods preserve same-named caller bindings; returned local list/dict/matrix handles are cloned before method-block dynamic-slot cleanup. Fallback type mismatches, flat-list matrix returns, returned statement-only function calls, returned container match results, returned resource-effect discard, local resource binding method bodies, resource-method argument type mismatches, and global matrix capture stay fail-closed. Remaining open work is source-level fallback recovery for implicit cleanup and non-file reassignment cleanup, non-file cleanup families, pressure observers, local resource binding method bodies, lexical/global captures, and arbitrary value-producing resource-operation recovery beyond the covered value slices. | `ctest --test-dir build -R '^(StyioResourceEffects\.(ValueResourceMethod(Local(FlexBind\|FinalBind)ReturnsFromDirectAndResourceEffectCalls\|Local(ListDictMatrixBindsReturnScalarValues\|ContainerReturnsFromDirectAndFallbackCalls)\|StatementPrefaceReturnsFromDirectAndResourceEffectCalls\|ReturnsFromDirectAndResourceEffectCalls\|ScalarFamiliesReturnFromDirectAndFallbackCalls\|ReturnedBlockFunction(CallsInferResultFamilies\|MatrixResult)\|ReturnedResourceEffectRecoversInsideMethod)\|ResourceMethod(Value\|FmtStringValue)FallbackTypeMismatchReportsTypeCode\|ResourceMethodReturnedStatementOnlyFunctionReportsSemaCode\|ValueResourceMethod(FallbackRecoversReturnedInstantPullFailure\|NamedIoHandlerRecoversReturnedInstantPullFailure\|NoFallbackStopsReturnedInstantPullFailure\|FallbackRecoversReturnedStdinParseFailure\|NamedParseHandlerRecoversReturnedStdinParseFailure\|NoFallbackStopsReturnedStdinParseFailure\|FallbackRecoversReturnedContainerBoundsFailure\|NamedBoundsHandlerRecoversReturnedDictFailure\|NoFallbackStopsReturnedDictFailure\|FallbackRecoversReturnedMatrixBoundsFailure\|NamedBoundsHandlerRecoversReturnedMatrixFailure\|NoFallbackStopsReturnedMatrixFailure))\|StyioDiagnostics\.MatrixFunctionReturnFlatListFailsBeforeRuntime\|StyioSecurityNightlyParserStmt\.(ParsesResourceMethod(Local(FlexBind\|FinalBind)ReturnExpression\|LocalListDictMatrixBindReturnExpressions\|StatementPrefaceReturnExpression\|Returned((Dict(Index\|Slice)\|ListSlice\|Matrix(Index\|Row\|Slice))\|ScalarFmt\|ResourceEffect)FallbackExpression)\|RejectsResourceMethodReturnedResourceEffectDiscardExpression)\|StyioSecurityNightlySemantics\.(ResourceMethodLocalContainerReturnsLowerThroughValueScope\|MatrixFunctionReturnFeedsResourceMethodValue\|RejectsMatrixFunctionReturnFlatListBeforeRuntime\|RejectsResourceMethod(ArgumentTypeMismatch\|LocalResourceBindBeforeResourceValueScopeSupport\|GlobalMatrixCaptureBeforeLexicalCaptureSupport)))$' --output-on-failure` passed on 2026-05-31 |
| Resource method local list/dict/matrix container return slice | Local list/dict/matrix `=` / `:=` prefaces may now return the local container value itself, for example `@file::list_answer = () => { xs := [41,42] <| xs }`, `@file::dict_answer = () => { d := dict{"a": 40, "b": 2} <| d }`, and `@file::matrix_answer = () => { m: matrix := [[40,2],[1,3]] <| m }`. Sema records the list/dict/matrix result type, lowering binds method-local dynamic slots and propagates resource-method call result types, and codegen clones returned local list/dict/matrix handles before method-block cleanup so direct calls, direct printing, indexing, and `?| method() | fallback` guarded success paths keep valid handles. Local matrix prefaces may also return scalar cell values such as `<| m[0][0]`; local resource binding method bodies, lexical/global captures, returned container match results, statement-only function-call returns, and returned discard stay fail-closed. | `ctest --test-dir build -R '^(StyioResourceEffects\.(ValueResourceMethodLocal(ContainerReturnsFromDirectAndFallbackCalls\|ListDictMatrixBindsReturnScalarValues)\|ResourceMethodReturnedStatementOnlyFunctionReportsSemaCode)\|StyioSecurityNightlySemantics\.(ResourceMethodLocalContainerReturnsLowerThroughValueScope\|RejectsResourceMethodLocalResourceBindBeforeResourceValueScopeSupport\|RejectsResourceMethodGlobalMatrixCaptureBeforeLexicalCaptureSupport)\|StyioSecurityNightlyParserStmt\.ParsesResourceMethodLocalListDictMatrixBindReturnExpressions)$' --output-on-failure` passed on 2026-05-31 |
| Task await fallback settlement slice | `?| task -> value: T` and `?| task -> value: T | fallback` preserve the task/future pull path while sharing the resource-effect settlement boundary. Failed task pulls now run fallback only after clearing the materialized task error; failed task pulls without fallback stop at the await settlement site; non-task await sources and reserved bare continuation freeze fallback syntax fail closed through stable diagnostics. This closes the task_await compatibility sub-slice only; arbitrary value-producing resource-operation recovery beyond the instant-pull slices remains open. | `ctest --test-dir build -R '^(StyioResourceEffects\.(TaskAwaitFallbackRunsAfterFailedTaskPull\|FailedTaskAwaitWithoutFallbackStopsAtSettlementSite)\|StyioSecurityNightlyParserStmt\.(ParsesTaskGroupAndAwaitBindings\|RejectsBareAwaitFreezeFallbackBeforeContinuationLowering)\|StyioSecurityNightlySemantics\.(RejectsBareAwaitFreezeBeforeContinuationLowering\|RejectsAwaitPipeNonTaskSource)\|task_resources_(err_e0(3_bare_await_freeze\|4_await_non_task_source\|5_bare_await_fallback)\|t0(1_task_pull_left\|6_task_group_await)))$' --output-on-failure` passed on 2026-05-26 |
| Bounded matrix selector-history slice | Bounded matrix-valued Topology resources now use owned runtime matrix handles in their ring and pending storage. Sema accepts bounded `matrix` latest/history/slice/snapshot selectors, lowering materializes `list[matrix]` snapshots, codegen clones matrix handles on ring write/read and releases ring-owned handles on overwrite/scope cleanup, runtime lists can store matrix handles with clone/get/set/slice/pop semantics, and selector snapshots can be explicitly copied with `snapshot << @name[...]`. Unbounded matrix snapshots and tuple selector history remain fail-closed. | `ctest --test-dir build -R '^(state_resources_t12_topology_selector_snapshot_matrix\|state_resources_err_e06_selector_snapshot_unbounded_matrix_unsupported)$' --output-on-failure` passed on 2026-05-30; focused `StyioResourceTopology.ResourceSelectorMatrixSnapshotsCloneValues` and `StyioSecurityResourceTopology.(LowersBoundedMatrixResourceSelectors\|KeepsUnboundedMatrixResourceSelectorSnapshotFailClosed)` passed on 2026-05-30 |
| Materialized, mixed file/stdin/list, and bounded selector-snapshot stream zip slice | `SIOStreamZip` now carries both input element types, emits runtime-list zip loops for non-file materialized `list[T]` handles, emits mixed `@file` / materialized-list loops in both source orders, accepts bounded Topology selector snapshots once sema/lowering have materialized them as `list[T]` values, and treats `@stdin` as the accepted standard-input line-stream side for finite zip with materialized lists or `@file` streams in both source orders. The slice covers `i64`, `string`, `f64`, `bool`, and `char` list elements plus bounded matrix selector snapshots materialized as `list[matrix]`; finite zip terminates at the shorter list length, file EOF, or stdin EOF, and keeps non-iterable, scalar/latest selector, duplicate `@stdin & @stdin`, raw matrix handle, or broader stream-source inputs fail-closed. [tests/features/stream_processing/t11_zip_bound_lists.styio](../../tests/features/stream_processing/t11_zip_bound_lists.styio) covers parser-shadow-safe bound-list/literal zip, [tests/features/stream_processing/t12_zip_file_bound_list.styio](../../tests/features/stream_processing/t12_zip_file_bound_list.styio) covers list-left/file-right feature execution, [tests/features/stream_processing/t13_zip_resource_selector_snapshots.styio](../../tests/features/stream_processing/t13_zip_resource_selector_snapshots.styio) covers bounded `i64` and `string` selector snapshots as finite zip inputs, [tests/features/stream_processing/t14_zip_matrix_selector_snapshots.styio](../../tests/features/stream_processing/t14_zip_matrix_selector_snapshots.styio) covers bounded matrix selector snapshots as finite zip inputs, `StyioStreamZip.MaterializedListHandlesSupportF64AndBool` covers f64/bool bound-list runtime zip, `StyioSecurityNightlySemantics.CharMaterializedListsFeedZipBarrier` covers char materialized-list zip lowering, `StyioStreamZip.ResourceMatrixSelectorSnapshotsFeedZipBarrier` and `StyioSecurityNightlySemantics.MatrixSelectorSnapshotsFeedZipBarrier` cover matrix selector snapshot runtime and LLVM shape, `StyioStreamZip.MixedFileAndMaterializedListsTerminateAtShorterInput` covers both mixed file/list source orders and shorter-input termination, `StyioStreamZip.StdinAndMaterializedListsTerminateAtShorterInput` covers stdin/list source orders and EOF/list-length termination, `StyioStreamZip.StdinAndFileStreamsTerminateAtShorterInput` covers stdin/file source orders and shorter-input termination, `StyioSecurityNightlySemantics.StdinFeedsStreamZipBarrier` covers lowering/codegen shape, `StyioSecurityNightlySemantics.RejectsDuplicateStdinZipBeforeDriverDecision` keeps duplicate stdin consumption fail-closed and `StyioDiagnostics.DuplicateStdinZipReportsFeatureCode` proves `STYIO_TYPE_STREAM_DUPLICATE_DRIVER_UNSUPPORTED`, `StyioStreamZip.ResourceScalarSelectorFailsClosedAsZipInput` and `StyioStreamZip.ResourceMatrixLatestSelectorFailsClosedAsZipInput` keep latest selectors out of the materialized-list zip path with `STYIO_TYPE_STREAM_ZIP_UNSUPPORTED_SOURCE`, and `StyioDiagnostics.StreamZipUnsupportedSourceReportsFeatureCode` covers the adjacent non-iterable boundary. This is not the distinct IM-D5 snapshot-join path. | `ctest --test-dir build -R '^(StyioStreamZip\.(MaterializedListHandlesSupportF64AndBool\|MixedFileAndMaterializedListsTerminateAtShorterInput\|StdinAndMaterializedListsTerminateAtShorterInput\|StdinAndFileStreamsTerminateAtShorterInput\|ResourceScalarSelectorFailsClosedAsZipInput\|ResourceMatrixSelectorSnapshotsFeedZipBarrier\|ResourceMatrixLatestSelectorFailsClosedAsZipInput)\|StyioSecurityNightlySemantics\.(CharMaterializedListsFeedZipBarrier\|MatrixSelectorSnapshotsFeedZipBarrier\|StdinFeedsStreamZipBarrier\|RejectsDuplicateStdinZipBeforeDriverDecision)\|StyioDiagnostics\.(StreamZipUnsupportedSourceReportsFeatureCode\|DuplicateStdinZipReportsFeatureCode)\|stream_processing_t(05_zip_files\|11_zip_bound_lists\|12_zip_file_bound_list\|13_zip_resource_selector_snapshots\|14_zip_matrix_selector_snapshots))$' --output-on-failure` passed on 2026-05-30 for the earlier stream zip slice and on 2026-05-31 for duplicate-stdin diagnostics |

## 6. Next-Stage Execution Entry

Start the next stage from checkpoint-sized implementation slices. Do not recreate the deleted long-form plan files; each slice should update this ledger, the owning runbook, the design SSOT if semantics change, and the smallest matching tests. Prefer parallel read-only lanes first; dependencies below block only implementation merges that touch the dependent surface.

| Checkpoint | First output | Owner path | Parallel lanes | Required proof |
|------------|--------------|------------|----------------|----------------|
| P0 | Complete the Sema/IR placeholder inventory before new feature closure lands | W1 / Sema / IR + Codegen / Runtime + Test Quality | Split by placeholder cluster: empty visitors, unsupported inline clones, generated/default `SGConstInt(0)`, verifier coverage, diagnostic coverage, and affected fixture discovery. | Inventory diff covering placeholder clusters, empty visitors, unsupported inline-clone surfaces, generated/default `SGConstInt(0)` uses, StyioIR verifier regression, and `python3 scripts/docs-audit.py` |
| P0 | Keep [IM-D2-PARSER-AUTHORITY-INVENTORY.md](./IM-D2-PARSER-AUTHORITY-INVENTORY.md) current when new syntax forms or parser service entrypoints land | W1 / Frontend + Test Quality | Split by accepted syntax family, parser service entrypoint, no-fallback fixture, shadow fixture, and diagnostics fixture. | No-fallback parser evidence, syntax-check contract test, and `python3 scripts/docs-audit.py` |
| P1 | Keep [IM-D1-STYIOIR-CONTRACT-INVENTORY.md](./IM-D1-STYIOIR-CONTRACT-INVENTORY.md) current when new AST or IR nodes land | W1 / Sema / IR | Split inventory, verifier-golden prep, source-reachable fixture discovery, and docs/test catalog updates; merge implementation per AST/IR cluster. | Focused inventory diff, verifier regression, plus `python3 scripts/docs-audit.py` |
| P1 | Continue refining broad IM-D3 fallback families such as remaining `STYIO_TYPE_ERROR`, `STYIO_RUNTIME_ERROR`, or `STYIO_NATIVE_INTEROP_ERROR` cases into narrower feature-owned diagnostics | Owning compiler/runtime team for that feature | Split by diagnostic family and public surface: CLI JSONL, IDE/LSP mapping, runtime/native payloads, negative fixture discovery, and service-doc deltas. | Focused negative tests, updated service docs, and [IM-D3-DIAGNOSTIC-CONTRACT-INVENTORY.md](./IM-D3-DIAGNOSTIC-CONTRACT-INVENTORY.md) |
| P2 | Pick one implementation-debt syntax family from the IM-D1 inventory and either implement accepted sema/lowering or keep it rejected by stable diagnostics | W1 / Sema / IR + Codegen / Runtime | Sub-agents may prepare candidate slices, tests, docs deltas, and risk notes in parallel; only one touched implementation cluster merges at a time. | Targeted unit tests, affected `styio_pipeline` cases, and `security` when diagnostics or ownership change |
| P2 | Carry one stream-processing source combination through parser, sema, lowering, runtime, and tests | W2 / Frontend + Sema / IR + Codegen / Runtime | Split parser, sema/lowering, runtime, feature fixture, duplicate-driver negative, and five-layer evidence prep; merge after touched P0 cluster is known. | stream-processing feature case, five-layer evidence, and parser shadow gates |
| P3 | Advance one canonical resource topology compiler slice without adding legacy syntax back | W6 / Frontend + Sema / IR + Test Quality | Split active-syntax audit, selector/copy fixture prep, migration negative diagnostics, docs drift, and test catalog updates while P0 inventory runs. | Resource-topology unit tests, active-syntax docs update, and migration diagnostics when retired syntax is touched |
| P3 | Run Algorithmic Optimization C: parser Pratt / precedence table after the P0 inventory and canonical topology prerequisites are represented | Frontend + Perf / Stability + Test Quality | Split precedence inventory, fixture generation, perf corpus, shadow equivalence expectations, and diagnostics preservation audit before parser implementation. | Legacy/nightly parser equivalence, precedence fixtures, parser shadow/no-fallback gates, and large/deep expression perf evidence |
| P4 | Route resource/task pressure evidence through `styio-benchmark` while keeping Styio-side probes stable | Perf / Stability + Test Quality | Split benchmark handoff, Styio probe stability, runtime evidence, and docs/report-path confirmation. | `styio-benchmark` report path or documented handoff plus Styio probe gate |

## 7. Next-Stage Workstream Queue

The next stage should not be a single monolithic rewrite. Use checkpoint-sized workstreams that map to team ownership and hard gates. `Depends on` names the serial merge dependency; audit, fixture discovery, docs drift checks, and external confirmation may still run in parallel unless the row says otherwise.

| Queue | Scope | Primary teams | Depends on | Parallelization rule | Minimum gate |
|-------|-------|---------------|------------|----------------------|--------------|
| W1 | Complete the P0 inventory and retire active sema/lowering placeholders one cluster at a time | Sema / IR, Codegen / Runtime, Test Quality | None | Fan out by cluster for inventory and test discovery; merge one implementation cluster at a time. | inventory diff, targeted unit coverage, verifier evidence, plus affected feature cases |
| W2 | Carry one stream-processing slice end-to-end | Frontend, Sema / IR, Codegen / Runtime, Test Quality | Touched W1 cluster for implementation merge only | Parser fixture prep, runtime design audit, duplicate-driver negative coverage, and docs/test sync may run in parallel. | feature tests, five-layer checks, and relevant runtime/security coverage |
| W4 | Harden the delivered `compile_plan` producer contract for `spio` handoff | CLI / Nano, Docs / Ecosystem, `styio-spio` coordination | None | Split by payload family, malformed-input family, handoff doc, and consumer confirmation. | `StyioDiagnostics.*` coverage, handoff doc update, docs audit |
| W5 | Complete nano negative-path coverage and contract hardening | CLI / Nano, Test Quality | W4 not required | Split by local repository schema, cloud manifest, blob integrity, publish guard, and CLI misuse family. | nano-focused unit tests plus docs audit |
| W6 | Re-open resource topology only as a canonical-only dedicated migration track | Frontend, Sema / IR, Docs / Ecosystem, Test Quality | Touched W1 cluster strongly recommended for implementation merge only | Active-syntax audit, selector/copy fixture prep, migration diagnostics, and docs drift can run in parallel with W1. | feature acceptance, design doc update, ADR when ownership/lifecycle semantics change, and retired-form migration diagnostics |
| W7 | Keep feature catalog and migration diagnostics aligned with active syntax | Test Quality with affected module owners | Parallel with W1-W6 | Runs continuously as a companion lane; final catalog/index updates merge with the owning implementation checkpoint. | `ctest -L language_feature`, catalog/doc sync, no orphan fixtures |
| W8 | Run Algorithmic Optimization C: parser Pratt / precedence table | Frontend, Perf / Stability, Test Quality | P0 parser/Sema inventory and W6 syntax prerequisites represented for implementation merge only | Precedence inventory, perf corpus, fixture generation, and equivalence expectations can start immediately. | parser equivalence, precedence fixtures, parser shadow/no-fallback gates, and large/deep expression perf evidence |

## 8. Rules for Scalable Team Execution

1. Keep checkpoints scoped to one language feature slice, one functional closure, or one minimal test-evidence group, consistent with [../../workflows/CHECKPOINT-WORKFLOW.md](../../workflows/CHECKPOINT-WORKFLOW.md).
2. Route every checkpoint through the owning runbook in [../teams/COORDINATION-RUNBOOK.md](../teams/COORDINATION-RUNBOOK.md); do not leave cross-team review implicit.
3. Do not let package-manager UX drift back into `styio`; only compiler-side contracts belong here.
4. Do not expand the public IDE surface until the stdio runtime drain path and semantic publication discipline are closed.
5. Do not remove parser fallback or compatibility routes without the shadow/five-layer gates that already protect the nightly-first baseline.
6. When a gap is closed, update this ledger, the owning SSOT, the relevant runbook or handoff doc, and the smallest matching tests in the same merge unit.

## 9. Immediate Stage Conclusion

1. The repository is not “unfinished everywhere”; it already has a real nightly-first baseline, a real IDE core, and a real nano bootstrap contract.
2. The deepest unfinished work is concentrated in compiler completion debt: feature-specific parser additions under the no-fallback authority contract, sema/type feature debt, stream-processing runtime closure, and resource topology migration debt.
3. Broad industrial-language gaps must not remain implicit. They are either implemented through checkpoint-sized work or parked in §5.7 with a named decision, a mature reference architecture, and a stop condition.
4. Package-manager expectations must stay split cleanly: `styio` now owns the compiler-side compile-plan contract baseline and its compatibility maintenance, but not a full package-manager product surface.
5. IDE next-stage work should prioritize operational closure over feature count: drain semantics correctly first, then expand methods.
