# 2026-04-06 开发记录

**Purpose:** 记录 F.2 错误分类收敛 checkpoint，保证“先失败后修复”书签与恢复命令可直接复现。

**Last updated:** 2026-04-06

## Checkpoint（F.2.1：Stream Zip 不支持来源的误分类收敛）

### 当前状态

- 发现可触发路径：
  - `a = [1, 2, 3]` 后参与 `a >> #(n) & ["x", "y", "z"] >> #(s) => { ... }`；
  - 原行为命中 `StyioNotImplemented("unsupported stream zip lowering")`，顶层归类 `RuntimeError`（退出码 `5`）。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioDiagnostics.StreamZipUnsupportedSourceReportsTypeError`；
  - 初始红灯（exit `5`）后转绿（exit `4`）。
- 修复内容：
  - `SGStreamZip` 默认兜底分支从 `StyioNotImplemented` 改为 `StyioTypeError`；
  - 文案明确当前支持边界：`list literal` 与 `@file stream`。
- 回归结果：
  - `ctest -L styio_pipeline` 全绿（25/25）；
  - `ctest -L security` 全绿（78/78）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest -L soak_smoke` 全绿（6/6）。
- ADR 落地：`docs/adr/ADR-0063-stream-zip-unsupported-source-typeerror.md`。

### 下一步

1. F.2 继续：扫描剩余 `StyioNotImplemented` 可触发路径（优先 codegen），逐条做“红灯用例 + 分类收敛”。
2. F.1 协同：保持 text/jsonl 在错误分类与退出码上的一致性，防止同类错误漂移回 `RuntimeError`。
3. D.5 联动：将该失败样本纳入回归模板，作为长周期恢复时的快速健康检查。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio styio_test -j8
ctest --test-dir build -R '^StyioDiagnostics\.StreamZipUnsupportedSourceReportsTypeError$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
```

## Checkpoint（F.2.2：活跃流水线 `NotImplemented` 清零收口）

### 当前状态

- 代码收口：
  - `Parser.cpp` 中 5 处 `StyioNotImplemented` 全部替换为 `StyioParseError`；
  - `ToStyioIR.cpp` 中 16 处 `StyioNotImplemented` 全部替换为 `StyioTypeError`；
  - 叠加 F.2.1 的 `CodeGenG.cpp` 收口后，活跃链路（Parser/Analyzer/CodeGen）不再抛 `StyioNotImplemented`。
- 行为回归补齐：
  - `StyioParserEngine.HashIteratorMatchForwardChainReturnsParseError` 增强断言：输出含 `Styio.ParseError`，不含 `Styio.NotImplemented`；
  - 新增 `StyioDiagnostics.SeriesIntrinsicWindowNonLiteralReportsTypeError`：
    - `@[3](ma = p[avg, n])` 返回 `TypeError`（exit `4`）；
    - 输出不含 `Styio.NotImplemented`。
- 一致性验证：
  - `rg -n "throw StyioNotImplemented\\("` 在 `Parser.cpp`/`ToStyioIR.cpp`/`CodeGenG.cpp` 无命中；
  - `ctest -L styio_pipeline` 全绿（26/26）；
  - `ctest -L security` 全绿（78/78）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest -L soak_smoke` 全绿（6/6）。
- ADR 落地：`docs/adr/ADR-0064-retire-notimplemented-from-active-pipeline.md`。

### 下一步

1. F.2 完成：后续新增“不支持”分支必须直接归入 `ParseError` 或 `TypeError`，禁止回退到 `StyioNotImplemented`。
2. F.3：继续收敛维护入口标准（新增错误分支需附 JSONL 断言 + checkpoint 恢复指引）。
3. D.5：把“错误分类漂移”检查纳入常规回归模板。

### 复现命令

```bash
cmake -S . -B build
cmake --build build -j8
rg -n "throw StyioNotImplemented\\(" src/StyioParser/Parser.cpp src/StyioAnalyzer/ToStyioIR.cpp src/StyioCodeGen/CodeGenG.cpp
ctest --test-dir build -R '^Styio(ParserEngine\.HashIteratorMatchForwardChainReturnsParseError|Diagnostics\.SeriesIntrinsicWindowNonLiteralReportsTypeError)$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
```

## Checkpoint（F.3：`?=` 无默认分支崩溃收口）

### 当前状态

- 崩溃根因定位：
  - `parse_cases_only` 中 `default_stmt` 未初始化；
  - 当 `?= { ... }` 无 `_ => ...` 默认分支时，`CasesAST` 接收到悬空指针，触发非确定性崩溃（可复现 `exit 138/139`）。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioDiagnostics.MatchWithoutDefaultDoesNotCrash`；
  - 红灯阶段复现 `Bus error`（退出码 `138`）；
  - 修复后转绿，断言退出码 `0` 且输出 `1`。
- 修复内容：
  - `Parser.cpp::parse_cases_only`：`StyioAST* default_stmt = nullptr;`
- fuzz 语料补齐：
  - `tests/fuzz/corpus/parser/seed-match-no-default.styio` 纳入无默认分支 `?=` 崩溃历史样本。
- 一致性验证：
  - `ctest -L styio_pipeline` 全绿（27/27）；
  - `ctest -L security` 全绿（78/78）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest -L soak_smoke` 全绿（6/6）。
- ADR 落地：`docs/adr/ADR-0065-match-cases-default-arm-null-init.md`。

### 下一步

1. F.3 继续：补 `?= {}`（空 case 集）语义边界测试，明确“允许/拒绝”契约，避免未来漂移。
2. B.4/B.5 协同：在 `STYIO_ENABLE_FUZZ=ON` 配置下跑 parser smoke，确认新增 seed 在随机扰动下稳定无崩溃。
3. D.5 协同：把该崩溃样本加入最小化回归模板，作为中断恢复健康检查项。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioDiagnostics\.MatchWithoutDefaultDoesNotCrash$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
```

## Checkpoint（F.3/B.4 联动：Parser 异常标注防崩溃 + Fuzz Smoke 稳定化）

### 当前状态

- 新增语义边界回归：
  - `StyioParserEngine.EmptyMatchCasesAreRejectedWithParseError` 固化 `?= {}` 当前拒绝契约（`ParseError`, exit `3`，legacy/new 一致）。
- fuzz 发现并收口崩溃：
  - `fuzz_parser_smoke` 命中 `parse_stmt_or_expr` 错误路径，`mark_cur_tok` 越界导致崩溃；
  - 修复为 `mark_cur_tok` 边界防护（越界降级返回注释文本，不再二次崩溃）。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioDiagnostics.MalformedStatementPrefixReportsParseErrorWithoutCrash`；
  - 对应样本在修复后稳定返回 `ParseError`，不再 signal 退出。
- fuzz 语料与构建链路补齐：
  - 新增 seed：`tests/fuzz/corpus/parser/seed-malformed-stmt-prefix.styio`；
  - `tests/fuzz/CMakeLists.txt` 为 fuzz 目标补 `LLVM_INCLUDE_DIRS` / `LLVM_DEFINITIONS_LIST`；
  - `fuzz_smoke` 增加 `ASAN_OPTIONS=detect_container_overflow=0`，规避 libFuzzer 运行时 container-overflow 误报噪音。
- gate 结果：
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L security` 全绿（78/78）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest -L soak_smoke` 全绿（6/6）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（2/2）。
- ADR 落地：
  - `docs/adr/ADR-0066-parser-mark-cur-tok-guard-and-fuzz-smoke-stability.md`。

### 下一步

1. B.5：nightly fuzz 任务增加“失败样本自动回流 corpus + 单测模板”动作，避免仅停留在 artifact。
2. F.3：继续扫描 parser 内“错误路径调用高风险定位函数”并补同类边界测试。
3. D.5：把 fuzz 崩溃复现命令纳入最小化回归模板。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^Styio(ParserEngine\.EmptyMatchCasesAreRejectedWithParseError|Diagnostics\.MalformedStatementPrefixReportsParseErrorWithoutCrash)$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure

cmake -S . -B build-fuzz \
  -DSTYIO_ENABLE_FUZZ=ON \
  -DCMAKE_C_COMPILER=/opt/homebrew/opt/llvm@18/bin/clang \
  -DCMAKE_CXX_COMPILER=/opt/homebrew/opt/llvm@18/bin/clang++ \
  -DCMAKE_OSX_SYSROOT=/Applications/Xcode.app/Contents/Developer/Platforms/MacOSX.platform/Developer/SDKs/MacOSX.sdk \
  -DCMAKE_CXX_FLAGS='-stdlib=libc++' \
  -DCMAKE_EXE_LINKER_FLAGS='-stdlib=libc++' \
  -DLLVM_DIR=/opt/homebrew/opt/llvm@18/lib/cmake/llvm \
  -DCMAKE_PREFIX_PATH='/opt/homebrew/opt/llvm@18;/opt/homebrew/opt/icu4c@78'
cmake --build build-fuzz --target styio_fuzz_lexer styio_fuzz_parser -j8
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（C.3/C.4：`CasesAST` / `MatchCasesAST` 所有权收口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.CasesOwnsCasePairsAndDefault`；
  - 新增 `StyioSecurityAstOwnership.MatchCasesOwnsScrutineeAndCases`；
  - 红灯阶段析构计数均为 `0`，确认子节点未被释放。
- 修复内容：
  - `AST.hpp::CasesAST` 引入 `case_owners_` 与 `default_owner_`，内部 `unique_ptr` 接管；
  - `AST.hpp::MatchCasesAST` 引入 `value_owner_` 与 `cases_owner_`；
  - 对外仍保持 `case_list`/`case_default` 与 getter 的原始指针接口，避免调用侧大改。
- 回归结果：
  - 新增两条 `StyioSecurityAstOwnership.*` 用例转绿；
  - `ctest -L security` 全绿（86/86）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）。
- ADR 落地：`docs/adr/ADR-0070-cases-and-matchcases-raii-ownership.md`。

### 下一步

1. C.4 继续：按相同模式覆盖 `CondFlowAST`、`IteratorAST`、`StreamZipAST` 等控制流节点所有权。
2. D.3/D.5：将新所有权节点加入长周期内存增长守门样本（回归 + soak）。
3. F.3：继续保留“新增崩溃样本优先映射到安全回归”流程。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\\.(CasesOwnsCasePairsAndDefault|MatchCasesOwnsScrutineeAndCases)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（F.3：`parse_path`/`peak_operator` 越界异常收口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityParserPath.SingleLetterPathDoesNotThrowOutOfRange`；
  - 新增 `StyioSecurityParserContext.PeakOperatorAtEofReturnsEofWithoutThrow`；
  - 红灯阶段分别复现 `std::out_of_range("basic_string")`。
- 修复内容：
  - `Parser.cpp::parse_path`：
    - Windows 盘符判断改为 `text.size() >= 2` 后再判定 `X:`；
    - 路径扫描增加 EOF 边界，未闭合引号抛 `StyioSyntaxError`，避免死循环。
  - `Parser.hpp::peak_operator`：
    - 重写为边界安全扫描，不再使用无保护 `code.at(...)`；
    - EOF 场景统一返回 `"EOF"`。
- 回归结果：
  - 新增两条 `StyioSecurityParser*` 用例转绿；
  - `ctest -L security` 全绿（84/84）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）。
- ADR 落地：`docs/adr/ADR-0069-parser-path-and-peak-operator-boundary-guards.md`。

### 下一步

1. F.3：继续扫描 parser 内部 “`string/vector::at` + 错误路径” 组合点，优先补可红灯化的小样本。
2. B.5：nightly fuzz 新样本优先映射到 `StyioSecurityParserContext.*`/`StyioDiagnostics.*` 双层回归。
3. F.3/F.2 守门：新增 parser 错误分支时，强制附带 `--error-format=jsonl` 分类断言。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityParser(Path\\.SingleLetterPathDoesNotThrowOutOfRange|Context\\.PeakOperatorAtEofReturnsEofWithoutThrow)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（F.3：ParserContext EOF 降级与越界前移钳制）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityParserContext.EmptyTokenVectorFallsBackToEofToken`；
  - 新增 `StyioSecurityParserContext.MoveForwardBeyondTokenTailIsClampedToEof`；
  - 红灯阶段复现 `std::out_of_range("vector")`。
- 修复内容：
  - `Parser.hpp` 为 `StyioContext` 引入 EOF fallback token；
  - `cur_tok` / `cur_tok_type` 在越界时降级为 `TOK_EOF`；
  - `move_forward` 超尾部时钳制到 `tokens.size()`；
  - `map_match` 在剩余 token 不足时直接返回 `false`；
  - `try_match` / `try_match_panic` 首次匹配改为边界安全判断。
- 回归结果：
  - 新增两条 `StyioSecurityParserContext.*` 用例转绿；
  - `ctest -L security` 全绿（80/80）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）。
- ADR 落地：`docs/adr/ADR-0068-parser-context-eof-fallback-and-forward-clamp.md`。

### 下一步

1. F.3：继续沿 parser 错误路径做“内部异常文案泄露”巡检，新增样本即回归化。
2. D.5：把本 checkpoint 的两条 `StyioSecurityParserContext.*` 纳入恢复演练首批健康检查。
3. B.5：nightly fuzz 失败样本优先映射到 parser context 边界测试模板。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test -j8
ctest --test-dir build -R '^StyioSecurityParserContext\\.(EmptyTokenVectorFallsBackToEofToken|MoveForwardBeyondTokenTailIsClampedToEof)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure

cmake -S . -B build-fuzz \
  -DSTYIO_ENABLE_FUZZ=ON \
  -DCMAKE_C_COMPILER=/opt/homebrew/opt/llvm@18/bin/clang \
  -DCMAKE_CXX_COMPILER=/opt/homebrew/opt/llvm@18/bin/clang++ \
  -DCMAKE_OSX_SYSROOT="$(xcrun --show-sdk-path)" \
  -DCMAKE_CXX_FLAGS='-stdlib=libc++' \
  -DCMAKE_EXE_LINKER_FLAGS='-stdlib=libc++' \
  -DLLVM_DIR=/opt/homebrew/opt/llvm@18/lib/cmake/llvm \
  -DCMAKE_PREFIX_PATH='/opt/homebrew/opt/llvm@18;/opt/homebrew/opt/icu4c@78'
cmake --build build-fuzz --target styio_fuzz_lexer styio_fuzz_parser -j8
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（B.5/D.5：Nightly fuzz 样本回流 case pack 闭环）

### 当前状态

- nightly fuzz 流水线完成闭环改造：
  - `.github/workflows/nightly-fuzz.yml` 在 deep fuzz 后无条件执行 `scripts/fuzz-regression-pack.sh`；
  - 新增 `fuzz-regressions/` 工件上传，保留 `fuzz-artifacts/` 与 `fuzz-regressions/` 双工件；
  - deep fuzz 改为“先记录 `lexer/parser` 退出码，再在末尾统一 fail”，确保失败样本不会因提前退出而丢失。
- 回流打包能力落地：
  - `scripts/fuzz-regression-pack.sh` 生成 `summary.json`、`manifest.tsv`、`corpus-backflow/*.seed`、`CASE.md`、`apply-corpus-backflow.sh`；
  - `tests/fuzz/README.md` 新增 nightly case pack 使用方式。
- 自动化自检补齐：
  - 新增 `tests/fuzz/fuzz_regression_pack_smoke.sh`；
  - 新增 `tests/fuzz/fuzz_smoke_runner.sh`，fuzz smoke 先复制临时 corpus，避免污染仓库语料；
  - 新增 `fuzz_regression_pack_smoke`（标签：`fuzz;fuzz_smoke;fuzz_regression`）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 现覆盖 lexer/parser smoke + case pack smoke 三条链路。
- gate 回归结果：
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L security` 全绿（78/78）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest -L soak_smoke` 全绿（6/6）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）。
- ADR 落地：`docs/adr/ADR-0067-nightly-fuzz-case-pack-backflow.md`。

### 下一步

1. F.3：继续扫描 parser 错误路径中潜在“定位函数二次崩溃”点，按同模式补边界回归。
2. D.5：把 `fuzz-regressions/<run-id>/CASE.md` 中“回流 seed + 固化单测”纳入每周恢复演练清单。
3. F.3/F.2 守门：新增“不支持”路径时强制附 JSONL 分类断言，防止分类漂移。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test styio_soak_test -j8
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure

cmake -S . -B build-fuzz \
  -DSTYIO_ENABLE_FUZZ=ON \
  -DCMAKE_C_COMPILER=/opt/homebrew/opt/llvm@18/bin/clang \
  -DCMAKE_CXX_COMPILER=/opt/homebrew/opt/llvm@18/bin/clang++ \
  -DCMAKE_OSX_SYSROOT="$(xcrun --show-sdk-path)" \
  -DCMAKE_CXX_FLAGS='-stdlib=libc++' \
  -DCMAKE_EXE_LINKER_FLAGS='-stdlib=libc++' \
  -DLLVM_DIR=/opt/homebrew/opt/llvm@18/lib/cmake/llvm \
  -DCMAKE_PREFIX_PATH='/opt/homebrew/opt/llvm@18;/opt/homebrew/opt/icu4c@78'
cmake --build build-fuzz --target styio_fuzz_lexer styio_fuzz_parser -j8
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（C.3/C.4：`FunctionAST` / `SimpleFuncAST` 所有权 RAII 收口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.FunctionOwnsNameParamsRetTypeAndBody`；
  - 新增 `StyioSecurityAstOwnership.SimpleFuncOwnsNameParamsRetTypeAndReturnExpr`；
  - 红灯阶段两条用例析构计数均为 `0`。
- 修复内容：
  - `AST.hpp::FunctionAST` 新增 owner 字段：
    - `func_name_owner_` / `params_owner_` / `ret_type_owner_` / `ret_type_tuple_owner_` / `func_body_owner_`；
  - `AST.hpp::SimpleFuncAST` 新增同构 owner 字段与 `ret_expr_owner_`；
  - 对外接口保持兼容：`func_name` / `params` / `ret_type` / `func_body` / `ret_expr` 继续暴露 raw view。
- 回归结果：
  - 两条新增 ownership 用例转绿；
  - `ctest -L security` 全绿（90/90）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）。
- ADR 落地：`docs/adr/ADR-0071-function-and-simplefunc-raii-ownership.md`。

### 下一步

1. C.4：继续迁移循环/迭代节点（`InfiniteLoopAST`、`IteratorAST`、`StreamZipAST`）到相同 owner 模式。
2. C.5：收口 parser 侧“语句块容器”所有权边界（优先 `BlockAST/MainBlockAST` 的可中断小步改造）。
3. D.3：在下一批节点改造时并行观察 ASan/UBSan 与 soak 的 RSS 走势，避免隐性回归。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\\.(FunctionOwnsNameParamsRetTypeAndBody|SimpleFuncOwnsNameParamsRetTypeAndReturnExpr)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（C.4：`InfiniteLoopAST` / `StreamZipAST` 所有权 RAII 收口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.InfiniteLoopOwnsWhileCondAndBodyNode`；
  - 新增 `StyioSecurityAstOwnership.StreamZipOwnsCollectionsParamsAndBody`；
  - 红灯阶段复现：析构计数均为 `0`，且 loop 场景 `tracked_node_count` 残留 `2`。
- 修复内容：
  - `AST.hpp::InfiniteLoopAST` 新增 `while_cond_owner_` / `body_owner_`；
  - `AST.hpp::StreamZipAST` 新增 `collection_*_owner_`、`params_*_owners_`、`following_owners_`；
  - 通过 `adopt_params` / `adopt_following` 统一把构造入参接管为 owning 容器，同时保留 raw view getter。
- 回归结果：
  - 两条新增 ownership 用例转绿；
  - `ctest -L security` 全绿（92/92）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）。
- ADR 落地：`docs/adr/ADR-0072-loop-and-streamzip-raii-ownership.md`。

### 下一步

1. C.4：继续迁移 `IteratorAST`（含 `following` 追加路径）并同步 parser 侧追加接口，避免 owner/view 脱钩。
2. C.5：开始 `BlockAST/MainBlockAST` 容器接管设计，先落不改语义的空壳接口。
3. D.3：在下一步容器接管后补一轮 ASan/UBSan + soak smoke 交叉验证。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\\.(InfiniteLoopOwnsWhileCondAndBodyNode|StreamZipOwnsCollectionsParamsAndBody)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（C.4：`IteratorAST` 所有权收口 + parser 追加路径对齐）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.IteratorOwnsCollectionParamsAndFollowing`；
  - 红灯阶段复现 collection/param/following 析构计数均为 `0`。
- 修复内容：
  - `AST.hpp::IteratorAST` 新增 `collection_owner_` / `params_owners_` / `following_owners_`；
  - 引入 `append_followings`，确保追加路径同步接管所有权；
  - `Parser.cpp::parse_iterator_with_forward` 从 `output->following.insert(...)` 改为 `output->append_followings(...)`，避免 owner/view 脱钩。
- 回归结果：
  - 新增 ownership 用例转绿；
  - `ctest -L security` 全绿（93/93）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）。
- ADR 落地：`docs/adr/ADR-0073-iterator-raii-ownership-and-following-append.md`。

### 下一步

1. C.5：开始 `BlockAST/MainBlockAST` 容器接管（优先提供 append API，再替换直接容器写入点）。
2. C.6：在 AST 容器改造后补一轮 ASan/UBSan 本地验证，确认无新 UAF/DF。
3. D.3：把 iterator/streamzip 所有权回归纳入 soak 问题定位清单。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\\.IteratorOwnsCollectionParamsAndFollowing$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（C.5：`BlockAST` / `MainBlockAST` 容器所有权收口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.BlockOwnsStmtList`；
  - 新增 `StyioSecurityAstOwnership.MainBlockOwnsResourceAndStmtList`；
  - 红灯阶段两条用例析构计数均为 `0`。
- 修复内容：
  - `AST.hpp::BlockAST` 新增 `stmt_owners_` / `following_owners_`，并提供 `set_followings(...)`；
  - `AST.hpp::MainBlockAST` 新增 `resources_owner_` / `stmt_owners_`；
  - `Parser.cpp` 将 `block->followings = ...` 切换为 `block->set_followings(...)`，保证容器写入与 owner 同步。
- 回归结果：
  - 两条新增 ownership 用例转绿；
  - `ctest -L security` 全绿（95/95）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）。
- ADR 落地：`docs/adr/ADR-0074-block-and-mainblock-raii-ownership.md`。

### 下一步

1. C.5：继续收口 `MainBlockAST` 的资源访问接口（增加显式 getter，减少直接字段依赖）。
2. C.6：补一轮 ASan/UBSan 组合验证，重点关注容器重排路径。
3. D.3：把新增容器所有权用例纳入 soak 失败后优先排查列表。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\\.(BlockOwnsStmtList|MainBlockOwnsResourceAndStmtList)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（C.5：`CheckIsinAST` / `InfiniteAST` / `AnonyFuncAST` 所有权收口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.CheckIsinOwnsIterableExpr`；
  - 新增 `StyioSecurityAstOwnership.InfiniteOwnsStartAndIncrementExprs`；
  - 新增 `StyioSecurityAstOwnership.AnonyFuncOwnsArgsAndThenExpr`；
  - 红灯阶段三条用例析构计数均为 `0`。
- 修复内容：
  - `AST.hpp::CheckIsinAST` 新增 `iterable_owner_`；
  - `AST.hpp::InfiniteAST` 新增 `start_owner_` / `inc_el_owner_`；
  - `AST.hpp::AnonyFuncAST` 新增 `args_owner_` / `then_expr_owner_`；
  - 保持原有 getter 与构造签名不变。
- 回归结果：
  - 三条新增 ownership 用例转绿；
  - `ctest -L security` 全绿（98/98）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）。
- ADR 落地：`docs/adr/ADR-0075-checkisin-infinite-anonyfunc-raii-ownership.md`。

### 下一步

1. C.5/C.6：推进 `ForwardAST` / `BackwardAST` / `CODPAST` 的复杂所有权边界收口。
2. C.6：补一轮 ASan/UBSan 本地验证，确认新增 owner 改造无 UAF/DF 回归。
3. D.3：把新增节点的所有权用例加入内存增长异常排查清单。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\\.(CheckIsinOwnsIterableExpr|InfiniteOwnsStartAndIncrementExprs|AnonyFuncOwnsArgsAndThenExpr)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
```

## Checkpoint（C.6：sanitizer 捕获 `StreamZip` downcast 并修复 parser 分派边界）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 现有 `StyioDiagnostics.StreamZipUnsupportedSourceReportsTypeError` 在 `build-asan-ubsan` 下红灯（UBSan：`StreamZipAST` 被 downcast 为 `IteratorAST`，进程 `Abort trap`）。
- 修复内容：
  - `Parser.hpp/Parser.cpp` 中 `parse_iterator_only` 与 `parse_iterator_with_forward` 返回类型统一改为 `StyioAST*`；
  - 移除 `static_cast<IteratorAST*>`；
  - 仅在 `dynamic_cast<IteratorAST*>` 成功时追加 forward，非 plain iterator 路径直接保持边界语义。
- 回归结果：
  - 失败用例恢复为稳定 `TypeError`；
  - `build-asan-ubsan` 上 `security`（105/105）与 `styio_pipeline`（29/29）全绿。
- ADR 落地：`docs/adr/ADR-0076-codp-forward-backward-and-stream-node-raii.md`。

### 下一步

1. 继续 C.3/C.5：收口剩余 AST 节点的 owner/view 边界。
2. 继续 C.6：在每批 owner 改造后跑一轮 ASan/UBSan 组测。
3. D.3：将 sanitizer 失败样本固化为长期回归基线。

### 复现命令

```bash
cmake --build build-asan-ubsan -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -R '^StyioDiagnostics\.StreamZipUnsupportedSourceReportsTypeError$' --output-on-failure
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -L security --output-on-failure
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -L styio_pipeline --output-on-failure
```

## Checkpoint（C.4：`TypeTupleAST` / `CheckEqualAST` 所有权收口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.TypeTupleOwnsTypeNodes`；
  - 新增 `StyioSecurityAstOwnership.CheckEqualOwnsRightValueExprs`。
- 修复内容：
  - `AST.hpp::TypeTupleAST` 引入 `type_owners_` 与 `adopt_type_list(...)`，`type_list` 保留为 view；
  - `AST.hpp::CheckEqualAST` 引入 `right_value_owners_` 与 `adopt_right_values(...)`，`right_values` 保留为 view。
- 回归结果：
  - 新增 2 条 ownership 用例全绿；
  - `ctest -L security` 全绿（112/112）；
  - `ctest -L styio_pipeline` 全绿（31/31）；
  - `build-asan-ubsan` 上新增 2 条 ownership 用例全绿（2/2）。
- ADR 落地：`docs/adr/ADR-0081-typetuple-checkequal-raii-ownership.md`。

### 下一步

1. C.4/C.5：继续扫描剩余“容器持有裸指针”节点，按 owner+view 模式补齐析构回归。
2. C.6：保持 owner checkpoint 后的 sanitizer 验证，优先覆盖 `security + styio_pipeline`。
3. D.5：把新 ownership 回归加入中断恢复首批健康检查清单。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\.(TypeTupleOwnsTypeNodes|CheckEqualOwnsRightValueExprs)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure

cmake --build build-asan-ubsan --target styio_security_test -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -R 'TypeTupleOwnsTypeNodes|CheckEqualOwnsRightValueExprs' --output-on-failure
```

## Checkpoint（C.5/C.6：单参数 state helper 内联识别修复 + 克隆覆盖扩展）

### 当前状态

- 根因与红灯：
  - `# pulse = (x) => @[sum = 0](out = $sum + x)`（simple func 直接返回 `StateDeclAST`）之前不进入 pulse state 识别，调用参数不会参与 state 更新；
  - 实测会出现语义退化（例如输出固定为 `0/0/0`）。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioDiagnostics.SingleArgStateFunctionInliningUsesCallArgument`；
  - 固化输入输出：state helper 调用链路输出稳定 `1/3/6`。
- 修复内容：
  - `ToStyioIR.cpp` 引入 `simple_func_returns_single_state_decl(...)`，统一给：
    - `stmt_may_contain_pulse_state`（检测阶段）；
    - `resolve_state_decl_impl`（执行阶段）；
  - 支持两种 helper 返回形态：
    - 直接 `StateDeclAST`；
    - 单语句 block 包裹 `StateDeclAST`。
  - 扩展 `clone_state_expr_with_subst` 覆盖：
    - `WaveDispatchAST` / `FmtStrAST` / `ReturnAST` / `PrintAST` / `PassAST` / `BreakAST` / `ContinueAST` / `BlockAST`。
- 回归结果：
  - `ctest -L styio_pipeline` 全绿（30/30）；
  - `ctest -L security` 全绿（110/110）；
  - `ctest -L milestone` 全绿（92/92）；
  - `build-asan-ubsan` 上：
    - `ctest -L styio_pipeline` 全绿（29/29）；
    - `ctest -L security` 全绿（110/110）。
- ADR 落地：`docs/adr/ADR-0079-state-inline-direct-return-and-clone-coverage.md`。

### 下一步

1. C.5：继续按“新增回归样本 -> 扩展克隆覆盖”节奏收口 state inlining 的 `unsupported AST node` 兜底。
2. C.6：保持 state 相关改动后至少跑 `build-asan-ubsan` 的 `styio_pipeline + security` 双标签。
3. D.3：补一条 state helper 内联的 soak 样本，观察长生命周期内存曲线。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R '^StyioDiagnostics\.SingleArgStateFunctionInliningUsesCallArgument$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L milestone --output-on-failure

cmake --build build-asan-ubsan --target styio_test styio_security_test -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -L styio_pipeline --output-on-failure
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -L security --output-on-failure
```

## Checkpoint（D.3：state helper 内联长生命周期 Soak 门禁）

### 当前状态

- 目标：
  - 将刚修复的单参数 state helper（直返 `StateDecl`）内联链路纳入长跑守门，避免仅有一次性单测覆盖。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioSoakSingleThread.StateInlineHelperProgramLoop`；
  - 样例固定为：
    - `# pulse = (x) => @[sum = 0](out = $sum + x)`
    - `[1, 2, 3] >> #(v) => { pulse(v); >_(out) }`
  - 每轮断言输出稳定 `1/3/6`。
- 修复内容：
  - `tests/soak/styio_soak_test.cpp`：
    - 新增 `StateInlineHelperProgramLoop`，支持 `STYIO_SOAK_STATE_INLINE_ITERS` 放大量。
  - `tests/CMakeLists.txt`：
    - 新增 `soak_deep_state_inline_program`（`STYIO_SOAK_STATE_INLINE_ITERS=1500`）。
  - `tests/soak/README.md`：
    - 同步新增用例说明与环境变量。
- 回归结果：
  - `ctest -R 'StyioSoakSingleThread\.StateInlineHelperProgramLoop'` 全绿（1/1）；
  - `ctest -L soak_smoke` 全绿（7/7）；
  - `ctest -R '^soak_deep_state_inline_program$'` 全绿（1/1）；
  - `ctest -L styio_pipeline` 全绿（30/30）。
- ADR 落地：`docs/adr/ADR-0080-state-inline-soak-loop-gate.md`。

### 下一步

1. D.3：继续把 state inlining 的高风险样例（如 `WaveDispatch/Fallback` 组合）沉淀为 soak 子用例。
2. D.4：保持 `soak_deep` 与 nightly 工作流同步，新增用例默认进入夜间门禁。
3. C.5：继续收窄 state clone 的 unsupported 兜底分支，维持“红灯样例先行”节奏。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_soak_test -j8
ctest --test-dir build -R 'StyioSoakSingleThread\.StateInlineHelperProgramLoop' --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -R '^soak_deep_state_inline_program$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
```

## Checkpoint（C.4：`PrintAST` / `StateRefAST` / `HistoryProbeAST` / `SeriesIntrinsicAST` 所有权收口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.PrintOwnsExpressionList`；
  - 新增 `StyioSecurityAstOwnership.StateRefOwnsNameNode`；
  - 新增 `StyioSecurityAstOwnership.HistoryProbeOwnsTargetAndDepth`；
  - 新增 `StyioSecurityAstOwnership.SeriesIntrinsicOwnsBaseAndWindow`。
- 修复内容：
  - `AST.hpp`：
    - `PrintAST` 新增 `expr_owners_`，`exprs` 保留为 view；
    - `StateRefAST` 新增 `name_owner_`；
    - `HistoryProbeAST` 新增 `target_owner_`/`depth_owner_`；
    - `SeriesIntrinsicAST` 新增 `base_owner_`/`window_owner_`。
- 回归结果：
  - 新增 4 条 ownership 用例全绿；
  - `ctest -L security` 全绿（109/109）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `build-asan-ubsan` 上 `security`（109/109）全绿。
- ADR 落地：`docs/adr/ADR-0077-print-and-state-probe-node-raii-ownership.md`。

### 下一步

1. C.4/C.5：推进 `StateDeclAST` 前，先在 `ToStyioIR::subst_param_in_expr` 引入“替换表达式克隆”策略，消除共享子树风险。
2. C.6：保持每个 owner checkpoint 后执行 `ASan/UBSan` 组测。
3. D.3：把状态表达式替换路径纳入长生命周期 soak 观察点（重点看内存增长与异常样本沉淀）。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\.(PrintOwnsExpressionList|StateRefOwnsNameNode|HistoryProbeOwnsTargetAndDepth|SeriesIntrinsicOwnsBaseAndWindow)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
cmake --build build-asan-ubsan -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -L security --output-on-failure
```

## Checkpoint（C.4/C.5：`StateDeclAST` 所有权收口 + inlining 克隆替换边界）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.StateDeclOwnsAccInitExportVarAndUpdateExpr`。
- 修复内容：
  - `AST.hpp`：
    - `StateDeclAST` 新增 `window_header_owner_` / `acc_name_owner_` / `acc_init_owner_` / `export_var_owner_` / `update_expr_owner_`；
    - 保留原 getter 作为 view，外部接口不变。
  - `ToStyioIR.cpp`：
    - state inlining 路径改为克隆替换（`clone_state_expr_with_subst`）；
    - `resolve_state_decl_impl` 构造内联 `StateDeclAST` 时不再复用原 `window/acc/export` 子指针。
- 回归结果：
  - `ctest -L security` 全绿（110/110）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `build-asan-ubsan` 上 `security`（110/110）全绿。
- ADR 落地：`docs/adr/ADR-0078-statedecl-raii-and-inline-substitution-clone.md`。

### 下一步

1. C.5：继续收口 state 表达式克隆策略覆盖面（减少 `unsupported AST node` 分支触发可能）。
2. C.6：保持每次 state 链路改动后执行 ASan/UBSan `security + styio_pipeline` 组测。
3. D.3：补一条“状态函数 inlining + 长生命周期执行”的 soak 样本，观察内存曲线与稳定性。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\.StateDeclOwnsAccInitExportVarAndUpdateExpr$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
cmake --build build-asan-ubsan -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -L security --output-on-failure
```

## Checkpoint（C.3/C.5：`Forward/Backward/CODP` 与 `Snapshot/Instant/IterSeq/Extractor` 所有权收口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityAstOwnership.SnapshotDeclOwnsVarAndResource`；
  - 新增 `StyioSecurityAstOwnership.InstantPullOwnsResource`；
  - 新增 `StyioSecurityAstOwnership.IterSeqOwnsHashTags`；
  - 新增 `StyioSecurityAstOwnership.ExtractorOwnsTupleAndOperation`；
  - 新增 `StyioSecurityAstOwnership.BackwardOwnsObjectParamsOperationsAndReturns`；
  - 新增 `StyioSecurityAstOwnership.CodpOwnsArgsAndNextChain`；
  - 新增 `StyioSecurityAstOwnership.ForwardSetRetExprTakesOwnership`。
- 修复内容：
  - `AST.hpp`：
    - `SnapshotDeclAST`/`InstantPullAST` 接管资源对象；
    - `IterSeqAST` 接管 `hash_tags`；
    - `ExtractorAST` 接管 tuple/op；
    - `BackwardAST` 接管 object/params/operations/ret_exprs；
    - `CODPAST` 接管 `OpArgs` 与 `NextOp` 链，`PrevOp` 保持 non-owning；
    - `ForwardAST` 接管默认 block 与 `setRetExpr` 注入表达式。
  - `Parser.cpp::parse_codp` 切换为 `setNextOp(...)`，与 `CODPAST` 链式 owner 规则对齐。
- 回归结果：
  - 新增 7 条 ownership 用例全绿；
  - `ctest -L security` 全绿（105/105）；
  - `ctest -L styio_pipeline` 全绿（29/29）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ctest --test-dir build-fuzz -L fuzz_smoke` 全绿（3/3）；
  - `build-asan-ubsan` 上 `security`（105/105）与 `styio_pipeline`（29/29）全绿。
- ADR 落地：`docs/adr/ADR-0076-codp-forward-backward-and-stream-node-raii.md`。

### 下一步

1. 继续 C.4：补齐剩余 Stmt/Decl 分支 owner 迁移与回归。
2. 继续 C.6：保持每个 checkpoint 后 sanitizer 组测。
3. D.3：将 ownership 新增节点纳入 soak 增长异常定位优先级。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityAstOwnership\.(SnapshotDeclOwnsVarAndResource|InstantPullOwnsResource|IterSeqOwnsHashTags|ExtractorOwnsTupleAndOperation|BackwardOwnsObjectParamsOperationsAndReturns|CodpOwnsArgsAndNextChain|ForwardSetRetExprTakesOwnership)$' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
ctest --test-dir build-fuzz -L fuzz_smoke --output-on-failure
cmake --build build-asan-ubsan -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -L security --output-on-failure
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -L styio_pipeline --output-on-failure
```
