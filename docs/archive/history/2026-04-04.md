# 2026-04-04 开发记录

**Purpose:** 记录 D 阶段 soak 框架落地与 CTest/CI 接入，作为中断恢复锚点。

**Last updated:** 2026-04-04

## Checkpoint（E.7 细化：Hash `>>` Iterator 分支接管）

### 当前状态

- NewParser `#` 分支新增 `ITERATOR (>>)` 接管：
  - 复用 `parse_iterator_with_forward(context, params[0])`。
- 约束对齐：
  - 与 legacy 一致，仅允许单参数目标（多参数时报语法错误）。
- token gate 扩展：
  - 新增 `ITERATOR`。
- 回归补齐：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnFunctionDefSubsetSamples` 增加
    `# iter_only(x) >> (n) ?= 2 => >_(n)` parser 等价样例。
- gate 结果：
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*` 全绿；
  - `StyioParserEngine.*` 全绿；
  - shadow suite gate：M1 `40/40`，M2 `20/20`。
- ADR 补齐：`docs/adr/ADR-0051-parser-hash-iterator-branch-subset.md`。

### 下一步

1. E.7 后续：评估 `#` 分支里 forward/iter-seq 余下路径的可切割迁移点。
2. D 阶段协同：针对 iterator 运行时稳定性（既有崩溃样例）单独补安全回归，不与 parser 子集迁移混做一刀。
3. F.1：统一 hash 子集错误提示与恢复指引模板。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
```

## Checkpoint（E.7 细化：Hash `?=` Match Cases 子集接管）

### 当前状态

- NewParser `#` 分支新增 `?=` 接管：
  - `# f(...) ?={ ... }`；
  - `# f(...) ?= expr[, expr...]`。
- token gate 扩展：
  - 新增 `MATCH` 与 `TOK_UNDLINE`（`_`）以覆盖 cases 语法。
- 回归补齐：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnFunctionDefSubsetSamples` 增加 `# parity(n: i32) ?={ ... }`；
  - `StyioParserEngine.LegacyAndNewMatchOnHashMatchCases` 新增 CLI 一致性检查。
- gate 结果：
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*` 全绿；
  - `StyioParserEngine.*` 全绿（含新增 hash match-cases 用例）；
  - shadow suite gate：M1 `40/40`，M2 `20/20`。
- ADR 补齐：`docs/adr/ADR-0050-parser-hash-match-cases-subset.md`。

### 下一步

1. E.7 后续：评估 hash `ITERATOR (>>)` 分支的可切割接管方案。
2. E.6：继续跟踪 M1/M2 summary 连续窗口，关注是否出现新 mismatch。
3. F.1：把 hash 子集新增分支的错误文案纳入统一模板。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
```

## Checkpoint（E.6 门槛加固：M2 新增 Hash 形态 Golden）

### 当前状态

- M2 新增用例：
  - `tests/milestones/m2/t11_hash_assign_and_arrow.styio`
  - 覆盖 `= expr` 与无赋值 `=>` 的 hash 形态组合。
- 新增 golden：
  - `tests/milestones/m2/expected/t11_hash_assign_and_arrow.out`。
- `ctest -L m2` 已扩展为 10 个用例并全绿。
- M2 shadow suite gate 统计更新为 `cases=10/runs=20/match=20`。
- ADR 补齐：`docs/adr/ADR-0049-m2-hash-form-coverage-golden.md`。

### 下一步

1. E.6：持续按 `summary.json` 观察 M2 新增样例的稳定窗口。
2. E.7：继续拆解 `# ... ?= ...` / forward 语法迁移候选。
3. F.1：为新增里程碑样例补充最小失败复现模板（便于回归定位）。

### 复现命令

```bash
cmake -S . -B build
ctest --test-dir build -L m2 --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
cat "$tmp_dir/summary.json"
```

## Checkpoint（E.7 细化：Hash 无赋值直接箭头体）

### 当前状态

- NewParser hash 子集新增无赋值直接箭头支持：
  - `# f : T => expr`
  - `# f => expr`
- 实现策略：
  - 运算符分支中允许直接命中 `=>`（不再要求已有参数列表）；
  - `=>` 后 block/non-block 路径保持既有逻辑。
- 新增回归：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnFunctionDefSubsetSamples` 增加 `# const42 : i32 => 42` 与 `# ping => 1`；
  - `StyioParserEngine.LegacyAndNewMatchOnHashArrowWithoutAssignment`（CLI 引擎一致性）。
  - M2 里程碑新增 `t11_hash_assign_and_arrow` golden，用例纳入常驻 m2/full shadow gate。
- gate 结果：
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*` 全绿；
  - `StyioParserEngine.*` 全绿；
  - shadow suite gate：M1 `40/40`，M2 `20/20`。
- ADR 补齐：`docs/adr/ADR-0048-parser-hash-arrow-no-assign-subset.md`。

### 下一步

1. E.7 后续：评估 `# ... ?= { ... }` 与 forward 语法的拆解可行性。
2. E.6：继续观察 M1/M2 连续窗口统计是否出现波动。
3. F.1：收敛 hash 子集错误文案并补统一断言模板。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
```

## Checkpoint（E.7 细化：Hash 无箭头赋值表达式体）

### 当前状态

- NewParser hash 子集新增无箭头赋值体支持：
  - `# f(...) : T = expr`
  - `# f(...) : T := expr`
- 解析策略：
  - 若命中 `=` / `:=` 且后续不是 `=>`，直接按 `expr` 解析函数体并构造 `SimpleFuncAST`；
  - `=>` 路径保持原行为不变。
- 新增回归：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnFunctionDefSubsetSamples` 增加 `# mix(a: i32, b: i32) : i32 = a + b`；
  - `StyioParserEngine.LegacyAndNewMatchOnHashExprBodyWithoutArrow`（CLI 引擎一致性）。
- gate 结果：
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*` 全绿；
  - `StyioParserEngine.*` 全绿；
  - shadow suite gate：M1 `40/40`，M2 `18/18`。
- ADR 补齐：`docs/adr/ADR-0047-parser-hash-assign-expression-subset.md`。

### 下一步

1. E.7 后续：评估 `# ... ?= { ... }` / forward 相关分支的可切割迁移点。
2. E.6：持续积累 M1/M2 summary 连续窗口，跟踪 mismatch 漂移。
3. F.1：统一 hash 子集错误文案并固化断言模板。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
```

## Checkpoint（E.7 细化：Hash 非 block 体支持语句子集）

### 当前状态

- NewParser hash 非 block 分支由“仅表达式”扩展为“语句/表达式”：
  - `# ... => >_(...)` 不再必然回退 legacy。
- 实现方式：
  - `parse_hash_stmt_new_subset` 在 `=>` 后改为调用 `parse_stmt_new_subset`。
  - 外层 rollback/fallback 机制保持不变。
- 安全回归补齐：
  - `MatchesLegacyOnFunctionDefSubsetSamples` 新增 `# alert := () => >_("ALERT")` 样例。
- gate 结果保持稳定：
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*` 全绿；
  - `StyioParserEngine.*` 全绿；
  - shadow suite gate：M1 `40/40`，M2 `18/18`。
- ADR 补齐：`docs/adr/ADR-0046-parser-hash-body-stmt-or-expr-subset.md`。

### 下一步

1. E.7 后续：按语法域继续缩小 `parse_hash_tag` fallback（优先评估 match/cases 分支）。
2. E.6：保持 M1/M2 gate 连续窗口统计，确认切换风险曲线。
3. F.1：统一 hash 子集错误提示文案，减少调试噪音。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
```

## Checkpoint（E.7 细化：Hash 签名顺序与返回类型子集对齐）

### 当前状态

- NewParser `#` 子集已对齐 M2 主语法顺序：
  - 支持 `# name [ : Ret ] :=/=(params) => body`；
  - 兼容 `# name(params) => body`。
- hash 返回类型子集扩展为：
  - `NAME`、`[|n|]`、`(NAME/[|n|], ...)`。
- `=> { ... }` block-body 改为 NewParser 直解（不再默认抛错回退）。
- 安全回归补齐：
  - `SubsetTokenGateIncludesFunctionDefTokens` 新增 `BOUNDED_BUFFER_OPEN/CLOSE`；
  - `MatchesLegacyOnFunctionDefSubsetSamples` 新增 `[|n|]` 与 tuple 返回类型样例。
- 关键 gate 全绿：
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*`
  - `StyioParserEngine.*`
  - `parser-shadow-suite-gate.sh`：M1 `40/40 match`，M2 `18/18 match`。
- ADR 补齐：`docs/adr/ADR-0045-parser-hash-signature-subset-alignment.md`。

### 下一步

1. E.7 后续：继续抽离 `parse_hash_tag` 中 `match/cases` 等非函数分支，减少 hash fallback 面。
2. E.6：将 M1/M2 `summary.json` 纳入稳定窗口统计（连续通过门槛）。
3. F.1：统一 parser 子集失败文案（可机读分类不变，文本提示收敛）。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
```

## Checkpoint（F.3 维护性收敛：Shadow Gate 脚本命名通用化）

### 当前状态

- 新增通用脚本：`scripts/parser-shadow-suite-gate.sh`。
- 旧脚本 `scripts/parser-shadow-m1-gate.sh` 保留为兼容包装（转发到新脚本）。
- CI 与文档引用切换为通用脚本名。
- ADR 补齐：`docs/adr/ADR-0044-shadow-gate-script-generalization.md`。

### 下一步

1. 后续逐步清理旧脚本调用（保留兼容窗口后可评估移除）。
2. 统一产物目录命名策略（M1/M2 与未来套件）。
3. 继续 E.7 语法子域迁移。

### 复现命令

```bash
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
```

## Checkpoint（E.6 门槛对齐：M2 脚本化预算与工件）

### 当前状态

- CI 中 M2 gate 从单条 `ctest` 升级为脚本化预算：
  - `scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 ...`
- M2 与 M1 对齐产物：
  - 都会生成 `summary.json`；
  - 失败时统一上传工件目录。
- 上传打包范围扩展：
  - `build/parser-shadow-artifacts`（M1）
  - `build/parser-shadow-artifacts-m2`（M2）
- ADR 补齐：`docs/adr/ADR-0043-parser-m2-artifactized-shadow-gate.md`。

### 下一步

1. E.6：统一定义 M1/M2 稳定窗口统计脚本。
2. E.7：继续减少 `#` 语法路径中的 legacy 依赖。
3. E.7：评估下一个语法域（M3）可接管切片。

### 复现命令

```bash
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
cat "$tmp_dir/summary.json"
```

## Checkpoint（E.7 细化：Hash SimpleFunc 子集原生解析）

### 当前状态

- NewParser `#` 分支新增“先尝试子集解析，再回退 legacy”机制：
  - 子集支持：`# name [ : Ret ] :=/=(params) => expr`
  - 参数列表复用 `parse_params`
  - 返回类型当前仅支持简单 `NAME`
  - block body（`=> { ... }`）与复杂返回类型仍回退 legacy。
- 回退策略：
  - 解析失败时恢复 cursor，调用 `parse_hash_tag(context)`。
- 相关回归保持全绿：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnFunctionDefSubsetSamples`
  - `StyioParserEngine.ShadowCompareAcceptsM2FullSuite`
- ADR 补齐：`docs/adr/ADR-0042-parser-hash-simple-func-subset.md`。

### 下一步

1. E.7 后续：扩展 hash 返回类型子集（tuple/bounded）。
2. E.7 后续：评估 block body 路径逐步迁移策略。
3. E.6：持续观察 M1/M2 full gate 稳定窗口。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
```

## Checkpoint（E.6 门槛升级：M2 Full Shadow Gate）

### 当前状态

- 新增回归：
  - `StyioParserEngine.ShadowCompareAcceptsM2FullSuite`
  - 自动遍历 `tests/milestones/m2/t*.styio`，双引擎 shadow compare。
- CI 显式 gate 升级：
  - 从 `M2 core suite` 切到 `M2 full suite`。
  - 执行 `ctest --test-dir build -R '^StyioParserEngine.ShadowCompareAcceptsM2FullSuite$'`。
- 现有 M1 full gate + artifact/summary 保持不变。
- ADR 补齐：`docs/adr/ADR-0041-parser-m2-full-shadow-gate.md`。

### 下一步

1. E.7：继续迁移 `parse_hash_tag` 内部路径，减少 legacy 依赖。
2. E.6：把 M2 full gate 纳入稳定窗口统计口径。
3. E.7：评估 M2 之外（M3）的下一批可接管语法域。

### 复现命令

```bash
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioParserEngine.ShadowCompareAcceptsM2FullSuite$' --output-on-failure
```

## Checkpoint（E.6 门槛扩展：M2 Core Shadow Gate）

### 当前状态

- CI 新增显式步骤：
  - `Parser shadow gate (M2 core suite)`
  - 执行 `ctest --test-dir build -R '^StyioParserEngine.ShadowCompareAcceptsM2CoreSuite$'`
- 现有 M1 gate 保持不变：
  - `parser-shadow-suite-gate.sh` + `summary.json` + 失败工件上传。
- 形成 M1 + M2 的 parser shadow 双门槛。
- ADR 补齐：`docs/adr/ADR-0040-parser-m2-shadow-gate.md`。

### 下一步

1. E.7：扩展 M2 shadow 覆盖范围（从 core 到 full suite）。
2. E.6：开始统计稳定窗口（基于 M1 summary + M2 gate 连续通过）。
3. E.7：继续拆分 `parse_hash_tag` 子路径迁移。

### 复现命令

```bash
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioParserEngine.ShadowCompareAcceptsM2CoreSuite$' --output-on-failure
```

## Checkpoint（E.7 函数定义入口接管：M2 子集）

### 当前状态

- NewParser 语句子集新增 `TOK_HASH` 入口，函数定义语句不再直接回退。
- 语义体策略：
  - 入口接管到 NewParser；
  - 具体函数语义继续复用 legacy `parse_hash_tag(context)`。
- token 门补齐函数定义必需项：
  - `TOK_HASH`、`ARROW_DOUBLE_RIGHT`、`TOK_LCURBRAC`、`TOK_RCURBRAC`、`EXTRACTOR`。
- 新增回归：
  - `StyioSecurityNewParserStmt.SubsetTokenGateIncludesFunctionDefTokens`
  - `StyioSecurityNewParserStmt.MatchesLegacyOnFunctionDefSubsetSamples`
  - `StyioParserEngine.LegacyAndNewMatchOnM2SimpleFuncSample`
  - `StyioParserEngine.ShadowCompareAcceptsM2CoreSuite`
- 验证结果：
  - `ctest -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.'` 全绿
  - `ctest -R '^StyioParserEngine\\.'` 全绿
  - `parser-shadow-suite-gate.sh` 40/40 match
- ADR 补齐：`docs/adr/ADR-0039-parser-function-def-entry-subset.md`。

### 下一步

1. E.7 后续：拆分 `parse_hash_tag` 内部路径（参数/返回类型/块体）逐步迁移到 NewParser。
2. E.7 后续：扩展 M2 shadow compare 覆盖到 full suite。
3. E.6：基于 `summary.json` 做稳定窗口统计。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
cat "$tmp_dir/summary.json"
```

## Checkpoint（E.7 边界冻结：Dot-Chain 一致拒绝）

### 当前状态

- 新增引擎一致性回归：
  - `StyioParserEngine.DotChainStillRejectedConsistentlyAcrossEngines`
- 测试行为：
  - 输入 `foo.bar(1).baz(2)`；
  - `legacy/new` 均应返回 `ParseError`（退出码 3，JSONL category=`ParseError`）。
- ADR 补齐：`docs/adr/ADR-0038-parser-dot-chain-rejection-boundary.md`。

### 下一步

1. E.7 后续：chain-call 若要支持，需要先在 legacy/new 统一语义并更新冻结测试。
2. E.7 后续：继续迁移函数/复杂语句域。
3. E.6：维持 shadow gate 与 summary 统计连续稳定。

### 复现命令

```bash
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioParserEngine.DotChainStillRejectedConsistentlyAcrossEngines$' --output-on-failure
```

## Checkpoint（E.6 稳定窗口准备：Shadow Summary 工件）

### 当前状态

- `scripts/parser-shadow-suite-gate.sh` 新增统一汇总输出：
  - 在 artifact 根目录写 `summary.json`
  - 固定字段：`cases/runs/artifacts/match/mismatch/shadow_error/failed_runs/passed`
- gate 判定逻辑保持不变（仍要求 mismatch/shadow_error 为 0 且工件数量完整）。
- ADR 补齐：`docs/adr/ADR-0037-parser-shadow-summary-artifact.md`。

### 下一步

1. E.6 后续：用 `summary.json` 做连续窗口统计（例如最近 N 次 gate）。
2. E.7：继续语法域迁移（function/复杂语句）。
3. F：把 summary 消费规则补进维护模板。

### 复现命令

```bash
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
cat "$tmp_dir/summary.json"
```

## Checkpoint（E.7 下一段：Dot-Call 子集接管）

### 当前状态

- NewParser 表达式子集新增 dot-call：
  - `name.method(args...)`
- token 子集门加入 `TOK_DOT`，dot-call 不再默认回退 legacy。
- 调用识别边界与 legacy 对齐：
  - 仅在同一行内拼接调用/点调用（`skip_spaces_no_linebreak`）。
- 保守边界保持：
  - `foo.bar(1).baz(2)` 这类 chain-call 仍按 legacy/new 一致失败处理。
- 新增回归：
  - `StyioSecurityNewParserExpr.SubsetTokenGateIncludesDotCallTokens`
  - `StyioSecurityNewParserStmt.MatchesLegacyOnDotCallSubsetSamples`
  - `StyioSecurityNewParserShadow.FallsBackOnDotChainSequence`
- 关键 gate 全绿：
  - `ctest -R '^StyioParserEngine\\.'`
  - `parser-shadow-suite-gate.sh`（40/40 match）
- ADR 补齐：`docs/adr/ADR-0036-parser-dot-call-subset-with-chain-fallback.md`。

### 下一步

1. E.7 后续：评估 chain-call 语义是否需要在 legacy 先补齐再迁移。
2. E.7 后续：拆分函数定义/复杂语句域迁移切片。
3. E.6：继续累积 shadow gate 稳定窗口数据。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
```

## Checkpoint（E.7 准备：简单调用表达式子集接管）

### 当前状态

- NewParser 表达式子集新增简单调用：
  - `name(args...)` 可用于表达式语句、print 参数、赋值 RHS。
- 移除 `name(` 的预扫描硬回退，改为“先尝试 new，失败再保守回退 legacy”。
- 保留保守边界：
  - `dot-call`（如 `foo.bar(1)`）仍由 legacy 路径处理。
- 新增安全回归：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnSimpleCallSubsetSamples`
  - `StyioSecurityNewParserShadow.FallsBackOnDotCallSequence`
- 关键 gate 验证：
  - `ctest -R '^StyioParserEngine\\.'` 全绿
  - `parser-shadow-suite-gate.sh` 为 40/40 match。
- ADR 补齐：`docs/adr/ADR-0035-parser-simple-call-subset-and-dot-fallback.md`。

### 下一步

1. E.7 下一段：迁移 dot-call / chain-call 子集（保持保守回退）。
2. E.7 下一段：评估函数定义语句域接管切片。
3. E.6：继续累积稳定窗口数据，准备默认切换评估。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
```

## Checkpoint（F.3 维护模板收敛：Shadow Gate 工作流）

### 当前状态

- 更新 `docs/assets/templates/REFACTOR-WORKFLOW-TEMPLATE.md`：
  - 新增“6. 双轨重构附加模板（Shadow Gate）”。
  - 固化双轨重构默认清单：引擎开关、shadow compare、artifact dir、CI 显式 gate、失败工件上传。
- ADR 补齐：`docs/adr/ADR-0034-refactor-template-shadow-gate-pattern.md`。

### 下一步

1. 复用该模板推进 E.7 语法域迁移（调用链/复杂语句域）。
2. 将 nightly 失败样本最小化入口也纳入模板附录。
3. 在下一轮重构中验证模板可执行性并做二次收敛。

### 复现命令

```bash
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
```

## Checkpoint（E.4/E.7 准备：比较/逻辑表达式子集接管）

### 当前状态

- NewParser 表达式子集放开比较/逻辑 token 白名单：
  - `BINOP_GT/GE/LT/LE/EQ/NE`
  - `TOK_RANGBRAC/TOK_LANGBRAC`
  - `LOGIC_AND/LOGIC_OR`
- 修复 AST 形状对齐：
  - 比较运算构造成 `BinCompAST`（不再是 `BinOpAST(Undefined)`）
  - 逻辑运算构造成 `CondAST`
  - 算术运算保持 `BinOpAST`
- 新增安全回归：
  - `StyioSecurityNewParserExpr.SubsetTokenGateIncludesCompareAndLogic`
  - `StyioSecurityNewParserStmt.MatchesLegacyOnCompareAndLogicSubsetSamples`
- 关键 gate 保持全绿：
  - `StyioParserEngine.ShadowCompareAcceptsM1FullSuite`
  - `scripts/parser-shadow-suite-gate.sh`（40/40 match）
- ADR 补齐：`docs/adr/ADR-0033-parser-compare-logic-subset-alignment.md`。

### 下一步

1. E.7：继续迁移 legacy 独占语法（优先调用链与复杂语句域）。
2. E.6：补“稳定窗口”定义（连续 N 次 gate 全绿再评估切默认）。
3. F 准备：把 parser shadow gate 经验沉淀为通用维护模板。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
```

## Checkpoint（E.6 差异预算量化：M1 Shadow Gate 脚本）

### 当前状态

- 新增 `scripts/parser-shadow-suite-gate.sh`：
  - 自动遍历 `tests/milestones/m1/t*.styio`；
  - 执行双引擎 shadow compare（2N 次）；
  - 汇总 `match/mismatch/shadow_error` 与工件数量。
- gate 规则固定为：
  - `mismatch == 0`
  - `shadow_error == 0`
  - JSONL 工件数不少于运行次数。
- CI `Parser shadow gate` 步骤改为运行该脚本并在失败时上传 `build/parser-shadow-artifacts`。
- ADR 补齐：`docs/adr/ADR-0032-parser-shadow-budget-script.md`。

### 下一步

1. E.6 下一段：定义“稳定窗口”长度（例如连续 N 次 PR 全绿）再切默认。
2. E.7：继续拆分 legacy 清退的语法域迁移顺序。
3. F 阶段准备：把 parser gate/工件流程抽象为通用回归模板。

### 复现命令

```bash
cmake --build build --target styio -j8
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
find "$tmp_dir" -name '*.jsonl' | wc -l
```

## Checkpoint（E.6 默认切换门槛首段：CI 显式 Gate）

### 当前状态

- CI 主流程新增显式步骤：
  - `Parser shadow gate (M1 full suite)`
  - 执行 `ctest --test-dir build -R '^StyioParserEngine.ShadowCompareAcceptsM1FullSuite$'`
- `new` parser 默认切换仍保持关闭（默认 `legacy`）。
- 门槛语义进入 ADR：
  - 在 gate 稳定通过与差异工件可回放之前，不切默认。
- ADR 补齐：`docs/adr/ADR-0031-parser-default-switch-gate.md`。

### 下一步

1. E.6 下一段：定义“差异预算”量化口径（允许数量、严重级别、持续窗口）。
2. E.6 下一段：把门槛写入文档化切换清单（切默认前必过项）。
3. E.7 准备：继续拆分 legacy 独占语法域迁移批次。

### 复现命令

```bash
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioParserEngine.ShadowCompareAcceptsM1FullSuite$' --output-on-failure
```

## Checkpoint（E.5 Shadow 差异样本落盘）

### 当前状态

- CLI 新增 `--parser-shadow-artifact-dir`。
- 约束收紧：
  - 仅允许与 `--parser-shadow-compare` 联用；
  - 单独使用会返回 `CliError`（退出码 6）。
- shadow compare 现在会落盘 JSONL 记录（包含 `status/file/engine/hash/size/detail`）。
- 当状态为 `mismatch` 或 `shadow_error` 时，额外落盘可回放工件：
  - 原始输入 `.styio`
  - 主引擎 AST 文本
  - 影子 AST 文本（可用时）
  - 影子错误文本（可用时）
- 新增回归：
  - `StyioParserEngine.ShadowCompareWritesArtifactRecordWhenDirConfigured`
  - `StyioParserEngine.ShadowArtifactDirRequiresShadowCompareFlag`
- ADR 补齐：`docs/adr/ADR-0030-parser-shadow-artifact-recording.md`。

### 下一步

1. E.6：固化默认切换门槛（覆盖阈值、差异预算、回退窗口）。
2. E.6：为门槛增加自动化 gate（例如 CI 中强制 shadow M1 通过）。
3. E.7 准备：继续盘点 legacy 仍独占的语法域并拆分迁移顺序。

### 复现命令

```bash
cmake --build build --target styio styio_test -j8
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./build/bin/styio --parser-engine=new --parser-shadow-compare --parser-shadow-artifact-dir "$tmp_dir" --file tests/milestones/m1/t01_int_arith.styio
ls -la "$tmp_dir"
```

## Checkpoint（E.5 Shadow Compare 扩展：M1 全量门槛 + Bool 对齐）

### 当前状态

- `NewParserExpr` 在 `NAME` primary 路径新增 bool literal 对齐：
  - `true` / `false` 直接映射为 `BoolAST`，不再当作普通 `NameAST`。
- `StyioParserEngine` 新增全量回归：
  - `ShadowCompareAcceptsM1FullSuite`（遍历 `tests/milestones/m1/t*.styio`）。
- `--parser-shadow-compare` 在 M1 全量样例上通过：
  - legacy 主引擎 + new 影子
  - new 主引擎 + legacy 影子
- ADR 补齐：`docs/adr/ADR-0029-parser-shadow-full-m1-and-bool-literal-alignment.md`。

### 下一步

1. E.5 下一段：落地 shadow 差异样本落盘模板（失败时自动产出可回放工件）。
2. E.6：固化默认切换门槛（覆盖阈值 + 差异预算 + 回退窗口）。
3. E.7 准备：梳理 legacy 清退前仍未接管的语法域（调用/复杂语句）。

### 复现命令

```bash
cmake --build build --target styio styio_test -j8
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
ctest --test-dir build -L security --output-on-failure
./build/bin/styio --parser-engine=legacy --parser-shadow-compare --file tests/milestones/m1/t18_bool.styio
./build/bin/styio --parser-engine=new --parser-shadow-compare --file tests/milestones/m1/t18_bool.styio
```

## Checkpoint（E.4 NewParser 语句子集第四段：Compound Assign）

### 当前状态

- `NewParser` 语句子集新增 5 类复合赋值：
  - `+=` / `-=` / `*=` / `/=` / `%=`。
- 语句白名单新增：
  - `COMPOUND_ADD` / `COMPOUND_SUB` / `COMPOUND_MUL` / `COMPOUND_DIV` / `COMPOUND_MOD`。
- 语义映射与 legacy 对齐，统一生成 `BinOpAST(Self_*_Assign, NameAST(id), expr)`。
- 新增回归：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnCompoundAssignSubsetSamples`
  - `StyioParserEngine.LegacyAndNewMatchOnM1CompoundAssignSample`
- ADR 补齐：`docs/adr/ADR-0028-parser-compound-assign-subset.md`。

### 下一步

1. E.5 下一段：把 shadow compare 扩到更多里程碑样例并沉淀差异模板。
2. E.6：定义 parser 默认切换门槛（覆盖率、差异预算、回退窗口）。
3. E.7 准备：梳理 legacy 清退剩余语法缺口（调用语法/复杂类型）。

### 复现命令

```bash
cmake --build build --target styio styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（E.5 Shadow Compare 模式首版）

### 当前状态

- CLI 新增 `--parser-shadow-compare`（默认关闭）。
- 开启后执行“双解析比对”：
  - 主引擎按 `--parser-engine` 正常解析；
  - 影子引擎使用另一引擎再解析一次；
  - AST `toString` 不一致时返回 `ParseError`。
- 影子解析抛异常也会返回 `ParseError`，并沿用已有诊断输出通道（text/jsonl）。
- 新增流水线测试：
  - `StyioParserEngine.ShadowCompareAcceptsM1TypedBindSample`
  - `StyioParserEngine.ShadowCompareAcceptsM1CoreSuite`
- ADR 补齐：`docs/adr/ADR-0027-parser-shadow-compare-mode.md`。

### 下一步

1. E.5 下一段：把 shadow mismatch 样本沉淀为最小回归模板（按语法域分类）。
2. E.6 准备：为默认切换定义“差异预算”阈值与稳定窗口。
3. E.7 前置：评估 legacy 清退所需的最后语法缺口（调用语法/复杂类型等）。

### 复现命令

```bash
cmake --build build --target styio styio_test -j8
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
./build/bin/styio --parser-engine=legacy --parser-shadow-compare --file tests/milestones/m1/t07_typed_bind.styio
./build/bin/styio --parser-engine=new --parser-shadow-compare --file tests/milestones/m1/t07_typed_bind.styio
```

## Checkpoint（E.4 NewParser 语句子集第三段：FinalBind + 安全回退）

### 当前状态

- `NewParser` 语句子集新增 simple typed bind：
  - `NAME : NAME := expr`（先覆盖简单类型名形态）。
- 语句白名单新增 `TOK_COLON`/`WALRUS`，允许 final bind 子集进入 new 路由。
- `parse_main_block_new_subset(...)` 改为局部 `unique_ptr` 持有语句，避免异常路径泄漏。
- `parse_main_block_new_shadow(...)` 新增异常回退：new 子集抛异常时恢复游标并回退 legacy。
- 新增回归：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnFinalBindSubsetSamples`
  - `StyioParserEngine.LegacyAndNewMatchOnM1TypedBindSample`
- ADR 补齐：`docs/adr/ADR-0026-parser-final-bind-subset-and-safe-fallback.md`。

### 下一步

1. E.4 下一段：评估是否增量接管 compound assign（`+=` 等）或继续保持 legacy 回退。
2. E.5：加入影子比对模式（双解析 + 差异输出），形成稳定差异清单。
3. E.6：按覆盖率与差异阈值定义默认切换门槛。

### 复现命令

```bash
cmake --build build --target styio styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（E.4 NewParser 语句子集第二段：FlexBind）

### 当前状态

- `NewParser` 语句子集新增 `NAME = expr` 路径，生成 `FlexBindAST`。
- 语句白名单放开 `TOK_EQUAL`，允许 `flex bind` 语句走 new 路径。
- Shadow 路由新增保守序列回退门：
  - 发现 `name(` 调用序列时直接回退 legacy（new 尚未接管调用语法）。
- 新增安全回归：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnFlexBindSubsetSamples`
  - `StyioSecurityNewParserShadow.FallsBackOnCallExpressionSequence`
- ADR 补齐：`docs/adr/ADR-0025-parser-stmt-subset-flexbind-and-call-fallback.md`。

### 下一步

1. E.4 下一段：增量迁移 typed/final bind（`x: T := expr`）并保持非子集回退策略。
2. E.5：加入影子比对模式（同输入双解析 + AST/诊断差异输出）。
3. E.6：定义默认切换门槛（覆盖率、差异预算、回退窗口）。

### 复现命令

```bash
cmake --build build --target styio styio_security_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（E.4 NewParser 语句子集第一段）

### 当前状态

- 在 `NewParser` 路径新增最小语句子集能力：
  - `parse_main_block_new_subset(...)`
  - `styio_new_parser_is_stmt_subset_start(...)`
  - `styio_new_parser_is_stmt_subset_token(...)`
- 当前语句子集只接管：
  - `print` 语句（`>_(...)`）
  - 表达式语句（复用 E.3 子集表达式）
- `parse_main_block_new_shadow(...)` 路由门从表达式白名单升级到语句白名单：
  - token 不在子集 => 直接回退 legacy；
  - 子集内 => 走 `new` 语句子集主路径。
- 新增安全回归测试：
  - `StyioSecurityNewParserStmt.MatchesLegacyOnPrintSubsetSamples`
- ADR 补齐：`docs/adr/ADR-0024-parser-stmt-subset-print-shadow.md`。

### 下一步

1. E.4 下一段：增量迁移 bind 语句域（`x = ...` / `x := ...`）并保持白名单回退策略。
2. E.5：加入影子比对模式（同输入双解析 + AST/诊断对比）并沉淀差异样本。
3. E.6 准备：定义默认切换门槛（覆盖率、差异阈值、回退窗口）。

### 复现命令

```bash
cmake --build build --target styio styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（E.3 NewParser 表达式子集首段）

### 当前状态

- 新增 `src/StyioParser/NewParserExpr.hpp/.cpp`，落地 `parse_expr_new_subset(...)`。
- `parse_main_block_new_shadow(...)` 接入子集路由门：
  - 起始 token 必须命中子集；
  - 全 token 必须落入子集；
  - 尾部语法不匹配时回滚游标并回退 legacy。
- 为避免语义漂移，E.3 首段子集聚焦算术表达式（关系/逻辑表达式继续走 legacy）。
- 子集解析对齐 legacy 历史行为：
  - 二元链路保持右结合；
  - 一元 `-` 捕获后续表达式。
- 新增安全回归测试：
  - `StyioSecurityNewParserExpr.MatchesLegacyOnSubsetSamples`
  - `StyioSecurityNewParserExpr.RejectsNonSubsetStatementToken`
- ADR 补齐：`docs/adr/ADR-0023-parser-expr-subset-shadow-guard.md`。

### 下一步

1. E.4：在 `new` 路径增量引入语句域（按语法域拆分小 PR，默认仍不切主行为）。
2. E.5：加入影子比对模式（测试中双解析并比较 AST/诊断）。
3. E.6：在覆盖阈值达标后评估默认引擎切换条件与回退窗口。

### 复现命令

```bash
cmake --build build --target styio styio_security_test -j8
ctest --test-dir build -R '^StyioSecurityNewParserExpr\\.' --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（D.1-D.5 单线程 Soak 体系闭环）

### 当前状态

- 新增 `tests/soak/styio_soak_test.cpp`，包含 3 类单线程 soak 用例：
  - 词法长输入摄入循环
  - 文件句柄生命周期循环（open/read/rewind/close）
  - M6 流式程序重复执行循环（`t02_running_max`）
- 新增文件句柄内存增长守卫：`StyioSoakSingleThread.FileHandleMemoryGrowthBound`（RSS 阈值断言）。
- 新增 `tests/soak/README.md`，定义 smoke 与放大量运行方式（`STYIO_SOAK_*`）。
- `tests/CMakeLists.txt` 新增目标 `styio_soak_test` 与标签 `soak;soak_smoke;single_thread`。
- `tests/CMakeLists.txt` 新增 `soak_deep_*` 用例与深跑参数注入（D.2 分档 + D.3 门槛）。
- 修复 `gtest_discover_tests` 多标签传递方式，`ctest -L soak_smoke`/`-L single_thread` 可稳定命中。
- CI 主流程（`.github/workflows/ci.yml`）新增 `Soak smoke tests` 步骤，执行 `ctest -L soak_smoke`。
- 新增 nightly 深跑工作流 `.github/workflows/nightly-soak.yml`，执行 `ctest -L soak_deep` 并归档日志。
- 新增 D.5 工具链：
  - `scripts/soak-minimize.sh`（单变量二分最小化）
  - `tests/soak/REGRESSION-TEMPLATE.md`（回归记录模板）
  - `tests/soak/regressions/README.md`（样本目录约定）
- 修复 soak 文件句柄循环中的 C ABI 误用：不再释放 `styio_file_read_line` 的借用缓冲指针。
- 文档同步：
  - `docs/adr/ADR-0018-soak-single-thread-framework.md`
  - `docs/adr/ADR-0019-soak-tiering-and-rss-guard.md`
  - `docs/adr/ADR-0020-soak-regression-artifact-workflow.md`
  - `docs/assets/workflow/TEST-CATALOG.md` §11

### 下一步

1. C.8 完成后：把真实句柄表实现接入并提升 RSS 守卫阈值策略（含 ASan/UBSan 长跑门禁）。
2. E 阶段：Parser Strangler 双轨时增加 `legacy/new` 影子对比 soak 入口。
3. F 阶段：统一诊断码文案后，把 soak 失败分类映射到稳定错误码统计。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio styio_test styio_security_test styio_soak_test -j8
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -L soak_deep --output-on-failure
./scripts/soak-minimize.sh --help
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（E.2 共享前瞻工具抽取）

### 当前状态

- 新增共享模块：
  - `src/StyioParser/ParserLookahead.hpp`
  - `src/StyioParser/ParserLookahead.cpp`
- 提供统一工具函数：
  - `styio_is_trivia_token(...)`
  - `styio_skip_trivia_tokens(...)`
  - `styio_try_check_non_trivia(...)`
- legacy 路径接入共享工具：
  - `StyioContext::skip()`
  - `StyioContext::try_check()`
- 新增安全回归测试：
  - `StyioSecurityParserLookahead.SkipTriviaFindsNextToken`
- ADR 补齐：`docs/adr/ADR-0022-parser-lookahead-shared-utils.md`。

### 下一步

1. E.3：在 `new` 路径实现表达式子集（先不碰语句域）。
2. E.4：按语法域分批迁移语句子集并保持默认 `legacy`。
3. E.5：增加 shadow 比对模式（双解析 + AST/诊断对比）。

### 复现命令

```bash
cmake --build build --target styio styio_test styio_security_test -j8
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（E.1 ParserEngine 双轨空壳）

### 当前状态

- parser 层新增引擎抽象与统一路由入口：
  - `StyioParserEngine`
  - `styio_parse_parser_engine(...)`
  - `parse_main_block_with_engine(...)`
- `--parser-engine=new` 已可走影子路径，当前仍复用 legacy 解析逻辑（默认行为不变）。
- 主程序去除硬编码分支，统一经 parser 层路由。
- 新增回归测试：
  - `StyioParserEngine.LegacyAndNewMatchOnM1Sample`
  - `StyioParserEngine.UnsupportedEngineIsRejected`
- ADR 补齐：`docs/adr/ADR-0021-parser-engine-shadow-routing.md`。

### 下一步

1. E.2：抽取 legacy/new 共享的词法前瞻与游标工具模块（避免后续重复实现）。
2. E.3：在 `new` 路径先落表达式子集，不触碰语句域。
3. E.5：引入影子比对模式（同输入双解析 + 结构/诊断比对）。

### 复现命令

```bash
cmake --build build --target styio styio_test -j8
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```
