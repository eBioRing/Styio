# 2026-04-05 开发记录

**Purpose:** 记录 E.7 风险收口与根因修复 checkpoint，并补充运行期匹配崩溃防线，确保关键路径可中断恢复并可回归验证。

**Last updated:** 2026-04-05

## Checkpoint（C.7.1：`styio_strcat_ab` 释放路径第一阶段收口）

### 当前状态

- runtime 所有权护栏落地：
  - `styio_strcat_ab` 分配结果登记到线程本地 owned 集；
  - `styio_free_cstr` 改为“仅释放已登记指针”，对 `nullptr`/借用指针安全 no-op。
- codegen 释放链路补齐：
  - `SGBinOp` 字符串 `+` 会追踪新临时值，并在下一次拼接消费旧临时值后释放；
  - `SIOPrint` 与 `SGResourceWriteToFile` 在消费后释放追踪临时值；
  - `SGFallback` / `SGMatch` / `SGWaveMerge` / `SGGuardSelect` 的 pointer 合并路径增加临时所有权转移；
  - `SGFlexBind`/`SGFinalBind` 新声明字符串槽加入 scope 清理（覆写即时释放留到后续别名策略收口）；
  - scope `pop_file_handle_scope` 增加字符串槽位回收。
- safety 回归更新：
  - `StrcatAbAllocatesAndSupportsPairingFree`（替代原“无配对释放”文档化测试）；
  - `FreeCstrIgnoresBorrowedPointer`（借用指针误传 free 不崩溃）。
- 验证结果：
  - `ctest -L security` 全绿（69/69）；
  - `ctest -L styio_pipeline` 全绿（21/21）；
  - `ctest -L milestone` 全绿（92/92）；
  - shadow gate：M1 `40/40`、M2 `20/20`。
- ADR 落地：`docs/adr/ADR-0056-owned-cstr-lifecycle-guardrail.md`。

### 下一步

1. C.7.2：增加针对“长链字符串拼接 + 长时间流处理”的 ASan/soak 监控，量化内存曲线。
2. C.8：继续把句柄表从空壳推进到真实 `open/read/write/close` 接管，和字符串释放路径共同验证。
3. E.6/E.7：在保持 `legacy` 回退能力前提下推进默认引擎切换条件评估。

### 复现命令

```bash
cmake -S . -B build
cmake --build build -j8
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
tmp_m1="$(mktemp -d /tmp/styio-shadow-m1-XXXXXX)"
tmp_m2="$(mktemp -d /tmp/styio-shadow-m2-XXXXXX)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio tests/milestones/m1 "$tmp_m1"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio tests/milestones/m2 "$tmp_m2"

cmake -S . -B build-asan-ubsan \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DCMAKE_PREFIX_PATH='/opt/homebrew/opt/icu4c@78;/opt/homebrew/opt/llvm@18' \
  -DLLVM_DIR='/opt/homebrew/opt/llvm@18/lib/cmake/llvm' \
  -DCMAKE_C_FLAGS='-fsanitize=address,undefined -fno-omit-frame-pointer' \
  -DCMAKE_CXX_FLAGS='-fsanitize=address,undefined -fno-omit-frame-pointer'
cmake --build build-asan-ubsan -j8
ctest --test-dir build-asan-ubsan -L security --output-on-failure
ctest --test-dir build-asan-ubsan -L soak_smoke --output-on-failure
ctest --test-dir build-asan-ubsan -L styio_pipeline --output-on-failure
ctest --test-dir build-asan-ubsan -L milestone --output-on-failure
```

## Checkpoint（C.7.2：字符串拼接长跑内存安全 Soak 门禁）

### 当前状态

- 新增 Soak 用例：`StyioSoakSingleThread.ConcatMemoryGrowthBound`
  - 覆盖高频 `styio_strcat_ab` + `styio_free_cstr` 链式拼接/释放；
  - 对 RSS 增长设置阈值断言，作为内存安全（Safety）回归门禁。
- Nightly deep soak 纳入新门禁：
  - 新增 `soak_deep_concat_rss_guard`；
  - 注入放大量参数：
    - `STYIO_SOAK_CONCAT_ITERS=6000`
    - `STYIO_SOAK_CONCAT_CHAIN=16`
    - `STYIO_SOAK_CONCAT_SEG_BYTES=128`
    - `STYIO_SOAK_CONCAT_RSS_GROWTH_LIMIT_KIB=98304`
- 术语修正（taxonomy）：
  - 运行时内存/资源生命周期相关测试统一用 `StyioSafetyRuntime.*` 前缀；
  - 保留 `security` 标签用于测试分组，不再把“内存安全”表述成“安全攻防”语义。
- 验证结果：
  - `ctest -L soak_smoke` 全绿；
  - `ctest -L security` 全绿（含 `StyioSafetyRuntime.*`）；
  - `ctest -L styio_pipeline` 全绿；
  - `ctest -L milestone` 全绿。

### 下一步

1. C.8：把句柄表从空壳推进到真实 `open/read/write/close` 接管，并纳入 soak 内存阈值回归。
2. D.5：为 `ConcatMemoryGrowthBound` 补最小化失败脚本模板，沉淀 `tests/soak/regressions/` 样本。
3. CI：视 PR 时长预算评估是否将 concat 轻量阈值门禁加入常规 PR 流水线。

### 复现命令

```bash
cmake -S . -B build
cmake --build build -j8
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（C.8：句柄表真实接管 `open/read/write/close`）

### 当前状态

- runtime 文件句柄从 thread-local `unordered_map<int64_t, FILE*>` 切换到 thread-local `StyioHandleTable`：
  - `styio_file_open/open_auto/open_write` 统一走 `acquire(HandleKind::File, FILE*)`；
  - `styio_file_close` 统一走 `release(..., fclose)`，并保持 close 幂等。
- 诊断边界收敛：
  - `styio_file_read_line` / `styio_file_rewind` / `styio_file_write_cstr` 对“非零非法句柄”输出稳定 stderr 诊断并置 runtime error；
  - 兼容保留 `0` 句柄哨兵语义（返回空/不动作，不置错），避免破坏既有流式 IR 行为。
- 线程退出清理：
  - 增加 thread-local cleanup，统一 `release_all(File, fclose)` 回收遗漏文件资源。
- Safety 回归补齐：
  - `StyioSafetyRuntime`：缺失文件置错、非法读写/rewind 可诊断、close 幂等；
  - `StyioSafetyHandleTable`：`acquire/lookup/release/release_all`、kind mismatch 与 stub 清理行为。
- 验证结果：
  - `ctest -L security` 全绿（73/73）；
  - `ctest -L soak_smoke` 全绿（5/5）；
  - `ctest -L styio_pipeline` 全绿（21/21）；
  - `ctest -L milestone` 全绿（92/92）；
  - `ASan/UBSan` 构建（`build-asan-ubsan`）下 `security/soak_smoke/styio_pipeline/milestone` 全绿；
  - shadow gate：M1 `40/40`、M2 `20/20`。
- ADR 落地：`docs/adr/ADR-0058-runtime-handle-table-takeover-and-sentinel-compat.md`。

### 下一步

1. D.5：把句柄异常样本沉淀到 soak regression 模板，补“非法句柄 + 长生命周期循环”最小化回归脚本。
2. C.6 后续：将 sanitizer 验证命令固化到 CI（nightly 或手动工作流），避免只依赖本地一次性验证。
3. F.1：统一 runtime 诊断文案模板（text/jsonl）中的句柄错误描述。

### 复现命令

```bash
cmake -S . -B build
cmake --build build -j8
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
tmp_m1="$(mktemp -d /tmp/styio-shadow-m1-XXXXXX)"
tmp_m2="$(mktemp -d /tmp/styio-shadow-m2-XXXXXX)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio tests/milestones/m1 "$tmp_m1"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio tests/milestones/m2 "$tmp_m2"
```

## Checkpoint（D.4/C.6：Sanitizer 夜间门禁 + 非法句柄长跑诊断）

### 当前状态

- CI 新增 `.github/workflows/nightly-sanitizers.yml`：
  - `clang-18` + `-fsanitize=address,undefined`；
  - 执行 `security/soak_smoke/styio_pipeline/milestone` 四组 gate；
  - sanitizer 日志统一归档到 `nightly-sanitizer-artifacts`。
- Soak 新增 `StyioSoakSingleThread.InvalidHandleDiagnosticsLoop`：
  - 非零非法句柄 `rewind/read/write` 必须置 runtime error；
  - 非法句柄 `close` 维持兼容 no-op；
  - `soak_deep_invalid_handle_diag` 深跑档位已接入（`STYIO_SOAK_INVALID_HANDLE_ITERS=120000`）。
- 本地验证：
  - 常规构建与 `ASan/UBSan` 构建下，`security/soak_smoke/styio_pipeline/milestone` 均全绿。
- ADR 落地：`docs/adr/ADR-0059-nightly-sanitizer-gate-and-handle-diagnostic-soak.md`。

### 下一步

1. D.5：出现 deep soak 失败时，按 `soak-minimize` + regression 模板沉淀最小失败样本。
2. F.1：统一 runtime 句柄错误文案在 text/jsonl 的描述格式。
3. C.8 后续：评估是否将 `safety` 标签拆分到独立 test binary，避免标签粒度过粗。

### 复现命令

```bash
cmake -S . -B build
cmake --build build -j8
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -N -L soak_deep

cmake -S . -B build-asan-ubsan \
  -DCMAKE_BUILD_TYPE=RelWithDebInfo \
  -DCMAKE_PREFIX_PATH='/opt/homebrew/opt/icu4c@78;/opt/homebrew/opt/llvm@18' \
  -DLLVM_DIR='/opt/homebrew/opt/llvm@18/lib/cmake/llvm' \
  -DCMAKE_C_FLAGS='-fsanitize=address,undefined -fno-omit-frame-pointer' \
  -DCMAKE_CXX_FLAGS='-fsanitize=address,undefined -fno-omit-frame-pointer'
cmake --build build-asan-ubsan -j8
ctest --test-dir build-asan-ubsan -L security --output-on-failure
ctest --test-dir build-asan-ubsan -L soak_smoke --output-on-failure
ctest --test-dir build-asan-ubsan -L styio_pipeline --output-on-failure
ctest --test-dir build-asan-ubsan -L milestone --output-on-failure
```

## Checkpoint（F.1：Runtime 诊断文案与 JSONL 统一输出）

### 当前状态

- runtime helper 新增 `styio_runtime_last_error()`：
  - 暴露线程本地 last-error 借用字符串；
  - `styio_runtime_clear_error()` 同时清理错误标记与错误消息。
- runtime 错误路径从“直接 `fprintf(stderr)`”统一为“记录错误 + 顶层输出”：
  - `main` 在 `generator.execute()` 后检测到 runtime error 时统一调用 `styio_emit_diagnostic`；
  - text 与 jsonl 都走同一诊断出口。
- 语义约束：
  - 同一执行周期采用 first-error-wins（保留首个错误消息）。
- 回归补齐：
  - `StyioSafetyRuntime.*` 增加 last-error 断言与 clear 行为断言；
  - 新增 `StyioDiagnostics.RuntimeHelperErrorEmitsJsonlRuntimeDiagnostic`，固定 runtime 失败下 JSONL 契约。
- 验证结果：
  - `ctest -L security` 全绿（74/74）；
  - `ctest -L soak_smoke` 全绿（6/6）；
  - `ctest -L styio_pipeline` 全绿（22/22）；
  - `ctest -L milestone` 全绿（92/92）；
  - shadow gate：M1 `40/40`、M2 `20/20`。
- ADR 落地：`docs/adr/ADR-0060-runtime-last-error-and-top-level-diagnostic-unification.md`。

### 下一步

1. F.1 后续：为 runtime 子类错误（句柄/路径/写入失败）补稳定子码策略（不破坏现有 `STYIO_RUNTIME` 主码）。
2. 文档：在 CLI 诊断章节补 runtime JSONL 示例，供后续 LSP/IDE 集成复用。
3. CI：将新增 `StyioDiagnostics.*` 用例保持在 `styio_pipeline` 门禁中持续观测。

### 复现命令

```bash
cmake -S . -B build
cmake --build build -j8
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
./build/bin/styio --error-format=jsonl --file tests/milestones/m5/t06_fail_fast.styio 2>&1
```

## Checkpoint（F.2：CodeGen `NotImplemented` 误分类收敛（复合赋值路径））

### 当前状态

- 识别到一个可触发的高风险路径：
  - 输入 `x : i64 := 1` 后执行 `x += 2`；
  - 原行为抛 `StyioNotImplemented`，被顶层归类为 `RuntimeError`（退出码 `5`）。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioDiagnostics.CompoundAssignOnImmutableBindingReportsTypeError`；
  - 初始运行红灯（exit `5`），修复后转绿（exit `4`）。
- 修复内容：
  - `SGBinOp` 复合赋值目标非 mutable 时改抛 `StyioTypeError`；
  - 同类 codegen 分支（immutable 变量重定义）从 `StyioNotImplemented` 收敛为 `StyioTypeError`。
- 回归结果：
  - `ctest -L styio_pipeline` 全绿（24/24）；
  - `ctest -L security` 全绿（78/78）；
  - `ctest -L milestone` 全绿（92/92）。
- ADR 落地：`docs/adr/ADR-0062-codegen-compound-assign-typeerror.md`。

### 下一步

1. F.2 继续：对其余可触发 `StyioNotImplemented` 的用户输入路径逐个做“红灯用例 + 分类收敛”。
2. F.1 协同：保持 text/jsonl 分类一致，禁止同一语义错误在不同阶段漂移到 `RuntimeError`。
3. D.5：把“错误分类漂移”样本加入回归模板，作为中断恢复后的快速健康检查项。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio styio_test -j8
ctest --test-dir build -R '^StyioDiagnostics\.CompoundAssignOnImmutableBindingReportsTypeError$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（C.6/E.7 交叉收口：Match 非整型 scrutinee 崩溃防线）

### 当前状态

- 新增红灯回归用例：
  - `StyioParserEngine.PointerScrutineeMatchDoesNotAbortAndReportsTypeError`。
  - 场景：资源流迭代中的 `line ?={ ... }`（`line` 为指针/字符串类型）。
- 修复 `SGMatch` lowering：
  - 在 `StyioToLLVM::toLLVMIR(SGMatch*)` 引入 `coerce_match_scrutinee_to_i64`；
  - 仅允许整型（含 `i1/i32`）扩展到 `i64`；
  - 非整型 scrutinee 抛 `StyioTypeError("match scrutinee must be integer-typed")`，替代 LLVM 断言中止。
- 错误分类收敛：
  - `main.cpp` 的 codegen 执行段优先捕获 `StyioTypeError` 并映射为 `TypeError`（退出码 `4`）。
- 回归结果：
  - 新增用例由红转绿；
  - `StyioParserEngine.*` 全绿；
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*` 全绿。
- ADR 落地：`docs/adr/ADR-0055-match-scrutinee-integer-boundary.md`。

### 下一步

1. C.6 后续：把该边界进一步上提到 analyzer/typeInfer 阶段，减少 codegen 期类型兜底路径。
2. D 阶段联动：把该输入加入 soak 回归模板，覆盖“长时间流式输入 + 非整型 match”组合。
3. E.7 后续：在 `match` 语义扩展前维持该失败契约，避免隐式 `ptrtoint` 带来的不可预测行为。

### 复现命令

```bash
cmake --build build --target styio styio_test styio_security_test -j8
ctest --test-dir build -R '^StyioParserEngine\\.PointerScrutineeMatchDoesNotAbortAndReportsTypeError$' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
```

## Checkpoint（E.7 根因修复：ParamAST 空类型 + Hash `>>` 恢复）

### 当前状态

- 使用 `lldb` 定位崩溃根因：
  - `ToStyioIR.cpp:param_to_sgarg` 解引用未标注类型参数时的空 `ParamAST::var_type` 触发段错误。
- 修复 `ParamAST` 构造：
  - `var_name` / `var_type` / `val_init` 与 `VarAST` 基类字段对齐；
  - 未显式类型参数也保有默认 `TypeAST`。
- hash iterator 定义恢复：
  - legacy/new `# ... >> ...` 分支恢复接管（保留单参数约束）。
- 回归更新：
  - `StyioParserEngine.LegacyAndNewMatchOnHashIteratorDefinition` 改为成功路径断言（exit 0 + 双引擎输出一致）；
  - `StyioParserEngine.LegacyAndNewMatchOnUntypedParamFunction` 新增未标注参数函数防回归；
  - `StyioSecurityNewParserStmt.MatchesLegacyOnFunctionDefSubsetSamples` 重新纳入 hash iterator 定义样例。
- 定向回归结果：
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*` 全绿；
  - `StyioParserEngine.*` 全绿；
  - `# iter_only(x) >> (n) => >_(n)` 样例在 legacy/new 下均退出码 `0`。
- ADR 落地：`docs/adr/ADR-0053-param-ctor-null-type-and-hash-iterator-restore.md`。

### 下一步

1. E.7 后续：评估 `?=` / iterator forward 组合语义（当前实现仍偏保守），补行为型 golden。
2. D 阶段联动：将 hash iterator 路径加入 soak 长跑样例，观察长生命周期稳定性。
3. F.1：统一 iterator 相关诊断文案，减少 legacy/new 文本差异。

### 复现命令

```bash
cmake --build build --target styio styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
cat >/tmp/styio-hash-iter-smoke.styio <<'EOF'
# iter_only(x) >> (n) => >_(n)
iter_only([1, 2, 3])
EOF
./build/bin/styio --parser-engine=legacy --file /tmp/styio-hash-iter-smoke.styio
./build/bin/styio --parser-engine=new --file /tmp/styio-hash-iter-smoke.styio
```

## Checkpoint（E.7 稳定性收口：Hash Iterator `?=` Forward Chain Guard）

### 当前状态

- 收敛一个“非崩溃但语义静默错误”路径：
  - `# f(x) >> (n) ?= 2 => ...` 在恢复后会被解析但行为不符合预期（静默空输出）。
- 在 `parse_iterator_with_forward` 增加 guard：
  - 首子句为 `CheckEqual/Cases` 时，稳定拒绝；
  - 多子句 forward chain（`size > 1`）时，稳定拒绝。
- 回归补齐：
  - `StyioParserEngine.HashIteratorMatchForwardChainReturnsParseError`（双引擎 JSONL：`ParseError` + 退出码 `3`）。
- 兼容性验证：
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*` 全绿；
  - `StyioParserEngine.*` 全绿；
  - shadow gate：M1 `40/40`，M2 `20/20`。
- ADR 落地：`docs/adr/ADR-0054-hash-iterator-match-forward-chain-guard.md`。

### 下一步

1. E.7 后续：为 iterator `?=` forward 组合补独立语义设计，再解除 guard。
2. D 阶段联动：将该失败样本加入 soak/回归模板，防止 silent behavior 回归。
3. F.1：统一这类“暂不支持组合语法”的诊断文案模板。

### 复现命令

```bash
cmake --build build --target styio styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
cat >/tmp/styio-hash-iter-forward-guard.styio <<'EOF'
# iter_only(x) >> (n) ?= 2 => >_(n)
iter_only([1, 2, 3])
EOF
./build/bin/styio --error-format=jsonl --parser-engine=legacy --file /tmp/styio-hash-iter-forward-guard.styio
./build/bin/styio --error-format=jsonl --parser-engine=new --file /tmp/styio-hash-iter-forward-guard.styio
```

## Checkpoint（测试韧性：Security 解析辅助函数异常清理）

### 当前状态

- `tests/security/styio_security_test.cpp` 的解析辅助函数
  - `parse_expr_to_repr`
  - `parse_program_to_repr`
  - `parse_program_engine_to_repr`
  统一补齐异常路径清理：
  - 释放 AST/context/tokens；
  - 调用 `StyioAST::destroy_all_tracked_nodes()` 防止异常后状态污染。
- 重新启用 security 侧拒绝回归：
  - `RejectsHashIteratorMatchForwardChainWithStableError`。
- 回归结果：
  - `StyioSecurityNewParser(Expr|Stmt|Shadow).*` 全绿；
  - `StyioParserEngine.*` 全绿。

### 下一步

1. 将同类异常清理模式扩展到其余测试辅助函数（fuzz/pipeline harness）。
2. 评估是否在测试工具层抽出统一 RAII fixture，减少重复清理逻辑。

### 复现命令

```bash
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
```

## Checkpoint（E.7 风险收口：Hash `>>` Iterator 定义 Crash Guard）

### 当前状态

- legacy/new 在 `# f(x) >> ...` 定义路径统一改为稳定拒绝：
  - 抛出 `StyioNotImplemented("hash iterator definitions are temporarily disabled (runtime crash guard)")`；
  - CLI 侧统一映射为 `ParseError`（退出码 `3`），不再段错误退出。
- 安全回归更新：
  - 删除“hash iterator 定义应成功”等价样例；
  - 新增 `RejectsHashIteratorFunctionDefWithStableError`，断言 legacy/new 都抛 `StyioBaseException`。
- parser-engine 回归新增：
  - `HashIteratorDefinitionReturnsParseErrorWithoutCrash`；
  - 双引擎在 `--error-format=jsonl` 下都返回退出码 `3`，且包含 `"category":"ParseError"`。
- shadow gate 结果保持：
  - M1 `40/40`；
  - M2 `20/20`。
- ADR 落地：`docs/adr/ADR-0052-hash-iterator-crash-guard.md`。

### 下一步

1. E.7 后续：将 `# ... >> ...` 的恢复实现拆成独立子切片（parser 语义保持守卫，优先查 runtime 崩溃根因）。
2. D 阶段联动：为 iterator runtime 增加 soak 压力与内存守卫回归，避免“修复即回归”。
3. F.1：统一 hash 子集错误文案模板，保持 JSON 分类稳定、文本提示可读。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParser(Expr|Stmt|Shadow)\\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\\.' --output-on-failure
tmp_dir="$(mktemp -d)"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m1 "$tmp_dir"
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m2 "$tmp_dir"
```

## Checkpoint（F.1.1：Runtime `subcode` 稳定化与覆盖补齐）

### 当前状态

- runtime `subcode` 统一集中常量，冻结当前子码集合：
  - `STYIO_RUNTIME_INVALID_FILE_HANDLE`
  - `STYIO_RUNTIME_FILE_PATH_NULL`
  - `STYIO_RUNTIME_FILE_OPEN_READ`
  - `STYIO_RUNTIME_FILE_OPEN_WRITE`
- safety 回归补齐：
  - `NullReadPathSetsStableSubcode`
  - `NullWritePathSetsStableSubcode`
  - `MissingWritePathSetsStableSubcode`
  - `FirstErrorWinsAcrossMultipleRuntimeFailures`
- 诊断回归补齐：
  - `StyioDiagnostics.RuntimeWriteHelperErrorEmitsJsonlRuntimeDiagnostic`
  - 验证 `--error-format=jsonl` 下 runtime 写失败输出 `subcode=STYIO_RUNTIME_FILE_OPEN_WRITE`。
- 文档收口：
  - `README.md` 新增 diagnostics 章节（text/jsonl + 字段说明）；
  - ADR 新增 `ADR-0061-runtime-subcode-taxonomy-freeze.md`。
- 验证结果：
  - `ctest -L security` 全绿（78/78）；
  - `ctest -L styio_pipeline` 全绿（23/23）；
  - `ctest -L soak_smoke` 全绿（6/6）；
  - `ctest -L milestone` 全绿（92/92）。

### 下一步

1. F.2：梳理剩余 `NotImplemented` 高风险路径，按“先失败后修复”补最小回归后再做语义放开。
2. F.1 后续：对 runtime 新增子码走 ADR + 回归双更新，保持 machine-readable 合约稳定。
3. D.5：把 runtime 子码相关失败样本纳入 soak 回归模板，提升长生命周期定位效率。

### 复现命令

```bash
cmake -S . -B build
cmake --build build -j8
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
./build/bin/styio --error-format=jsonl --file tests/milestones/m5/t06_fail_fast.styio 2>&1
```
