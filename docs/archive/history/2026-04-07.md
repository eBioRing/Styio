# 2026-04-07 开发记录

**Purpose:** 记录 C.5 state clone 覆盖扩展 checkpoint，保证“先失败后修复”书签与恢复命令可直接复现。

**Last updated:** 2026-04-07

> 术语说明：本文件较早条目中的 `new parser` / `--parser-engine=new` 自 2026-04-07 起统一视为 `nightly parser` / `--parser-engine=nightly`；`new` 仅保留为兼容别名。

## Checkpoint（C.5：state helper 内联克隆补齐 `MatchCases/Cases`）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioDiagnostics.StateInlineMatchCasesFunctionUsesCallArgument`；
  - 红灯阶段可复现 `Styio.TypeError: unsupported AST node in inlined state expression clone`（exit `4`）。
- 修复内容：
  - `ToStyioIR.cpp::clone_state_expr_with_subst` 新增 `CasesAST` 克隆分支；
  - `ToStyioIR.cpp::clone_state_expr_with_subst` 新增 `MatchCasesAST` 克隆分支；
  - 保持边界检查：`MatchCases` 克隆后若非 `CasesAST`，仍抛 `TypeError`。
- 回归结果：
  - `StyioDiagnostics.SingleArgStateFunctionInliningUsesCallArgument` 全绿；
  - `StyioDiagnostics.BlockStateFunctionInliningUsesCallArgument` 全绿；
  - `StyioDiagnostics.StateInlineMatchCasesFunctionUsesCallArgument` 全绿（输出 `10/12/15`）；
  - `ctest -L styio_pipeline` 全绿（32/32）；
  - `ctest -L security` 全绿（112/112）；
  - `build-asan-ubsan` 下新用例全绿（1/1）。
- ADR 落地：`docs/adr/ADR-0082-state-inline-matchcases-clone-coverage.md`。

### 下一步

1. C.5：继续扩展 state clone 覆盖，优先补齐高频表达式节点，减少 unsupported fallback。
2. C.6：保持每次 clone 覆盖扩展后的 sanitizer 验证（`styio_pipeline + security`）。
3. D.5：把 state inline 失败样本纳入恢复演练首批健康检查。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioDiagnostics\.(SingleArgStateFunctionInliningUsesCallArgument|BlockStateFunctionInliningUsesCallArgument|StateInlineMatchCasesFunctionUsesCallArgument)$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure

cmake --build build-asan-ubsan --target styio_test -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -R '^StyioDiagnostics\.StateInlineMatchCasesFunctionUsesCallArgument$' --output-on-failure
```

## Checkpoint（D.3：state helper `MatchCases` 内联 Soak 门禁）

### 当前状态

- 目标：
  - 将 `ADR-0082` 新增的 `MatchCases/Cases` 克隆路径纳入长跑守门，避免仅有单次单元覆盖。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioSoakSingleThread.StateInlineMatchCasesProgramLoop`；
  - 样例固定为 `# pulse = (x) => @[sum = 0](out = x ?= {...})`；
  - 每轮断言输出稳定 `10/12/15`。
- 修复内容：
  - `tests/soak/styio_soak_test.cpp`：新增 `StateInlineMatchCasesProgramLoop`。
  - `tests/CMakeLists.txt`：新增 `soak_deep_state_inline_matchcases_program`（`STYIO_SOAK_STATE_MATCH_ITERS=1500`）。
  - `tests/soak/README.md`：补齐新用例与 deep 环境变量。
- 回归结果：
  - `ctest -R StateInlineMatchCasesProgramLoop` 全绿（1/1）；
  - `ctest -R soak_deep_state_inline_matchcases_program` 全绿（1/1）；
  - `ctest -L soak_smoke` 全绿（8/8）；
  - `ctest -L styio_pipeline` 全绿（32/32）。
- ADR 落地：`docs/adr/ADR-0083-state-inline-matchcases-soak-gate.md`。

### 下一步

1. D.3：继续补 state inline 的复合表达式长跑样本（优先历史 crash/fuzz 样本）。
2. D.4：保持新增 soak_deep gate 与 nightly 工作流同步。
3. C.6：对 state inline 相关改动保持 sanitizer 组测节奏。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_soak_test -j8
ctest --test-dir build -R StateInlineMatchCasesProgramLoop --output-on-failure
ctest --test-dir build -R soak_deep_state_inline_matchcases_program --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
```

## Checkpoint（C.5：state helper 内联克隆补齐 `InfiniteAST`）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioDiagnostics.StateInlineInfiniteLiteralFunctionUsesCallArgument`；
  - 红灯阶段稳定复现 `Styio.TypeError: unsupported AST node in inlined state expression clone: 75`（exit `4`）。
- 修复内容：
  - `ToStyioIR.cpp::clone_state_expr_with_subst` 新增 `InfiniteAST` 克隆分支；
  - 支持 `InfiniteType::Original` 与 `InfiniteType::Incremental` 两种形态克隆。
- 回归结果：
  - `StyioDiagnostics.StateInlineInfiniteLiteralFunctionUsesCallArgument` 全绿（输出 `0/0`）；
  - 相关 state inline 组合回归全绿（`1/3/6`、`10/12/15`、`0/0`）；
  - `ctest -L styio_pipeline` 全绿（33/33）；
  - `ctest -L security` 全绿（112/112）；
  - `build-asan-ubsan` 下新用例全绿（1/1）。
- ADR 落地：`docs/adr/ADR-0084-state-inline-infinite-clone-and-soak-gate.md`。

### 下一步

1. C.5：继续扩展 state clone 覆盖，优先处理仍可能在内联路径出现的控制流节点（如 `CondFlow` 组合形态）。
2. C.6：每次 clone 扩展后保持 `security + styio_pipeline + ASan/UBSan` 验证节奏。
3. D.5：把 `unsupported AST node` 回归样本持续沉淀到恢复演练首批健康检查。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioDiagnostics\.StateInlineInfiniteLiteralFunctionUsesCallArgument$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure

cmake --build build-asan-ubsan --target styio_test -j8
ASAN_OPTIONS='detect_leaks=0:halt_on_error=1:abort_on_error=1' \
UBSAN_OPTIONS='print_stacktrace=1:halt_on_error=1' \
ctest --test-dir build-asan-ubsan -R '^StyioDiagnostics\.StateInlineInfiniteLiteralFunctionUsesCallArgument$' --output-on-failure
```

## Checkpoint（D.3：state helper `InfiniteAST` 内联 Soak 门禁）

### 当前状态

- 目标：
  - 将 `ADR-0084` 新增的 `InfiniteAST` 内联克隆路径纳入长跑守门。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioSoakSingleThread.StateInlineInfiniteProgramLoop`；
  - 样例固定为 `# pulse = (x) => @[sum = 0](out = [...])`；
  - 每轮断言输出稳定 `0/0`。
- 修复内容：
  - `tests/soak/styio_soak_test.cpp`：新增 `StateInlineInfiniteProgramLoop`；
  - `tests/CMakeLists.txt`：新增 `soak_deep_state_inline_infinite_program`（`STYIO_SOAK_STATE_INFINITE_ITERS=1500`）；
  - `tests/soak/README.md`：补齐新用例与 deep 环境变量。
- 回归结果：
  - `ctest -R StateInlineInfiniteProgramLoop` 全绿（1/1）；
  - `ctest -R soak_deep_state_inline_infinite_program` 全绿（1/1）；
  - `ctest -L soak_smoke` 全绿（9/9）；
  - `ctest -L styio_pipeline` 全绿（33/33）。

### 下一步

1. D.3：继续补 state inline 的“历史崩溃样本优先”长跑用例池。
2. D.4：保持新增 deep gate 与 nightly 同步（防止只在本地覆盖）。
3. C.6：对所有新增 state inline 路径持续跑 sanitizer 目标回归。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_soak_test -j8
ctest --test-dir build -R '^StyioSoakSingleThread\.StateInlineInfiniteProgramLoop$' --output-on-failure
ctest --test-dir build -R '^soak_deep_state_inline_infinite_program$' --output-on-failure
ctest --test-dir build -L soak_smoke --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
```

## Checkpoint（E.6：Parser 默认引擎切换到 `new`）

### 当前状态

- 变更内容：
  - `main.cpp` 将 `--parser-engine` 默认值从 `legacy` 切到 `new`；
  - 解析入口初始化默认引擎同步为 `StyioParserEngine::New`；
  - 保留 `--parser-engine=legacy` 显式回退能力。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioParserEngine.DefaultEngineIsNewInShadowArtifact`；
  - 通过 `--parser-shadow-compare --parser-shadow-artifact-dir` 读取 JSONL 工件，断言默认 `primary_engine` 为 `new`。
- 回归结果：
  - 新用例全绿（1/1）；
  - `ctest -L styio_pipeline` 全绿（34/34）；
  - `ctest -L security` 全绿（112/112）；
  - `ctest -L milestone` 全绿（92/92）。
- ADR 落地：`docs/adr/ADR-0085-parser-default-switch-to-new-with-legacy-fallback.md`。

### 下一步

1. E.6/E.7：继续观测 shadow gate 连续窗口，确认默认切换后无差异漂移。
2. E.7：在稳定窗口内保留 `legacy` 回退并逐步压缩剩余 fallback 面。
3. F.3：继续 parser 边界巡检，防止默认切换后暴露新的错误路径异常。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioParserEngine\.DefaultEngineIsNewInShadowArtifact$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
ctest --test-dir build -L milestone --output-on-failure
```

## Checkpoint（D.5：一键恢复脚本 `checkpoint-health`）

### 当前状态

- 目标：
  - 将 parser 默认切换 + state inline + soak + pipeline/security 的恢复链路收敛为单命令。
- 交付内容：
  - 新增 `scripts/checkpoint-health.sh`；
  - 支持 `--build-dir`、`--asan-build-dir`、`--no-asan` 参数；
  - 固化阶段输出前缀 `[checkpoint-health]` 便于日志定位。
- 覆盖范围：
  - `StyioParserEngine.DefaultEngineIsNewInShadowArtifact`；
  - state inline 诊断回归（`1/3/6`、`10/12/15`、`0/0`）；
  - state inline soak smoke + deep 三条；
  - `styio_pipeline`、`security` 标签；
  - 可选 ASan/UBSan 目标回归。
- 验证结果：
  - `./scripts/checkpoint-health.sh --no-asan` 全绿（含 deep soak + pipeline + security）。
- ADR 落地：`docs/adr/ADR-0086-checkpoint-health-batch-script.md`。

### 下一步

1. D.5：将后续新增高风险回归优先并入 `checkpoint-health`，避免恢复脚本与现实路径漂移。
2. D.4：nightly 任务按需复用该脚本的测试清单，保持本地/CI 一致。
3. F.3：若新增 parser 边界回归，优先纳入脚本的快速检查段。

### 复现命令

```bash
./scripts/checkpoint-health.sh --no-asan
# 或完整含 ASan/UBSan
./scripts/checkpoint-health.sh
```

## Checkpoint（E.7：NewParser Block/Control 子集接管 Hash Block Body）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.SubsetStartGateIncludesBlockAndControlStarters`；
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnBlockControlSubsetSamples`；
  - 红灯阶段 `parse_main_block_new_subset(...)` 在 block/control 样例上会抛 `StyioSyntaxError("unexpected statement token in new parser subset")`。
- 修复内容：
  - `NewParserExpr.cpp` 扩展 statement subset token/start gate，纳入 `{}` block、`<<`、`^`、`>>`、`...`、`|>`；
  - 新增 `parse_block_only_new_subset(...)` 与 `parse_block_with_forward_new_subset(...)`；
  - `parse_hash_stmt_new_subset(...)` 的 `# ... => { ... }` body 改走 new block subset，而不是 legacy `parse_block_with_forward(...)`。
- 回归结果：
  - `ctest -R '^StyioSecurityNewParserStmt\\.'` 全绿（12/12）；
  - `ctest -L styio_pipeline` 全绿（34/34）；
  - `ctest -L security` 全绿（114/114）。
- ADR 落地：`docs/adr/ADR-0087-new-parser-block-control-subset.md`。

### 下一步

1. E.7：继续压缩 block 内剩余 legacy 边界，优先评估 `match/cases` 与 `@` 相关语句的子集接管切片。
2. E.7：评估 `parse_main_block_new_shadow(...)` 是否可以从“整文件 token 白名单”向“更细粒度 fallback”推进。
3. F.3：继续沿 parser 错误路径巡检，优先锁定默认 `new` 主路径下的内部异常泄露点。

### 风险

1. 当前仅接管 block/control 主体；block 尾随 forward 列表仍复用 legacy `parse_forward_as_list(...)`。
2. 更复杂的 loop / `@` / `match-cases` block 体若超出新子集，当前仍可能通过 hash 外层保守回退到 legacy。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test -j8
ctest --test-dir build -R '^StyioSecurityNewParserStmt\.' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
```

## Checkpoint（E.7：NewParser 接管表达式 Postfix `?=` Match-Cases）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnMatchCasesSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM3MatchExprSample`；
  - 红灯阶段 `parse_main_block_new_subset(...)` 在 `label = x % 2 ?= { ... }` 这类输入上无法继续前进，只能依赖 legacy fallback。
- 修复内容：
  - `NewParserExpr.cpp` 为完整表达式新增 postfix 处理，仅接管 `?={...}`；
  - `parse_expr_new_subset(...)`、括号子表达式与函数调用参数现均可识别 `MatchCasesAST`；
  - cases body 暂时继续复用 legacy `parse_cases_only(context)`，保持切片范围受控。
- 回归结果：
  - `ctest -R '^StyioSecurityNewParserStmt\.'` 全绿（13/13）；
  - `ctest -R '^StyioParserEngine\.LegacyAndNewMatchOnM3MatchExprSample$'` 全绿（1/1）；
  - `ctest -L styio_pipeline` 全绿（35/35）；
  - `ctest -L security` 全绿（115/115）。
- ADR 落地：`docs/adr/ADR-0088-new-parser-matchcases-postfix-subset.md`。

### 下一步

1. E.7：继续压缩 postfix 相关剩余 legacy 边界，优先评估 iterator / redirect 是否能拆成独立子切片。
2. E.7：继续评估 `parse_main_block_new_shadow(...)` 的整文件 token gate 是否可以细化为更小粒度回退。
3. F.3：继续巡检默认 `new` 主路径在 parser 错误场景下的异常边界，避免内部异常文案泄露。

### 风险

1. 当前只接管 `?={...}`；其它 postfix 仍未纳入 new subset。
2. `cases` body 继续调用 legacy `parse_cases_only(...)`，尚未完全摆脱旧路径。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_security_test styio_test -j8
ctest --test-dir build -R '^StyioSecurityNewParserStmt\.' --output-on-failure
ctest --test-dir build -R '^StyioParserEngine\.LegacyAndNewMatchOnM3MatchExprSample$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
```

## Checkpoint（E.7：NewParser 主入口改为逐语句 Fallback，并输出 Route Stats）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioParserEngine.ShadowArtifactDetailCapturesPerStatementFallbackStats`；
  - 用混合程序（subset 语句 + unsupported 语句）读取 shadow artifact，确认 `detail` 字段包含 `primary_route=...legacy_fallback_statements=...`；
  - 红灯阶段无法观测 `new` 主路径内部路由，且任一 unsupported token 会让整文件直接回退 legacy。
- 修复内容：
  - `Parser.cpp::parse_main_block_new_shadow(...)` 从“整文件 token gate”改为“逐语句先尝试 new subset，失败再回退当前语句 legacy”；
  - 新增 `StyioParserRouteStats`，记录 `new_subset_statements` 与 `legacy_fallback_statements`；
  - `main.cpp` 将 route stats 写入 shadow artifact `detail`；
  - `NewParserExpr.cpp` 为 unsupported continuation（如 `<-` / `>>` / `>` / `[` / `|` / `<<`）显式抛异常，避免 name-led 语句被 new expr 子集吃半截后再在下一 token 处炸成 ParseError。
- 回归结果：
  - `ctest -R '^StyioParserEngine\.(PointerScrutineeMatchDoesNotAbortAndReportsTypeError|ShadowArtifactDetailCapturesPerStatementFallbackStats)$'` 全绿（2/2）；
  - `ctest -L styio_pipeline` 全绿（36/36）；
  - `ctest -L security` 全绿（115/115）。
- ADR 落地：`docs/adr/ADR-0089-new-parser-per-statement-fallback-and-route-stats.md`。

### 下一步

1. E.7：继续减少 route stats 中的 `legacy_fallback_statements`，优先拆 iterator / redirect / index-op 这类仍会触发 continuation fallback 的表达式尾缀。
2. E.7：继续把 route stats 接入更多恢复脚本或 nightly 统计，避免只在单测里可见。
3. F.3：沿新主路径继续巡检“吃半截 expression”类错误，新增样本即转成显式 fallback 或稳定诊断。

### 风险

1. 当前 route stats 只统计主入口语句级路由，不代表 block/cases/iterator 内部已完全去 legacy。
2. `unsupported continuation` 列表仍是白名单之外的保守拒绝，后续每接管一种 postfix 都需要同步收缩该列表。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R '^StyioParserEngine\.(PointerScrutineeMatchDoesNotAbortAndReportsTypeError|ShadowArtifactDetailCapturesPerStatementFallbackStats)$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
```

## Checkpoint（E.7：NewParser 接管资源写入/重定向 Postfix 子集）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnResourcePostfixSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM5WriteFileSample`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM5RedirectSample`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForResourcePostfixSubset`；
  - 红灯阶段 `expr >> @file{...}` / `expr -> @file{...}` 在 `new` 主路径上会命中 `unsupported continuation`，只能逐语句回退到 legacy。
- 修复内容：
  - `NewParserExpr.cpp` 在 postfix 阶段新增资源后缀接管；
  - `expr >> @file{...}` / `expr >> @{...}` 现在直接构造 `ResourceWriteAST`；
  - `expr -> @file{...}` / `expr -> @{...}` 现在直接构造 `ResourceRedirectAST`；
  - `>>` 若后面不是 `@...`，仍维持稳定异常并由外层 fallback 接管，不把 iterator tail 混进本切片。
- 回归结果：
  - `ctest --test-dir build -R 'ResourcePostfix|M5WriteFile|M5Redirect|ZeroFallbackForResourcePostfix' --output-on-failure` 全绿（4/4）；
  - `ctest --test-dir build -L styio_pipeline` 全绿（39/39）；
  - `ctest --test-dir build -L security` 全绿（117/117）。
- ADR 落地：`docs/adr/ADR-0090-new-parser-resource-postfix-subset.md`。

### 下一步

1. E.7：继续压缩表达式尾缀 fallback，优先评估 `x[i]` / token-index 这一类仍会触发 `unsupported continuation` 的路径。
2. E.7：继续拆分 `>>` 的非资源语义，把 iterator tail 与资源写入保持分离推进。
3. F.3：继续用 route stats 巡检默认 `new` 主路径，优先处理“看起来像 subset，实际还会半途回退”的尾缀样例。

### 风险

1. 当前只接管资源型 postfix；`>>` 的 iterator tail 仍走 legacy fallback。
2. 以 `@file{...}` 开头的流式输入语句不在这个 checkpoint 范围内，仍需后续独立切片。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'ResourcePostfix|M5WriteFile|M5Redirect|ZeroFallbackForResourcePostfix' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
```

## Checkpoint（E.7：NewParser 接管 Name-Iterator 与 Instant Pull 子集）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnIteratorStmtSubsetSamples`；
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnInstantPullSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM5ReadFileSample` / `LegacyAndNewMatchOnM5AutoDetectSample` / `LegacyAndNewMatchOnM5PipeFuncSample` / `LegacyAndNewMatchOnM7InstantPullSample`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForIteratorSubset`；
  - 红灯阶段 `f >> #(line) => { ... }` 与 `(<< @file{...})` 都会触发入口级 fallback。
- 修复内容：
  - `NewParserExpr.cpp` 的 name-led stmt 分支新增 `name >> ...`，直接复用 `parse_iterator_only(...)`；
  - `(` primary 分支新增 instant pull 识别，直接构造 `InstantPullAST`；
  - 本切片明确不接管 collection-start iterator（例如 `@file{...} >> ...` / `[1,2,3] >> ...`）。
- 回归结果：
  - `ctest --test-dir build -R 'IteratorSubset|InstantPullSubset|M5ReadFile|M5AutoDetect|M5PipeFunc|M7InstantPull' --output-on-failure` 全绿（6/6）；
  - `ctest --test-dir build -L styio_pipeline` 全绿（44/44）；
  - `ctest --test-dir build -L security` 全绿（119/119）。
- 额外验证：
  - `scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m5 ...` 仍因 `t06_fail_fast` 运行期失败返回非零，但所有 shadow artifact 仍为 `match`；
  - M5 全套 route stats 已降到 `legacy_fallback_statements=0`；
  - M7 `t04_instant_pull` 已降到 `new_subset_statements=3,legacy_fallback_statements=0`。
- ADR 落地：`docs/adr/ADR-0091-new-parser-iterator-and-instant-pull-subset.md`。

### 下一步

1. E.7：继续收 `@[...]` 起始语句，优先把 snapshot/state decl 从入口级 fallback 中摘出来。
2. E.7：将主要剩余 fallback 缩到 collection-start iterator（`@file... >>` / `[1,2,3] >>`）。
3. F.3：继续用 route stats 观测“入口级零回退但内部仍复用 legacy helper”的边界，避免误判完成度。

### 风险

1. `name >> ...` 当前只是在入口级复用稳定叶子 helper，不代表完整 iterator grammar 已由 new parser 原生拥有。
2. collection-start iterator 仍未收口，M7 的 zip/stream 类样例仍大量依赖 legacy。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'IteratorSubset|InstantPullSubset|M5ReadFile|M5AutoDetect|M5PipeFunc|M7InstantPull' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
tmp_dir=$(mktemp -d)
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m5 "$tmp_dir"
```

## Checkpoint（E.7：NewParser 接管 `@[...]` Snapshot / State Decl 起始子集）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnSnapshotDeclSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM7SnapshotSample`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForSnapshotDeclSubset`；
  - 红灯阶段 `@[...]` 起始语句不在 subset start gate 内，只能直接回退 legacy。
- 修复内容：
  - `parse_state_decl_after_at(...)` 提升为可复用接口；
  - `new` stmt subset 新增 `TOK_AT` 起始支持，但只接管 `@[...]`；
  - 非 `@[...]` 的 `@...` 起始语句继续稳定 fallback，不混入本切片；
  - subset token/start gate 同步纳入 `TOK_AT` / `[` / `]`。
- 回归结果：
  - `ctest --test-dir build -R 'Snapshot.*Subset|M7SnapshotSample' --output-on-failure` 全绿（3/3）；
  - `ctest --test-dir build -L styio_pipeline` 全绿（46/46）；
  - `ctest --test-dir build -L security` 全绿（120/120）。
- 额外验证：
  - `scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 ...` 全绿（20/20 artifacts match）；
  - `t03_snapshot` 从 `legacy_fallback_statements=2` 降到 `1`；
  - `t06_snapshot_lock` 从 `3` 降到 `2`；
  - `t09_snapshot_accum` 从 `3` 降到 `2`。
- ADR 落地：`docs/adr/ADR-0092-new-parser-at-snapshot-subset.md`。

### 下一步

1. E.7：剩余主要入口级 fallback 已集中在 collection-start iterator：
   - `@file{...} >> ...`
   - `[1,2,3] >> ...`
2. E.7：下一刀优先考虑只接管 collection atom 起始，不把 zip/forward/cases 连锁语义一次性拉入。
3. F.3：继续用 route stats 监控 M7 套件，避免新增样例时回退比例反弹。

### 风险

1. 当前 `TOK_AT` 只对 `@[...]` 开口；`@file{...}` / `@(…)` 仍会保守回退。
2. M7 剩余样例的主要难点不再是 `@[...]`，而是 iterator / zip 主体 grammar。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'Snapshot.*Subset|M7SnapshotSample' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
tmp_dir=$(mktemp -d)
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 "$tmp_dir"
```

## Checkpoint（E.7：NewParser 接管 `@file... >> ...` 资源流入口）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNewParserStmt.MatchesLegacyOnAtResourceSubsetSamples`；
  - 新增 `StyioParserEngine.LegacyAndNewMatchOnM7ZipFilesSample` / `LegacyAndNewMatchOnM7ArbitrageSample` / `LegacyAndNewMatchOnM7FullPipelineSample`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForAtResourceSubset`；
  - 红灯阶段 `@file{...} >> ...` 仍被 `new` 入口拒绝，只能逐语句 fallback 到 legacy。
- 修复内容：
  - 把 legacy 的 `TOK_AT` 入口分发提炼为 `parse_at_stmt_or_expr(...)`；
  - `parse_stmt_or_expr(...)` 与 `new` stmt subset 统一复用该 helper；
  - `@[...]`、`@(…)`、`@file{...}` / `@{...}` 起始语句现在都可以由 `new` 入口接住；
  - 本切片明确不处理 `[ ... ] >> ...` list-start iterator。
- 回归结果：
  - `ctest --test-dir build -R 'AtResourceSubset|M7ZipFilesSample|M7ArbitrageSample|M7FullPipelineSample' --output-on-failure` 全绿（5/5）；
  - `ctest --test-dir build -L styio_pipeline` 全绿（50/50）；
  - `ctest --test-dir build -L security` 全绿（121/121）。
- 额外验证：
  - `scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 ...` 全绿（20/20 artifacts match）；
  - `t05_zip_files` 已降到 `new_subset_statements=1,legacy_fallback_statements=0`；
  - `t08_arbitrage` 已降到 `1,0`；
  - `t10_full_pipeline` 已降到 `1,0`；
  - 剩余非零 fallback 仅见 `t01_zip_collections`、`t02_zip_unequal` 与含 `[1,2,3] >> ...` 的 snapshot 组合样例。
- ADR 落地：`docs/adr/ADR-0093-new-parser-at-resource-iterator-entry.md`。

### 下一步

1. E.7：剩余入口级 fallback 现在主要只剩 `[ ... ] >> ...` list-start iterator。
2. E.7：下一刀应避免一次性实现完整 zip grammar，先只把 list/tuple collection atom 交给可复用 helper。
3. F.3：继续以 M7 shadow gate 为主观测窗口，确认 route stats 不再因新增资源流样例反弹。

### 风险

1. `@...` 入口虽然已接住，但内部 iterator/zip 语义仍复用 legacy helper，不代表已完成彻底切换。
2. `[ ... ]` 起始语句仍未收口，M7 的最后一批 fallback 还在这条线上。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'AtResourceSubset|M7ZipFilesSample|M7ArbitrageSample|M7FullPipelineSample' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
tmp_dir=$(mktemp -d)
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 "$tmp_dir"
```

## Checkpoint（E.7/F.3：parser 命名切到 nightly，并冻结函数后缀策略）

### 当前状态

- 代码命名迁移：
  - parser engine 对外活动命名由 `new` 切到 `nightly`；
  - 保留 `--parser-engine=new` 作为 deprecated compatibility alias；
  - 共享入口统一为 `_latest`，例如 `parse_main_block_with_engine_latest(...)`、`styio_parse_engine_to_repr_latest(...)`、`styio_write_shadow_artifact_latest(...)`；
  - 稳定旧路径入口显式为 `_legacy`，例如 `parse_main_block_legacy(...)`、`parse_stmt_or_expr_legacy(...)`。
- 测试与 gate：
  - `DefaultEngineIsNightlyInShadowArtifact` 改为冻结默认引擎契约；
  - 新增 `DeprecatedNewAliasMatchesNightlySample`，防回归验证 `new -> nightly` CLI 别名；
  - pipeline/security/fuzz harness 统一改用 `parse_main_block_legacy(...)` 或 `parse_main_block_with_engine_latest(...)`。
- 流程文档：
  - `../assets/workflow/CHECKPOINT-WORKFLOW.md`、`../assets/templates/REFACTOR-WORKFLOW-TEMPLATE.md`、`../specs/AGENT-SPEC.md`、`../assets/workflow/TEST-CATALOG.md` 已写入 `_legacy / _nightly / _latest / _draft` 规则；
  - 新增 ADR：`docs/adr/ADR-0094-refactor-function-suffix-policy.md`。

### 下一步

1. E.7：继续把剩余 list-start iterator 接管切片按新命名体系推进，未完成版本显式落 `_draft`。
2. F.3：继续清点 parser 双轨范围内仍未后缀化的历史 helper，按“首次触碰即迁名”原则收口。
3. 文档维护：后续新增 checkpoint/ADR 禁止再使用活动术语 `new parser`。

### 风险

1. 历史文件名（如 `NewParserExpr.*`）仍保留旧模块名，当前只先冻结函数级命名；如需改文件名，必须单独做可回滚 checkpoint。
2. 历史 history/ADR 保留原文，阅读时必须按本页顶部术语说明解释。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'DefaultEngineIsNightlyInShadowArtifact|DeprecatedNewAliasMatchesNightlySample' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
# fuzz smoke 需先以 -DSTYIO_ENABLE_FUZZ=ON 重新配置
```

## Checkpoint（E.7：nightly parser 接管 `[ ... ] >> ...` list-start iterator 与换行边界）

### 当前状态

- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNightlyParserStmt.MatchesLegacyOnIteratorStmtSubsetSamples` 的 list-start 样例；
  - 新增 `StyioParserEngine.LegacyAndNightlyMatchOnM7ZipCollectionsSample`；
  - 新增 `StyioParserEngine.LegacyAndNightlyMatchOnM7ZipUnequalSample`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForListIteratorSubset`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackAcrossListBoundaryAfterBind`；
  - 红灯阶段 `[ ... ] >> ...` 仍命中逐语句 fallback，且 `result = true` 后换行起始的 `[` 会被 nightly expr 子集误判为 unsupported continuation。
- 修复内容：
  - 共享 helper 改为 `parse_list_exprs_latest_draft(...)`，明确处于“共享但在改”的 `_latest_draft` 状态；
  - `NewParserExpr.cpp` 新增 `parse_list_expr_or_iterator_nightly_draft(...)`，直接接住 list / infinite collection atom 与后续 iterator tail；
  - nightly expr subset token/start gate 纳入 `[` / `]` / `,` / `...`；
  - 新增“换行后的 `[` 视为下一条 statement 起点”规则，避免上一条赋值被吃半截；
  - 为保持 list-start 暴露后的链路稳定，nightly postfix 同步接管 `InfiniteAST` 上的 `?(...) >> ...` / `=> ...` loop 形式。
- 回归结果：
  - `ctest --test-dir build -R 'StyioParserEngine\\.(ShadowArtifactDetailShowsZeroFallbackForMixedRouteProgram|ShadowArtifactDetailShowsZeroFallbackForListIteratorSubset|ShadowArtifactDetailShowsZeroFallbackAcrossListBoundaryAfterBind|LegacyAndNightlyMatchOnM7ZipCollectionsSample|LegacyAndNightlyMatchOnM7ZipUnequalSample)|StyioSecurityNightlyParserStmt\\.MatchesLegacyOnIteratorStmtSubsetSamples' --output-on-failure` 全绿（6/6）；
  - `ctest --test-dir build -L styio_pipeline --output-on-failure` 全绿（55/55）；
  - `ctest --test-dir build -L security --output-on-failure` 全绿（121/121）；
  - `scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 ...` 全绿（20/20 artifacts match），且 `legacy_fallback_statements=[1-9]` 搜索为空。
- ADR 落地：`docs/adr/ADR-0095-nightly-list-start-iterator-and-statement-boundary.md`。

### 下一步

1. E.7：继续收缩 nightly parser 对内部 legacy helper 的复用面，优先识别还能单独切出的最小 postfix / body grammar。
2. E.7：在 route stats 已清零的前提下，开始评估 `legacy` 主路径的移除门槛和回退保留窗口。
3. F.3：把 M7 shadow gate 继续作为 parser 收敛的主监视窗口，避免后续切片把零 fallback 重新打破。

### 风险

1. `[ ... ] >> ...` 的入口已收口，但 zip/iterator 深层语义仍复用部分稳定 legacy helper，不代表内部实现已经完全切离。
2. `parse_list_exprs_latest_draft(...)` 仍处于 `_draft` 状态，后续若继续改 list grammar，需要重新跑 M7 shadow gate 防止回退比例反弹。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'StyioParserEngine\.(ShadowArtifactDetailShowsZeroFallbackForMixedRouteProgram|ShadowArtifactDetailShowsZeroFallbackForListIteratorSubset|ShadowArtifactDetailShowsZeroFallbackAcrossListBoundaryAfterBind|LegacyAndNightlyMatchOnM7ZipCollectionsSample|LegacyAndNightlyMatchOnM7ZipUnequalSample)|StyioSecurityNightlyParserStmt\.MatchesLegacyOnIteratorStmtSubsetSamples' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
tmp_dir=$(mktemp -d)
./scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m7 "$tmp_dir"
find "$tmp_dir" -name '*.jsonl' -maxdepth 2 -print0 | xargs -0 rg -n 'legacy_fallback_statements=[1-9]'
```

## Checkpoint（D.5/E.7：M7 shadow zero-fallback gate 纳入 CTest 与恢复脚本）

### 当前状态

- 目标：
  - 把已经人工验证达成的 `tests/milestones/m7` 零 fallback 状态转成自动化硬门槛。
- 交付内容：
  - `scripts/parser-shadow-suite-gate.sh` 新增 `--require-zero-fallback`；
  - summary 新增 `nonzero_fallback_records` 字段；
  - 新增 CTest `parser_shadow_gate_m7_zero_fallback`（标签：`styio_pipeline;shadow_gate;m7`）；
  - `scripts/checkpoint-health.sh` 默认纳入该 gate。
- 回归结果：
  - `ctest --test-dir build -R '^parser_shadow_gate_m7_zero_fallback$' --output-on-failure` 全绿（1/1）；
  - `./scripts/checkpoint-health.sh --no-asan` 全绿，且包含 M7 zero-fallback gate；
  - gate 输出稳定包含 `nonzero_fallback=0`。
- ADR 落地：`docs/adr/ADR-0096-m7-shadow-zero-fallback-gate.md`。

### 下一步

1. E.7：继续收缩 nightly parser 内部对 legacy helper 的复用面，优先处理 iterator / cases / forward body。
2. D.5：视后续进展，评估是否需要对 M5 等其它目录也追加 zero-fallback gate。
3. F.3：将 zero-fallback gate 作为移除 `legacy` 主路径前的稳定窗口观测项之一。

### 风险

1. 当前 zero-fallback gate 只覆盖 `m7` 套件，不代表其它目录已达到同样门槛。
2. gate 约束的是 shadow artifact 统计，不等于 nightly 内部 helper 已完全摆脱 legacy 复用。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R '^parser_shadow_gate_m7_zero_fallback$' --output-on-failure
./scripts/checkpoint-health.sh --no-asan
```

## Checkpoint（E.7：nightly internal legacy bridge 指标落地，并收缩 iterator/match-cases/infinite-loop body）

### 当前状态

- 目标：
  - 把 nightly parser “顶层零 fallback，但内部仍借 legacy body helper”的情况量化出来，并先收缩一批高频 body 路径。
- 交付内容：
  - `StyioParserRouteStats` 新增 `nightly_internal_legacy_bridges`；
  - `StyioContext` 新增 route stats 挂载与 `note_nightly_internal_legacy_bridge_latest()`；
  - shadow artifact `detail` 追加 `nightly_internal_legacy_bridges=<N>`；
  - 新增 nightly-first helper：
    - `parse_stmt_subset_with_legacy_fallback_latest_draft(...)`
    - `parse_block_only_subset_with_legacy_fallback_latest_draft(...)`
    - `parse_cases_only_nightly_draft(...)`
    - `parse_forward_as_list_nightly_draft(...)`
    - `parse_iterator_only_nightly_draft(...)`
  - `parse_infinite_loop_body_nightly_draft(...)` 改为 nightly-first body 解析。
- 先失败后修复（TDD 书签）：
  - 新增 `StyioSecurityNightlyParserStmt.MatchesLegacyOnInfiniteLoopSubsetSamples`；
  - 新增 `StyioParserEngine.ShadowArtifactDetailTracksZeroInternalLegacyBridgesForMatchCasesSubset`；
  - 扩展 `ShadowArtifactDetailShowsZeroFallbackForMixedRouteProgram` 与 `ShadowArtifactDetailShowsZeroFallbackForListIteratorSubset`，冻结 `nightly_internal_legacy_bridges=0`。
- 回归结果：
  - targeted parser/security：
    - `ctest --test-dir build -R 'StyioSecurityNightlyParserStmt\\.(MatchesLegacyOnIteratorStmtSubsetSamples|MatchesLegacyOnInfiniteLoopSubsetSamples|MatchesLegacyOnMatchCasesSubsetSamples)|StyioParserEngine\\.(ShadowArtifactDetailShowsZeroFallbackForMixedRouteProgram|ShadowArtifactDetailShowsZeroFallbackForListIteratorSubset|ShadowArtifactDetailTracksZeroInternalLegacyBridgesForMatchCasesSubset)' --output-on-failure` 全绿（6/6）；
  - `ctest --test-dir build -L styio_pipeline --output-on-failure` 全绿（57/57）；
  - `ctest --test-dir build -L security --output-on-failure` 全绿（122/122）。
- ADR 落地：`docs/adr/ADR-0097-nightly-internal-legacy-bridge-metric.md`。

### 下一步

1. E.7：继续沿 `nightly_internal_legacy_bridges` 指标收缩其余共享 helper，优先 `@file... >> ...` 和更深层的 iterator/forward 组合体。
2. E.7：当顶层 fallback 与内部 bridge 都长期归零后，再评估 `legacy` 主路径的移除窗口。
3. D.5：视需要把 internal bridge 指标加入后续健康检查摘要，方便 checkpoint 冷启动时快速判断 parser 收敛程度。

### 风险

1. `nightly_internal_legacy_bridges=0` 目前只在已覆盖样例上冻结，不代表所有 nightly 子域都已清零。
2. `parse_iterator_only_nightly_draft(...)` 仍只收了一段 body 路径；更深层共享 grammar 还没有完全切离。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'StyioSecurityNightlyParserStmt\.(MatchesLegacyOnIteratorStmtSubsetSamples|MatchesLegacyOnInfiniteLoopSubsetSamples|MatchesLegacyOnMatchCasesSubsetSamples)|StyioParserEngine\.(ShadowArtifactDetailShowsZeroFallbackForMixedRouteProgram|ShadowArtifactDetailShowsZeroFallbackForListIteratorSubset|ShadowArtifactDetailTracksZeroInternalLegacyBridgesForMatchCasesSubset)' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
ctest --test-dir build -L security --output-on-failure
```

## Checkpoint（E.7/D.5：M7 shadow zero-internal-bridges gate 纳入 CTest 与恢复脚本）

### 当前状态

- 目标：
  - 把 `nightly_internal_legacy_bridges=0` 从“可观测指标”升级为 `m7` 套件的自动化硬门槛。
- 先失败后修复（TDD 书签）：
  - 扩展 `StyioParserEngine.ShadowArtifactDetailShowsZeroFallbackForSnapshotDeclSubset`，冻结 `nightly_internal_legacy_bridges=0`；
  - 红灯阶段可稳定复现 `t03_snapshot` / `t06_snapshot_lock` / `t09_snapshot_accum` 的 internal bridge；
  - 根因收敛为 nightly expr 子集缺少 plain state ref `$name`，导致 iterator body 中的 `$ref` / `$factor` 触发内部 legacy bridge。
- 修复内容：
  - `NewParserExpr.cpp` 新增 `parse_state_ref_nightly_draft(...)`；
  - nightly expr subset token/start gate 新增 `TOK_DOLLAR`；
  - `scripts/parser-shadow-suite-gate.sh` 新增 `--require-zero-internal-bridges` 与 `nonzero_internal_bridge_records`；
  - `tests/CMakeLists.txt` 新增 `parser_shadow_gate_m7_zero_internal_bridges`；
  - `scripts/checkpoint-health.sh` 默认纳入该 gate。
- 回归结果：
  - `ctest --test-dir build -R 'StyioParserEngine\\.(ShadowArtifactDetailShowsZeroFallbackForSnapshotDeclSubset|ShadowArtifactDetailShowsZeroFallbackAcrossListBoundaryAfterBind)|StyioSecurityNightlyParserStmt\\.(MatchesLegacyOnIteratorStmtSubsetSamples|MatchesLegacyOnSnapshotDeclSubsetSamples)' --output-on-failure` 全绿（4/4）；
  - `ctest --test-dir build -R '^parser_shadow_gate_m7_zero_(fallback|internal_bridges)$' --output-on-failure` 全绿（2/2）；
  - `./scripts/checkpoint-health.sh --no-asan` 全绿；
  - `tests/milestones/m7` 整组扫描下，`legacy_fallback_statements=[1-9]` 与 `nightly_internal_legacy_bridges=[1-9]` 搜索均为空。
- ADR 落地：`docs/adr/ADR-0098-m7-shadow-zero-internal-bridges-gate.md`。

### 下一步

1. E.7：把 `zero-fallback + zero-internal-bridges` 作为移除 `legacy` 主路径前的最低门槛，而不再只看顶层 fallback。
2. D.5：评估是否把同类双门槛扩展到 `m5` 等仍在收缩中的目录。
3. F.3：继续补齐 nightly 子集对剩余高频 expr/stmt atom 的覆盖，减少未来 bridge 回潮。

### 风险

1. 当前双门槛仍只覆盖 `m7` 套件，不代表 `m1/m2/m5` 已达到相同收敛度。
2. 这次只补齐 plain `$name` state ref；更复杂的 state/history 变体若进入 nightly expr 子集，仍需要单独切片验证。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test styio_security_test -j8
ctest --test-dir build -R 'StyioParserEngine\.(ShadowArtifactDetailShowsZeroFallbackForSnapshotDeclSubset|ShadowArtifactDetailShowsZeroFallbackAcrossListBoundaryAfterBind)|StyioSecurityNightlyParserStmt\.(MatchesLegacyOnIteratorStmtSubsetSamples|MatchesLegacyOnSnapshotDeclSubsetSamples)' --output-on-failure
ctest --test-dir build -R '^parser_shadow_gate_m7_zero_(fallback|internal_bridges)$' --output-on-failure
./scripts/checkpoint-health.sh --no-asan
```

## Checkpoint（D.5/E.7：M1/M2 shadow dual-zero gates 纳入恢复脚本）

### 当前状态

- 目标：
  - 把 `m1/m2` 已经实测达成的 `zero-fallback + zero-internal-bridges` 状态升级成恢复脚本硬门槛。
- 交付内容：
  - `tests/CMakeLists.txt` 新增：
    - `parser_shadow_gate_m1_zero_fallback_and_internal_bridges`
    - `parser_shadow_gate_m2_zero_fallback_and_internal_bridges`
  - 两个 gate 都执行 `scripts/parser-shadow-suite-gate.sh --require-zero-fallback --require-zero-internal-bridges`；
  - `scripts/checkpoint-health.sh` 默认纳入 `m1/m2` 双零 gate。
- 回归结果：
  - `./scripts/parser-shadow-suite-gate.sh --require-zero-fallback --require-zero-internal-bridges ./build/bin/styio ./tests/milestones/m1 ...` 全绿（`match=40`, `nonzero_fallback=0`, `nonzero_internal_bridges=0`）；
  - `./scripts/parser-shadow-suite-gate.sh --require-zero-fallback --require-zero-internal-bridges ./build/bin/styio ./tests/milestones/m2 ...` 全绿（`match=20`, `nonzero_fallback=0`, `nonzero_internal_bridges=0`）；
  - `ctest --test-dir build -R '^parser_shadow_gate_m(1|2)_zero_fallback_and_internal_bridges$' --output-on-failure` 全绿（2/2）。
- ADR 落地：`docs/adr/ADR-0099-m1-m2-shadow-dual-zero-gates.md`。

### 下一步

1. E.7：继续评估 `m5` 的 suite gate 兼容方式，优先解决 fail-fast 样例与 shadow gate 的冲突。
2. D.5：保持 `checkpoint-health` 的 shadow 双零窗口与 parser 实际收敛状态一致，不接受只在人工命令下成立的“口头全绿”。
3. F.3：把 `m1/m2/m7` 三组 gate 当成 `legacy` 退场前的基础健康面板。

### 风险

1. `m1/m2` 虽然已进入双零 gate，但它们覆盖的是基础语法域，不能替代 `m5/m7` 的资源流与流处理边界。
2. `m5` 目前仍未进入同级别 gate，原因不是 parser 不稳定，而是 suite 内包含 fail-fast 样例，需要先处理 gate 兼容策略。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^parser_shadow_gate_m(1|2)_zero_fallback_and_internal_bridges$' --output-on-failure
./scripts/checkpoint-health.sh --no-asan
```

## Checkpoint（D.5/E.7：M5 shadow dual-zero gate 接入 expected-nonzero manifest）

### 当前状态

- 目标：
  - 让 `m5` 资源流目录进入 shadow gate，而不把 `t06_fail_fast` 这种“预期非零退出”样例误判为 gate 失败。
- 先失败后修复（TDD 书签）：
  - 旧版 `scripts/parser-shadow-suite-gate.sh ./build/bin/styio ./tests/milestones/m5 ...` 会因 `t06_fail_fast` 的非零退出直接失败；
  - 但该样例的 legacy/nightly shadow artifact 实际为 `status=match`，且 route stats 已是 `legacy_fallback_statements=0`、`nightly_internal_legacy_bridges=0`。
- 修复内容：
  - `scripts/parser-shadow-suite-gate.sh` 支持 suite 目录下的 `shadow-expected-nonzero.txt`；
  - manifest 中声明的样例允许非零退出，但仍必须产出 `match` artifact；
  - manifest 样例若意外返回 `0`，gate 反而失败；
  - `tests/milestones/m5/shadow-expected-nonzero.txt` 首批登记 `t06_fail_fast`；
  - `tests/CMakeLists.txt` 新增 `parser_shadow_gate_m5_dual_zero_expected_nonzero`；
  - `scripts/checkpoint-health.sh` 默认纳入该 gate。
- 回归结果：
  - `./scripts/parser-shadow-suite-gate.sh --require-zero-fallback --require-zero-internal-bridges ./build/bin/styio ./tests/milestones/m5 ...` 全绿；
  - 输出稳定包含：
    - `expected_nonzero_cases=1`
    - `allowed_nonzero_runs=2`
    - `expected_nonzero_mismatches=0`
    - `nonzero_fallback=0`
    - `nonzero_internal_bridges=0`
  - `ctest --test-dir build -R '^parser_shadow_gate_m5_dual_zero_expected_nonzero$' --output-on-failure` 全绿（1/1）。
- ADR 落地：`docs/adr/ADR-0100-m5-shadow-gate-expected-nonzero-manifest.md`。

### 下一步

1. E.7：在 `m1/m2/m5/m7` 都有 shadow gate 后，继续评估 `legacy` 主路径的移除窗口和回退保留策略。
2. D.5：如果后续再出现 fail-fast 套件，只允许通过 manifest 显式声明，不接受隐式放行。
3. F.3：继续把 parser 风险收敛从“样例绿”推进到“整目录 gate + 恢复脚本绿”。

### 风险

1. manifest 机制必须保持最小化，只能登记确属稳定契约的 expected-failure 样例。
2. `m5` 进入 gate 解决的是 suite 兼容问题，不代表资源流 grammar 已经完全没有后续收缩空间。

### 复现命令

```bash
cmake -S . -B build
cmake --build build --target styio_test -j8
tmp_dir=$(mktemp -d)
./scripts/parser-shadow-suite-gate.sh --require-zero-fallback --require-zero-internal-bridges ./build/bin/styio ./tests/milestones/m5 "$tmp_dir"
rm -rf "$tmp_dir"
ctest --test-dir build -R '^parser_shadow_gate_m5_dual_zero_expected_nonzero$' --output-on-failure
./scripts/checkpoint-health.sh --no-asan
```

## Checkpoint（B.4/E.7/F.3：Verification harness 对齐 nightly parser）

### 当前状态

- 目标：
  - 消除“CLI 已默认 nightly，但 fuzz / five-layer pipeline 仍直接走 legacy”的验证盲区。
- 先失败后修复（TDD 书签）：
  - `tests/fuzz/fuzz_parser.cpp` 旧实现只调用 `parse_main_block_legacy(...)`；
  - `src/StyioTesting/PipelineCheck.cpp` 的 L2 parser 旧实现也只调用 `parse_main_block_legacy(...)`。
- 修复内容：
  - `PipelineCheck.cpp` 新增 `pipeline_parser_engine_latest()`，并将 L2 parser 改为 `parse_main_block_with_engine_latest(..., StyioParserEngine::Nightly, ...)`；
  - `fuzz_parser.cpp` 新增 `fuzz_parse_with_engine_latest(...)`，对同一输入顺序驱动 `legacy` 与 `nightly` 两条 parser 路由；
  - five-layer pipeline 新增：
    - `tests/pipeline_cases/p02_simple_func/`：函数定义 + 参数 + 调用
    - `tests/pipeline_cases/p03_write_file/`：资源写入 + `/tmp/styio_pipeline_out.txt` 副作用文件断言
  - fuzz target 名称保持不变：`styio_fuzz_parser` / `fuzz_parser_smoke`；
  - `tests/fuzz/README.md`、`docs/assets/workflow/FIVE-LAYER-PIPELINE.md`、`docs/assets/workflow/TEST-CATALOG.md` 同步到 nightly-first 验证语义。
- 回归结果：
  - `ctest --test-dir build -R '^StyioFiveLayerPipeline\\.P01_print_add$|^StyioParserEngine\\.(LegacyAndNightlyMatchOnM1Sample|LegacyAndNightlyMatchOnM7SnapshotSample|DefaultEngineIsNightlyInShadowArtifact)$' --output-on-failure` 全绿（4/4）；
  - `ctest --test-dir build -R '^StyioFiveLayerPipeline\\.(P02_simple_func|P03_write_file)$' --output-on-failure` 全绿（2/2）；
  - `ctest --test-dir build -L styio_pipeline --output-on-failure` 全绿（62/62）；
  - `ctest --test-dir build-fuzz -R '^fuzz_parser_smoke$' --output-on-failure` 全绿（1/1）。
- ADR 落地：`docs/adr/ADR-0101-nightly-verification-harness-alignment.md`。

### 下一步

1. F.3：继续把剩余“仅在测试基础设施里直连 legacy”的入口收口，避免验证路径与默认行为漂移。
2. B.5：在 nightly fuzz case pack 回流时，优先观察 nightly-only 崩溃而不是被 legacy-only target 掩盖。
3. E.7：在 verification harness 已对齐 nightly 后，再评估 `legacy` 主路径对生产/测试双侧的真实剩余暴露面。

### 风险

1. five-layer pipeline 目前仍只有 `p01_print_add` 一个 case，虽然 parser 已切到 nightly，但 coverage 仍偏窄。
2. dual-engine fuzz 提高了 nightly 暴露面，但尚未把两引擎成功解析后的 AST 差异升级为 fuzz 失败条件。

### 复现命令

```bash
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioFiveLayerPipeline\.P01_print_add$|^StyioParserEngine\.(LegacyAndNightlyMatchOnM1Sample|LegacyAndNightlyMatchOnM7SnapshotSample|DefaultEngineIsNightlyInShadowArtifact)$' --output-on-failure

cmake --build build-fuzz --target styio_fuzz_parser -j8
ctest --test-dir build-fuzz -R '^fuzz_parser_smoke$' --output-on-failure
```

## Checkpoint（D.5/B.5：Checkpoint Health 接入可选 fuzz smoke）

### 当前状态

- 目标：
  - 让恢复脚本在存在 `build-fuzz` 时自动多过一层 `fuzz_smoke`，但不强制所有环境都配置 fuzz 工具链。
- 修复内容：
  - `scripts/checkpoint-health.sh` 新增：
    - `--fuzz-build-dir <dir>`
    - `--no-fuzz`
  - 默认改为 auto-detect：
    - 若 `build-fuzz` 下存在 CTest 元数据，则自动构建 `styio_fuzz_lexer` / `styio_fuzz_parser` 并执行 `ctest -L fuzz_smoke`
    - 若不存在，则打印 skip 信息而非失败
- 回归结果：
  - `./scripts/checkpoint-health.sh --no-asan` 在本地 `build-fuzz` 存在时自动跑通：
    - `fuzz_lexer_smoke`
    - `fuzz_parser_smoke`
    - `fuzz_regression_pack_smoke`
  - 全流程保持全绿（deep soak + `styio_pipeline` + `security` + shadow gates + `fuzz_smoke`）。
- ADR 落地：`docs/adr/ADR-0102-checkpoint-health-optional-fuzz-smoke.md`。

### 下一步

1. B.5：把 fuzz smoke 视为 checkpoint 健康面板的一部分，后续 fuzz target 调整必须同步恢复脚本。
2. D.5：若未来增加更多专用构建目录，继续沿“auto-detect + 显式强要求”的模式扩展，不做硬编码强依赖。
3. F.3：继续减少“主行为已变更，但恢复脚本没跟上”的验证漂移。

### 风险

1. auto-detect 只保证“有 fuzz build 就会跑”，不替代 nightly 深跑 fuzz 工作流。
2. fuzz 构建仍依赖 clang/libFuzzer 工具链；无该环境时只能 skip，不能假装已覆盖。

### 复现命令

```bash
./scripts/checkpoint-health.sh --no-asan
# 或显式指定 fuzz 构建目录
./scripts/checkpoint-health.sh --no-asan --fuzz-build-dir build-fuzz
# 或跳过 fuzz
./scripts/checkpoint-health.sh --no-asan --no-fuzz
```

## Checkpoint（F.3/E.7：Five-Layer Pipeline 增加 Snapshot/State Golden）

### 当前状态

- 目标：
  - 把 `nightly` parser 在 `snapshot decl + state ref + iterator block + compound accumulation` 这条链路正式纳入 five-layer golden。
- 先失败后修复（TDD 书签）：
  - five-layer pipeline 之前只有 `p01`–`p04`，仍缺一个能同时观察 `@[...]`、`$state` 和 iterator block lowering 的 case。
- 修复内容：
  - 新增 `tests/pipeline_cases/p05_snapshot_accum/`：
    - `input.styio`
    - `expected/tokens.txt`
    - `expected/ast.txt`
    - `expected/styio_ir.txt`
    - `expected/llvm_ir.txt`
    - `expected/stdout.txt`
    - `expected/factor_file.txt`
  - `tests/styio_test.cpp` 新增 `StyioFiveLayerPipeline.P05_snapshot_accum`：
    - 运行前准备 `/tmp/styio_pipeline_factor.txt`
    - 运行后清理临时输入文件
  - `docs/assets/workflow/FIVE-LAYER-PIPELINE.md`、`docs/assets/workflow/TEST-CATALOG.md`、`docs/history/README.md` 同步更新。
- 回归结果：
  - `ctest --test-dir build -R '^StyioFiveLayerPipeline\\.P05_snapshot_accum$' --output-on-failure` 全绿（1/1）；
  - `ctest --test-dir build -L styio_pipeline --output-on-failure` 全绿（65/65）。
- ADR 落地：`docs/adr/ADR-0103-five-layer-snapshot-state-pipeline-case.md`。

### 下一步

1. F.3：继续给 five-layer pipeline 增加更接近 `m7` 实战的资源流 / snapshot 组合 case，而不是只看 shadow gate。
2. E.7：在 nightly 行为已有 five-layer 约束后，再继续压缩 `legacy` 的显式入口和对外暴露面。
3. D.5：保持 `/tmp` 依赖显式化，后续凡是 case 需要外部输入文件，都走“测试准备 + pipeline case 固定路径”的模式。

### 风险

1. `llvm_ir.txt` 仍与当前目标平台绑定，换 LLVM/目标平台时需要按 five-layer 规则更新 golden。
2. 该 case 锁住了 `snapshot/state-ref` 的当前 lowering 形状；如果后续做合法的 IR 重写，需要同步更新全套 golden，而不能只改 stdout。

### 复现命令

```bash
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioFiveLayerPipeline\.P05_snapshot_accum$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
```

## Checkpoint（F.3/E.7：Parser Legacy Entry Audit Gate）

### 当前状态

- 目标：
  - 把“生产路径和 `src/StyioTesting` 不再直连 legacy parser”从口头规范升级为自动化硬门槛。
- 先失败后修复（TDD 书签）：
  - 现有 nightly-first 约束主要靠人工维护，若后续切片把 `PipelineCheck.cpp` 或主入口重新连回 `parse_main_block_legacy(...)`，恢复脚本不会第一时间报警。
- 修复内容：
  - 新增 `scripts/parser-legacy-entry-audit.sh`，冻结三条规则：
    - `src/` 中不允许出现 parser core 之外的 `parse_main_block_legacy(...)`
    - `src/StyioTesting/` 不允许出现 `StyioParserEngine::Legacy` 或 `parse_main_block_legacy(...)`
    - `src/main.cpp` 必须继续通过 `parse_main_block_with_engine_latest(...)` 进入 parser
  - `tests/CMakeLists.txt` 新增 CTest：`parser_legacy_entry_audit`
  - `scripts/checkpoint-health.sh` 默认纳入该 gate
  - `docs/assets/workflow/TEST-CATALOG.md`、`docs/history/README.md` 同步更新
- 回归结果：
  - `ctest --test-dir build -R '^parser_legacy_entry_audit$' --output-on-failure` 全绿（1/1）；
  - `./scripts/checkpoint-health.sh --no-asan --no-fuzz` 全绿。
- ADR 落地：`docs/adr/ADR-0104-parser-legacy-entry-audit-gate.md`。

### 下一步

1. E.7：继续缩小 `legacy` 的合法暴露面，优先从显式 CLI 回退和 parity harness 之外的剩余入口开始。
2. F.3：若后续还要新增 parser harness，默认先放进 nightly 路径，再由审计 gate 约束不回连 legacy。
3. D.5：恢复脚本继续沿“语义 gate + 结构 gate”组合扩展，而不是只看行为测试。

### 风险

1. 审计 gate 只约束“入口暴露面”，不替代语义 parity 或 shadow gate。
2. `legacy` 在 parity tests、shadow compare 和 dual-engine fuzz 中仍属合法使用；真正退场前，这些白名单仍要保留。

### 复现命令

```bash
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^parser_legacy_entry_audit$' --output-on-failure
./scripts/checkpoint-health.sh --no-asan --no-fuzz
```

## Checkpoint（F.3/E.7：Five-Layer Pipeline 增加 Zip/File Stream Golden）

### 当前状态

- 目标：
  - 把 `stream zip + file iterator + arithmetic print` 正式纳入 nightly-first five-layer golden。
- 先失败后修复（TDD 书签）：
  - five-layer pipeline 之前虽已覆盖 file read 和 snapshot/state，但还没有一条 case 同时约束 `stream.zip` AST、`styio.ir.stream_zip` 和 LLVM `zip_ff_*` lowering。
- 修复内容：
  - 新增 `tests/pipeline_cases/p06_zip_files/`：
    - `input.styio`
    - `expected/input_a.txt`
    - `expected/input_b.txt`
    - `expected/tokens.txt`
    - `expected/ast.txt`
    - `expected/styio_ir.txt`
    - `expected/llvm_ir.txt`
    - `expected/stdout.txt`
  - `tests/styio_test.cpp` 新增 `StyioFiveLayerPipeline.P06_zip_files`：
    - 运行前准备 `/tmp/styio_zip_a.txt` 和 `/tmp/styio_zip_b.txt`
    - 运行后清理两个临时输入文件
  - `StyioToken::getTokName(TOK_AMP)` 补齐稳定显示名 `&`，把 L1 token golden 中的 `<UNKNOWN>` 收口为可读符号名
  - `docs/assets/workflow/FIVE-LAYER-PIPELINE.md`、`docs/assets/workflow/TEST-CATALOG.md`、`docs/history/README.md` 同步更新。
- ADR 落地：`docs/adr/ADR-0105-five-layer-zip-file-pipeline-case.md`。

### 下一步

1. F.3：继续把 `M7` 的高价值 grammar 从 shadow/parity 推进到 five-layer，而不是只扩大 stdout golden。
2. E.7：若再补 one more case，优先考虑 `instant pull` 或 `at-resource` 路径。
3. D.5：保持所有 pipeline case 对外部输入文件都走“测试准备 `/tmp` 文件”的模式，避免工作目录敏感。

### 风险

1. 该 case 把当前 `zip_ff_*` LLVM lowering 直接冻结为 golden；后续如果 zip lowering 重写，需要同步更新整套 five-layer 产物。
2. token golden 里的 `&` 当前仍显示为 `<UNKNOWN>`，这是现有 tokenizer 显示名缺口，不是本次切片内修复目标。

### 复现命令

```bash
cmake --build build --target styio_test -j8
ctest --test-dir build -R '^StyioFiveLayerPipeline\.P06_zip_files$' --output-on-failure
ctest --test-dir build -L styio_pipeline --output-on-failure
```
