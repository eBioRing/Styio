# Development Record - 2026-07-31

**Purpose:** Record the decision provenance and independently accepted delivery checkpoints for the approved Q1–Q9 callable-type evolution batch.

**Last updated:** 2026-07-31

## Owner Decision

The language owner approved the recommended Q1–Q9 batch in
[`../design/syntax/CALLABLE-TYPE-EVOLUTION-QUESTIONS-2026-07-31.md`](../design/syntax/CALLABLE-TYPE-EVOLUTION-QUESTIONS-2026-07-31.md).
Each branch remains owned by its separate syntax-feature SSOT; this dated note
does not replace those authorities.

## Delivered Checkpoints

1. Q1 converged as allocation-free monomorphic callable values backed by final named function items and one complete invariant callable type.
2. Q4 converged as canonical closed/open callable effect rows in Sema, StyioIR, and the then-current schema-v3 interfaces; Q7 subsequently carried those facts into schema v4.
3. Q5 converged as compiler-owned canonical callable usage requirements with concrete-instance validation.
4. Q2 converged as exact explicit `$(...)` capture lists and an affine program-static scalar environment. Shared captures may escape under one concrete callable type, exclusive captures remain direct-call-only, and consume, imported, resource/container, or missing representation/drop environments fail before lowering.
5. Q7 converged as the schema-v4 split between a stable public callable contract and required canonical `styio.portable-styioir` schema-v1 bodies. The loader validates flat topological nodes, symbols, recomputed types, canonical bytes, semantic/dependency/compiler digests, and reconstructs imported definitions without parsing imported source or retaining a schema-v3 AST-text fallback.
6. Q8 converged as an opt-in local persistent cache of verified native specialization objects. Codegen partitions each full-digest specialization before ORC compilation, the cache revalidates current semantic/backend identity plus object structure and symbol ownership, concurrent writes are atomic, corruption recompiles, and deterministic age/byte/file ceilings plus path-free timing statistics are executable CLI contracts.

## Deferred Boundaries

Q3 rank-2 callback polymorphism and Q6 user-extensible callable constraints
remain deferred by owner decision even though Q3's prerequisite delivery floor
has converged. Their child SSOTs own the reopen conditions. The research-backed
follow-up queue now separates Q3.1–Q3.3 (contract origin, static scheme-witness
representation, and shallow pure subsumption) from Q6.1–Q6.3 (concrete nominal
heads, constraint-or-head ownership, and named static instance evidence).
Those questions are unanswered and authorize no implementation.

## Follow-up Gate

Q1, Q2, Q4, Q5, Q7, Q8, and Q9 are converged. The research-backed question
collection for the deliberately deferred Q3 and Q6 boundaries is refreshed
from current primary sources and linked from both child SSOTs. Batch-wide
functional regression and local-branch integration are complete. Every
delivered feature checkpoint updated only its child SSOT, compact shared views,
owning runbooks, generated feature graph, and focused evidence. Formal
promotion remains blocked only by the canonical global line-coverage gate
recorded below; the threshold has not been weakened or waived.

## Resolved Objective Verification Blocker

The earlier Q2, Q7, Q8, and deferred Q3/Q6 staged delivery gates reached
`python3 scripts/ecosystem-cli-doc-gate.py` and stopped on the retired
Spio-era Pafio/Vityo mirror contract. The integrated Pafio convergence branch
replaced that obsolete matrix with the versioned Styio compiler, Pafio
project-workflow, and Vityo consumer contracts. The same command now validates
all eight owner/consumer documents successfully, so the external blocker is
closed.

The focused evidence gathered while that blocker was active remains useful:
Q2 passed the affine/callable effect and capability suites plus full/nano
execution; Q7 passed callable-interface and portable-body replay/schema
coverage; Q8 passed cold/warm native reuse, transitive invalidation, namespace
isolation, corruption recovery, concurrent atomic writers, bounded retention,
and full/nano execution; and the Q3/Q6 queue records primary-source
implementation and failure evidence. Shared process evidence includes
syntax-feature state, runtime-surface, team-docs, docs-audit, docs-lifecycle,
local-info-leak, workflow-scheduler, tool-skill-registry, and
repository-hygiene gates.

## Batch-Wide Regression

The final coverage build registered 1,237 CTest entries. Every runnable entry
passed, with only declared platform skips remaining. The default checkpoint
pipeline also passed its 351 runnable entries after the health runner was fixed
to build the platform-internal test that it already selected; that focused
target then passed independently. IDE/LSP passed 2/2, all six parser gates
passed, and the security suite's one stale `ExtPackAST` expectation was aligned
with the already-approved callable-module import contract before the complete
coverage run. Documentation indexing, lifecycle, ownership, audit, ecosystem
CLI, workflow, local-information, registry, and repository-hygiene checks also
passed.

## Objective Coverage Blocker

The canonical 95% global line-coverage gate remains unsatisfied:
55,516/65,040 instrumented lines are covered, or 85.36%. This is not a
functional-test failure. A file-cohort comparison against the pre-batch
`nightly` base shows that source files untouched by this batch independently
cover only 16,480/19,607 lines, or 84.05%, so the shortfall is repository-wide
existing debt rather than a regression that can be closed within one callable
feature batch. No threshold, exclusion, or test result was changed to conceal
the blocker.
