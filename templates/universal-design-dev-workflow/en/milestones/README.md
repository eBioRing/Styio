# Milestone documentation index

**Purpose:** Index dated `milestones/<YYYY-MM-DD>/` directories. Per-batch tables live in each `00-Milestone-Index.md`. Rules: [`../DOCUMENTATION-POLICY.md`](../DOCUMENTATION-POLICY.md).

**Last updated:** (set when forking)

| Date directory | Summary |
|----------------|---------|
| `<YYYY-MM-DD>/` | (one line) |

To add a batch: create `milestones/<YYYY-MM-DD>/`, add `00-Milestone-Index.md` and segment files from `_template/`, then add a row here.
