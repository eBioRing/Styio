# Pipeline Failure Verdicts

This record classifies the 23 failures reproduced by the 2026-08-19 `styio_pipeline` run before repair. An expectation is changed only where current behavior is confirmed by the compiler contract; all executable lifetime and verifier failures are implementation regressions.

## Confirmed stale expectations

- [x] `StyioFiveLayerPipeline.P02_simple_func` — **stale-expectation**. Root cause: the LLVM golden predates the required runtime-error probe before a function return. Evidence: initial Layer 4 diff, emitted LLVM IR, and the runtime error propagation contract.
- [x] `StyioFiveLayerPipeline.P05_snapshot_accum` — **stale-expectation**. Root cause: the typed AST golden retained `undefined` for a file instant-pull binding now correctly inferred as `i64`. Evidence: original-versus-type-checked `--styio-ast` output and the instant-pull type contract.
- [x] `StyioFiveLayerPipeline.P11_pipe_func` — **stale-expectation**. Root cause: the LLVM golden predates the required runtime-error probe before the piped function return. Evidence: initial Layer 4 diff, emitted LLVM IR, and the runtime error propagation contract.
- [x] `StyioFiveLayerPipeline.P14_stdin_pull` — **stale-expectation**. Root cause: the typed AST golden retained `undefined` for the default numeric stdin pull now correctly inferred as `i64`. Evidence: original-versus-type-checked `--styio-ast` output and the stdin pull type contract.
- [x] `StyioParserEngine.PointerScrutineeMatchDoesNotAbortAndReportsTypeError` — **stale-expectation**. Root cause: the assertion expected the retired wording `integer-typed`; the authoritative diagnostic accepts integer or char scrutinees. Evidence: JSONL diagnostics from both parser engines and the semantic diagnostic `match scrutinee must have integer or char type`.

## Confirmed implementation regressions

- [x] `StyioDiagnostics.ResourceMethodRangeBodyRunsAfterInlineClone` — **implementation-regression**. Root cause: `SGBlock` registered an escaped list clone in the inner scope before popping that scope, immediately releasing the value returned by the inlined resource method. Evidence: initial exit-code/output failure, emitted ownership IR, and passing post-fix regression.
- [x] `StyioResourceEffects.ValueResourceMethodReturnedBlockFunctionMatrixResult` — **implementation-regression**. Root cause: cleanup tracked a branch-local matrix producer instead of the merged resource value, producing a non-dominating `styio_matrix_release`. Evidence: LLVM verifier diagnostic naming the matrix allocation/release and passing post-fix regression.
- [x] `StyioResourceEffects.ValueResourceMethodLocalContainerReturnsFromDirectAndFallbackCalls` — **implementation-regression**. Root cause: inlined block escapes were registered before inner-scope cleanup, and resource-effect alternatives retained branch-local list/dict/matrix owners past their branch. Evidence: LLVM verifier diagnostics for list/dict releases, runtime invalid-handle reproduction, and passing post-fix regression.
- [x] `StyioResourceEffects.ValueResourceMethodFallbackRecoversReturnedContainerBoundsFailure` — **implementation-regression**. Root cause: fallback list ownership remained attached to branch-local allocations instead of the continuation PHI. Evidence: LLVM verifier non-dominance diagnostics and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueResourceMethodFallbackRecoversReturnedMatrixBoundsFailure` — **implementation-regression**. Root cause: fallback matrix ownership remained attached to a branch-local allocation instead of the continuation PHI. Evidence: LLVM verifier non-dominance diagnostics and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueResourceMethodNamedBoundsHandlerRecoversReturnedMatrixFailure` — **implementation-regression**. Root cause: named-handler matrix ownership remained attached to a branch-local allocation instead of the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and focused default/ASan regression passes.
- [x] `StyioResourceEffects.ValueListSliceSuccessSkipsFallback` — **implementation-regression**. Root cause: successful list-slice ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueListSliceFallbackRecoversBoundsFailure` — **implementation-regression**. Root cause: fallback list ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueListSliceNamedBoundsHandlerRecoversBoundsFailure` — **implementation-regression**. Root cause: named-handler list ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and focused default/ASan regression passes.
- [x] `StyioResourceEffects.ValueDictSliceSuccessSkipsFallback` — **implementation-regression**. Root cause: successful dict-slice ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueDictSliceFallbackRecoversBoundsFailure` — **implementation-regression**. Root cause: fallback dict ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueDictSliceNamedBoundsHandlerRecoversBoundsFailure` — **implementation-regression**. Root cause: named-handler dict ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and focused default/ASan regression passes.
- [x] `StyioResourceEffects.ValueMatrixRowFallbackRecoversBoundsFailure` — **implementation-regression**. Root cause: fallback row-list ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueMatrixSliceSuccessSkipsFallback` — **implementation-regression**. Root cause: successful matrix-slice ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueMatrixSliceFallbackRecoversBoundsFailure` — **implementation-regression**. Root cause: fallback matrix ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueMatrixSliceNamedBoundsHandlerRecoversBoundsFailure` — **implementation-regression**. Root cause: named-handler matrix ownership was not transferred from its branch-local producer to the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and focused default/ASan regression passes.
- [x] `StyioResourceEffects.ValueTypedListReturnsStdinInstantPull` — **implementation-regression**. Root cause: the successful typed-list pull kept cleanup ownership on a branch-local list rather than the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and passing post-fix pipeline run.
- [x] `StyioResourceEffects.ValueTypedListFallbackRecoversStdinInstantPullParseFailure` — **implementation-regression**. Root cause: the fallback typed-list value kept cleanup ownership on a branch-local list rather than the continuation PHI. Evidence: LLVM verifier non-dominance diagnostic and passing post-fix pipeline run.
