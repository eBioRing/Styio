# std.io

**Purpose:** Reserve the standard-library IO module boundary above language-core resource identifiers and runtime primitives.

**Last updated:** 2026-06-25

Status: planned. Current standard-stream and file resource spellings remain compiler/language contracts.

Typed stdin helper routing is implemented as a compiler/runtime route for accepted stdin syntax. It is not a `std.io` API until this module becomes active with manifest source and tests.
