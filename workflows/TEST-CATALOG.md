# Test Catalog

**Purpose:** Define the feature-based Styio test inventory, CTest labels, fixture layout, and gate commands for language acceptance coverage.

**Last updated:** 2026-07-31

---

## 1. Layout Rules

All language acceptance fixtures live under `tests/features/<feature>/`.

Each feature directory owns:

1. `t*.styio` positive executable fixtures.
2. `e*.styio` semantic or diagnostic failure fixtures when needed.
3. `expected/*.out` stdout goldens for positive fixtures.
4. `expected/*.err` diagnostic fragments for negative fixtures.
5. `data/` only when fixtures need local resource input.

CTest names use `<feature>_<fixture-name>` for positive fixtures and `<feature>_err_<fixture-name>` for negative fixtures. All feature fixtures carry the `language_feature` label plus their feature label.

Run the full language acceptance surface:

```bash
ctest --test-dir build/default -L language_feature --output-on-failure
```

Run one feature:

```bash
ctest --test-dir build/default -L scalar_expressions --output-on-failure
```

---

## 2. Feature Suites

| Feature label | Directory | Coverage focus | Targeted command |
|---------------|-----------|----------------|------------------|
| `scalar_expressions` | `tests/features/scalar_expressions/` | integer/float arithmetic, precedence, strings, bindings, comparison, logic, stdout shorthand | `ctest --test-dir build/default -L scalar_expressions --output-on-failure` |
| `keyword_free` | `tests/features/keyword_free/` | every word token remains `NAME`, keyword-like spellings remain ordinary identifiers outside symbol-anchored contexts | `ctest --test-dir build/default -L keyword_free --output-on-failure --no-tests=error` |
| `inferred_generics` | `tests/features/inferred_generics/` | principal rank-1 callable relations, independent concrete instances, expected-result inference, self/mutual recursive SCCs, and fail-closed polymorphic-recursion/underconstraint/authored-generic diagnostics | `ctest --test-dir build/default -L inferred_generics --output-on-failure --no-tests=error` |
| `callable_effects` | `tests/features/callable_effects/` | closed-empty-row generalization, transitive canonical output rows, single effectful instances, and fail-closed second instances for output or captured environments | `ctest --test-dir build/default -L callable_effects --output-on-failure --no-tests=error` |
| `callable_constraints` | `tests/features/callable_constraints/` | numeric, comparable, and indexable inferred constraints; transitive propagation; scalar/string comparison lowering; stable unsatisfied-constraint diagnostics | `ctest --test-dir build/default -L callable_constraints --output-on-failure --no-tests=error` |
| `callable_values` | `tests/features/callable_values/` | direct named scheme instantiation; invariant `#(...): ...` freezing for final noncapturing named callables; bind, pass, return, and indirect invocation; fail-closed mutable, capturing, mismatched, context-free generalized, and callable-container values | `ctest --test-dir build/default -L callable_values --output-on-failure --no-tests=error` |
| `callable_capabilities` | `tests/features/callable_capabilities/` | scalar/list/dict scheme instances; local and transitive usage-fact execution; fact-specific copy/task/state/topology/shape rejection for matrix, task, stream, file, nested-handle collection, and topology-resource instances | `ctest --test-dir build/default -L callable_capabilities --output-on-failure --no-tests=error` |
| `affine_closures` | `tests/features/affine_closures/` | exact `$(...)` capture parsing; shared program-static scalar escape; exclusive direct-call mutation; `bool`/`f64`/`char` storage widths; same-name local/capture scope isolation; stable missing/unused/duplicate capture, exclusive escape, consume, and representation/drop diagnostics | `ctest --test-dir build/default -L affine_closures --output-on-failure --no-tests=error` |
| `callable_interfaces` | `tests/features/callable_interfaces/` | explicit schema-v3 `.styioi` publication, canonical closed/open effect-row and sorted usage-requirement round trips, transitive open-tail/usage propagation, downstream generic/constrained specialization, private helper retention, direct-export visibility, and fail-closed module-id/missing/v2-schema/source/dependency/cycle/uninstantiated-body checks | `ctest --test-dir build/default -L callable_interfaces --output-on-failure --no-tests=error` |
| `callable_specialization` | `tests/features/callable_specialization/` | lazy reachable mono-item collection, deterministic full-digest symbols across repeat/call-order changes, transitive callee-body invalidation, one local definition owner, unreachable-instance omission, and concrete recursion-path ceilings | `ctest --test-dir build/default -L callable_specialization --output-on-failure --no-tests=error` |
| `literal_defaulting` | `tests/features/literal_defaulting/` | canonical `i64`/`f64` literal relations, contextual empty list/dictionary acceptance, missing-context failures, and integer/floating relation-family conflicts | `ctest --test-dir build/default -L literal_defaulting --output-on-failure --no-tests=error` |
| `functions` | `tests/features/functions/` | function definitions, typed returns, block bodies, call chains, no-param functions, nested calls | `ctest --test-dir build/default -L functions --output-on-failure` |
| `control_flow` | `tests/features/control_flow/` | match expressions, defaults, loops, break, continue, factorial/fizzbuzz examples | `ctest --test-dir build/default -L control_flow --output-on-failure` |
| `wave_dispatch` | `tests/features/wave_dispatch/` | wave merge/dispatch plus rejected fallback/noop spelling evidence | `ctest --test-dir build/default -L wave_dispatch --output-on-failure` |
| `file_resources` | `tests/features/file_resources/` | `@file`, file reads/writes, RAII, redirect, file pipelines, fail-fast file cases | `ctest --test-dir build/default -L file_resources --output-on-failure` |
| `state_resources` | `tests/features/state_resources/` | Topology-style state resources, bounded `i64`/`f64`/`bool`/`char`/`string` selector slice/snapshot and explicit selector-copy runtime evidence, bounded list/dict/matrix selector-history and handle selector-copy smoke coverage, unsupported selector fail-closed checks, and retired state-family negative diagnostics | `ctest --test-dir build/default -L state_resources --output-on-failure` |
| `stream_processing` | `tests/features/stream_processing/` | zip over literals, files, materialized list handles including `char`, mixed file/list pairs, stdin/list and stdin/file unit smokes, bounded scalar/string/matrix selector snapshots, plus snapshots, instant pull, file-backed streams, singleton, arbitrage, stream output artifacts | `ctest --test-dir build/default -L stream_processing --output-on-failure` |
| `final_bindings` | `tests/features/final_bindings/` | bounded final binding, reads, and same-name flex rejection diagnostics | `ctest --test-dir build/default -L final_bindings --output-on-failure` |
| `stdio_output` | `tests/features/stdio_output/` | `@stdout`, `@stderr`, format strings, mixed stdout/stderr assertions, legacy print smoke | `ctest --test-dir build/default -L stdio_output --output-on-failure` |
| `stdio_input` | `tests/features/stdio_input/` | `@stdin`, typed pulls, EOF, mixed output, invalid stdio resource direction diagnostics | `ctest --test-dir build/default -L stdio_input --output-on-failure` |
| `native_interop` | `tests/features/native_interop/` | native C/C++ extern compilation, linking, raw body source, and JIT calls | `ctest --test-dir build/default -L native_interop --output-on-failure` |
| `task_resources` | `tests/features/task_resources/` | task flow, task pull, captured values, task groups, await-pipe task settlement, and invalid task_flow / non-task_await diagnostics | `ctest --test-dir build/default -L task_resources --output-on-failure` |

---

## 3. Special Gates

| Gate | Purpose | Command |
|------|---------|---------|
| `StyioDiagnostics.SyntaxCheckRejectsNonAuthoritativeParserEngine` | Prove the public syntax-check contract is locked to the authoritative nightly parser. | `ctest --test-dir build/default -R '^StyioDiagnostics\.SyntaxCheckRejectsNonAuthoritativeParserEngine$' --output-on-failure` |
| `StyioSemanticBridge.RejectsMalformedInputWithoutRecovery` | Prove IDE semantic facts are not recovered from malformed source after strict compiler parsing fails. | `ctest --test-dir build/default -R '^StyioSemanticBridge\.RejectsMalformedInputWithoutRecovery$' --output-on-failure` |
| `StyioIdeProject.EnvironmentFallbacksAndWorkspaceSkipsStayExplicit` | Prove IDE project roots use stable `root-<hex-path>` cache identities, skip generated workspace directories, and keep missing-root scans fail-closed through `workspace_scan_error_count()`. | `ctest --test-dir build/default -R '^StyioIdeProject\.EnvironmentFallbacksAndWorkspaceSkipsStayExplicit$' --output-on-failure` |
| `StyioSemanticDb.SemanticTokensUseUtf16PositionsAndLengths` | Prove semantic-token delta positions and token lengths use LSP UTF-16 units for emoji/CJK text instead of byte columns. | `ctest --test-dir build/default -R '^StyioSemanticDb\.SemanticTokensUseUtf16PositionsAndLengths$' --output-on-failure` |
| `StyioIdeService.WatchFileRefreshFiltersAndCoalescesChangedPaths` | Prove watched-file refreshes only enqueue closed `.styio` files inside the workspace, preserve deletion refreshes, and coalesce duplicate changed paths. | `ctest --test-dir build/default -R '^StyioIdeService\.WatchFileRefreshFiltersAndCoalescesChangedPaths$' --output-on-failure` |
| `StyioLspServer.WatchFileChangesFilterAndCoalesceBackgroundRefresh` | Prove `workspace/didChangeWatchedFiles` uses concrete changed URIs and ignores empty, duplicate, open-file, non-Styio, and out-of-workspace events. | `ctest --test-dir build/default -R '^StyioLspServer\.WatchFileChangesFilterAndCoalesceBackgroundRefresh$' --output-on-failure` |
| `StyioLspServer` conservative public-surface tests | Prove rename, inlay hints, and code actions stay fail-closed behind compiler-owned identity, freshness, and exact diagnostic evidence; protocol-envelope smokes keep initialize notification handling and stdio header parsing non-crashing. | `ctest --test-dir build/default -R '^StyioLspServer\.(InitializeNotificationUpdatesWorkspaceWithoutResponse\|CoversTransportReaderAndMalformedRequestEdges\|RenameIsCapabilityGatedAndUsesResolvedSymbolIdentity\|InlayHintUsesResolvedCallsiteParameterNamesAndFailsClosed\|InlayHintReturnsEmptyWhileBackgroundRefreshIsPending\|CodeActionAddsEditForUnterminated(BlockComment\|StringLiteral)\|CodeActionDeletesUnmatchedClosingToken\|CodeActionEmitsDisabledQuickFixForUnsupportedEditorSyntaxDiagnostic)$' --output-on-failure` |
| `StyioSyntaxDrift.CorpusMatchesApprovedEnvelope` | Keep editor syntax snapshots documented as non-authoritative when they intentionally differ from strict compiler parsing on malformed source. | `ctest --test-dir build/default -R '^StyioSyntaxDrift\.CorpusMatchesApprovedEnvelope$' --output-on-failure` |
| `parser_shadow_gate_scalar_expressions_zero_fallback_and_internal_bridges` | Keep scalar expression fixtures identical across parser engines with no fallback records. | `ctest --test-dir build/default -R '^parser_shadow_gate_scalar_expressions_zero_fallback_and_internal_bridges$' --output-on-failure` |
| `parser_shadow_gate_functions_zero_fallback_and_internal_bridges` | Keep function fixtures identical across parser engines with no fallback records. | `ctest --test-dir build/default -R '^parser_shadow_gate_functions_zero_fallback_and_internal_bridges$' --output-on-failure` |
| `parser_shadow_gate_file_resources_dual_zero_expected_nonzero` | Keep file-resource parser artifacts matched while allowing registered fail-fast fixtures in `shadow-expected-nonzero.txt`. | `ctest --test-dir build/default -R '^parser_shadow_gate_file_resources_dual_zero_expected_nonzero$' --output-on-failure` |
| `parser_shadow_gate_stream_processing_zero_fallback` | Keep stream-processing fixtures matched with zero legacy fallback. | `ctest --test-dir build/default -R '^parser_shadow_gate_stream_processing_zero_fallback$' --output-on-failure` |
| `parser_shadow_gate_stream_processing_zero_internal_bridges` | Keep stream-processing fixtures matched with zero internal legacy bridges. | `ctest --test-dir build/default -R '^parser_shadow_gate_stream_processing_zero_internal_bridges$' --output-on-failure` |
| `parser_legacy_entry_audit` | Reject new parser legacy-entry regressions. | `ctest --test-dir build/default -R '^parser_legacy_entry_audit$' --output-on-failure` |
| `architecture_layer_gate` | Keep product orchestration on the approved Sema, lowering, and LLVM emission stage APIs while keeping internal AST type inference, AST-to-StyioIR lowering, StyioIR-to-LLVM codegen, and runtime/backend include dependencies inside layer boundaries. | `ctest --test-dir build/default -R '^architecture_layer_gate$' --output-on-failure` |
| `StyioIRContract` verifier diagnostic baseline | Prove StyioIR verifier failures use the public `ir_verify` phase and `STYIO_IR_VERIFY_*` code families, including inactive-node and pass-manager contract failures. | `ctest --test-dir build -R '^(StyioDiagnosticContract\.ClassifiersCoverFallbackAndPhaseFamilies\|StyioIRContract\.VerifierRejectsInactiveIR\|StyioLoweringInternal\.PassManagerRunsCanonicalizationAndVerifierStages)$' --output-on-failure` |
| `StyioCodeGenInternal.LegacyIoGetTypeUsesConcreteStatementAndPathTypes` | Prove merged legacy IO nodes do not regress to `i64` placeholder type mapping: path literals report pointer type, while statement-shaped print/read report `void`. | `ctest --test-dir build -R '^StyioCodeGenInternal\.LegacyIoGetTypeUsesConcreteStatementAndPathTypes$' --output-on-failure` |
| `StyioCodeGenInternal.IntegerDivAndModuloGuardUnsafeDivisorsBeforeInstruction` | Prove integer `/`, `%`, `/=`, and `%=` lower through safe numerator/divisor selects before LLVM `sdiv` / `srem`, so zero or undefined divisors cannot reach UB-producing instructions. | `ctest --test-dir build -R '^StyioCodeGenInternal\.IntegerDivAndModuloGuardUnsafeDivisorsBeforeInstruction$' --output-on-failure` |
| `StyioCodeGenInternal.CollectionValueMismatchesFailClosed` | Prove collection, matrix, list-set, dict-set, and runtime list-operation codegen rejects mismatched LLVM value families instead of substituting zero, null, or fake handle placeholders. | `ctest --test-dir build -R '^StyioCodeGenInternal\.CollectionValueMismatchesFailClosed$' --output-on-failure` |
| `runtime_surface_gate` | Lock the exported runtime helper surface across `ExternLib.hpp/.cpp`, ORC registrations, and codegen-emitted `styio_*` helper references. | `ctest --test-dir build/default -R '^runtime_surface_gate$' --output-on-failure` |
| `stdlib_manifest_gate` | Validate the first-class `library/manifest.json` standard-library envelope, including module uniqueness, reserved directories, schema markers, and active prelude source/test evidence. | `ctest --test-dir build/default -R '^stdlib_manifest_gate$' --output-on-failure` |
| `StyioSecurityNightlyParserStmt.ParsesResourcePreludeSourceFile` | Prove the active `std.resource` compatibility prelude source parses as accepted internal Styio source without adding hidden pseudo-primitives. | `ctest --test-dir build/default -R '^StyioSecurityNightlyParserStmt\.ParsesResourcePreludeSourceFile$' --output-on-failure` |
| Series intrinsic evidence | Prove the active `[avg,n]` / `[max,n]` slice has parser AST, state-slot lowering, ownership, string representation, and pulse codegen coverage while other series operators remain deferred. | `ctest --test-dir build/default -R '^(StyioSecurityParserContext\.CoversModernIndexTailsAndCharLiteralEdgesDirectly\|StyioIRContract\.(IteratorPulseStateHelperCloneCoversHistoryAndSeriesExpressions\|IteratorPulseStateSeriesDetectionScansWaveMergeBranches\|StateReferenceHistoryAndSeriesContextsStayExplicit)\|StyioSecurityAstOwnership\.SeriesIntrinsicOwnsBaseAndWindow\|StyioSecurityNightlyCodegen\.PulsePlanCodegenCoversStateSlotsAndSeriesIntrinsics)$' --output-on-failure` |
| Matrix intrinsic evidence | Prove compiler-owned matrix helper calls infer shape/element families, lower to typed runtime entrypoints, execute through sample coverage, and keep invalid matrix shapes fail-closed. | `ctest --test-dir build/default -R '^(StyioTypeInferenceContract\.MatrixIntrinsicCallsInferShapesAndRejectBadInputs\|StyioIRContract\.MatrixIntrinsicLoweringUsesTypedRuntimeEntryPoints\|StyioSecurityNightlySemantics\.(LowersMatrixIntrinsicFunctions\|AllowsMatrixTypedNestedListLiteral\|RejectsRaggedMatrixTypedNestedListLiteral\|RejectsStaticMatrixShapeMismatch)\|StyioSamples\.MatrixOperationsAndIntrinsics)$' --output-on-failure` |
| `styio_core_benchmark_smoke` | Prove the in-repo core performance corpus executes through `styio`, writes `styio.core_benchmark_result.v1` JSON, and keeps timing-schema evidence available without the external benchmark checkout. | `ctest --test-dir build/default -R '^styio_core_benchmark_smoke$' --output-on-failure` |
| `coverage-gate` | Build with LLVM source coverage, run the core coverage label set, and fail below 95% project source line coverage. | `scripts/coverage-gate.sh --build-dir build/coverage --threshold 95` |
| `StyioSecurity` source-coverage sentinels | Keep high-gap internal code paths exercised while broader feature coverage catches up: pulse-plan codegen, direct IR type mapping, rare AST/IR text forms, token/string representation switches, runtime matrix/list/dict/task APIs, and stable runtime error-family classification. | `ctest --test-dir build/default -R '^(StyioSecurityNightlyCodegen\.(PulsePlanCodegenCoversStateSlotsAndSeriesIntrinsics\|GetTypeCoversScalarCollectionIoAndTaskNodes)\|StyioSecurityRepr\.RareAstAndIrNodesHaveStableTextForm\|StyioSecurityTokenRepr\.StableNamesCoverAstDataTypeOperatorAndTokenSwitches\|StyioSafetyRuntime\.(MatrixApiCoversI64F64OperationsAndStableErrors\|DictApiCoversBackendsFamiliesClonesAndViews\|TaskApiCoversReadySpawnPullProfilesAndInvalidHandles))$' --output-on-failure` |
| `StyioDiagnostics.CompoundAssignOnImmutableBindingReportsSemaCode` | Prove compound assignment to a final binding keeps the TypeError exit family while reporting the narrower `STYIO_SEMA_IMMUTABLE_BINDING` code and public `sema` phase. | `ctest --test-dir build -R '^StyioDiagnostics\.CompoundAssignOnImmutableBindingReportsSemaCode$' --output-on-failure` |
| `StyioDiagnostics` undeclared-symbol diagnostic refinements | Prove unknown user function calls and unknown Topology resource references keep the TypeError exit family while reporting `STYIO_SEMA_UNDECLARED_SYMBOL` with public phase `sema`, without broadening symbol resolution, hidden native symbol visibility, import behavior, or resource lookup semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.(UnknownFunctionReportsSemaUndeclaredSymbolCode\|UnknownResourceReportsSemaUndeclaredSymbolCode)$' --output-on-failure` |
| `StyioDiagnostics` call-arity diagnostic refinements | Prove user function and resource-method arity mismatches keep the TypeError exit family while reporting `STYIO_SEMA_CALL_ARITY_MISMATCH` with public phase `sema`, without broadening function calling or resource-method dispatch semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.(UserFunctionArityReportsSemaCallArityMismatchCode\|ResourceMethodArityReportsSemaCallArityMismatchCode)$' --output-on-failure` |
| `StyioDiagnostics` resource capability diagnostic refinements | Prove focused resource capability mismatches keep the TypeError exit family while reporting `STYIO_SEMA_RESOURCE_CAPABILITY_MISMATCH` with public phase `sema`, without broadening resource capability rules, resource family support, pressure observers, or fallback recovery semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.(ResourceWriteToStdinReportsSemaCapabilityCode\|InstantPullFromStderrReportsSemaCapabilityCode)$' --output-on-failure` |
| `StyioDiagnostics` pressure observer unsupported-family boundary | Prove `resource.pressure >> #(p) => { ... }` parses to the resource-family Sema boundary, keeps current `@stdout` and topology-resource families fail-closed with `STYIO_SEMA_RESOURCE_PRESSURE_OBSERVER_UNSUPPORTED`, and stops before following output without implying pressure payloads, observer execution, or runtime pressure-stream support. | `ctest --test-dir build -R '^(StyioSecurityNightlyParserStmt\.ParsesPressureObserverStatementToSemaBoundary\|StyioSecurityNightlySemantics\.RejectsPressureObserverBeforeResourceFamilySupport\|StyioDiagnostics\.PressureObserverOn(Stdout\|TopologyResource)ReportsSemaCode)$' --output-on-failure` |
| `StyioDiagnostics` call argument-type diagnostic refinements | Prove user function and resource-method argument type mismatches keep the TypeError exit family while reporting `STYIO_TYPE_CALL_ARGUMENT_MISMATCH` with public phase `type`, without broadening function calling, implicit argument adaptation, native ABI behavior, or resource-method dispatch semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.(UserFunctionArgumentMismatchReportsTypeCallArgumentMismatchCode\|ResourceMethodArgumentMismatchReportsTypeCallArgumentMismatchCode)$' --output-on-failure` |
| `StyioDiagnostics` matrix literal invalid diagnostic refinements | Prove invalid typed matrix literal shape/context failures keep the TypeError exit family while reporting `STYIO_TYPE_MATRIX_LITERAL_INVALID` with public phase `type`, without broadening matrix literal acceptance, matrix return compatibility, matrix operators, resource-method matrix bodies, or matrix runtime behavior. | `ctest --test-dir build -R '^StyioDiagnostics\.(MatrixBindingRaggedLiteralReportsTypeCode\|MatrixFunctionReturnFlatListFailsBeforeRuntime)$' --output-on-failure` |
| `StyioDiagnostics.IteratorSequenceHashTagRoutingReportsFeatureCode` | Prove retired hash-tag iterator routes stay fail-closed, stop before following output, and report `STYIO_TYPE_STREAM_HASH_TAG_ROUTE_UNSUPPORTED` with public phase `type` after the IM-D5-P1 retirement decision. | `ctest --test-dir build -R '^StyioDiagnostics\.IteratorSequenceHashTagRoutingReportsFeatureCode$' --output-on-failure` |
| `StyioDiagnostics.IteratorUnsupportedSourceReportsFeatureCode` | Prove non-iterable ordinary iterator sources stay fail-closed, stop before following output, and report `STYIO_TYPE_ITERATION_UNSUPPORTED_SOURCE` with public phase `type` without broadening iterator or selector semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.IteratorUnsupportedSourceReportsFeatureCode$' --output-on-failure` |
| `StyioDiagnostics.DuplicateStdinZipReportsFeatureCode` | Prove duplicate `@stdin & @stdin` zip sources stay fail-closed, stop before following output, and report `STYIO_TYPE_STREAM_DUPLICATE_DRIVER_UNSUPPORTED` with public phase `type` while the owner decision keeps implicit stdin duplication and sequential double reads unsupported. | `ctest --test-dir build -R '^StyioDiagnostics\.DuplicateStdinZipReportsFeatureCode$' --output-on-failure` |
| `StyioDiagnostics` native interop diagnostic refinements | Prove missing native `@extern` referenced sources, explicit binding/signature misses, unsupported signature shapes, host compiler rejections, native artifact load failures, missing exported symbols, and unavailable toolchain configuration keep the TypeError exit family while reporting `STYIO_NATIVE_SOURCE_READ_FAILED`, `STYIO_NATIVE_SIGNATURE_NOT_FOUND`, `STYIO_NATIVE_UNSUPPORTED_SIGNATURE`, `STYIO_NATIVE_HOST_COMPILE_FAILED`, `STYIO_NATIVE_LOAD_FAILED`, `STYIO_NATIVE_SYMBOL_MISSING`, or `STYIO_NATIVE_TOOLCHAIN_UNAVAILABLE` with public phase `native_interop`, without broadening native ABI, signature support, symbol visibility, artifact loading, or host toolchain behavior. | `ctest --test-dir build -R '^StyioDiagnostics\.(NativeExternMissingSourceReportsNativeCode\|NativeExternMissingBindingReportsNativeCode\|NativeExternUnsupportedSignatureReportsNativeCode\|NativeExternVariadicSignatureReportsNativeCode\|NativeExternHostCompileFailureReportsNativeCode\|NativeExternSymbolMissingReportsNativeCode\|NativeExternToolchainUnavailableReportsNativeCode\|NativeExternLoadFailureReportsNativeCode)$' --output-on-failure` |
| `StyioDiagnostics` typed stdin unsupported-target refinements | Prove unsupported explicit-target stdin scalar/list families such as `bool` and `list[bool]` keep the TypeError exit family while reporting `STYIO_TYPE_STDIN_UNSUPPORTED_TARGET` with public phase `type`, without broadening stdin target support or resource-effect recovery semantics. | `ctest --test-dir build -R '^StyioDiagnostics\.UnsupportedTypedStdin(ListTarget\|Target)ReportsTypeCode$' --output-on-failure` |
| `StyioStreamZip` stdin/file/list/source-selector combination tests | Prove stdin/list and stdin/file zip source orders execute with finite shorter-input termination, existing file/list and materialized-list paths remain green, bounded matrix selector snapshots feed finite zip as materialized `list[matrix]`, duplicate `@stdin & @stdin` fails closed by owner decision with `STYIO_TYPE_STREAM_DUPLICATE_DRIVER_UNSUPPORTED`, and scalar/latest selectors or non-iterables stay rejected with `STYIO_TYPE_STREAM_ZIP_UNSUPPORTED_SOURCE`. | `ctest --test-dir build/default -R '^(StyioStreamZip\.(MaterializedListHandlesSupportF64AndBool\|MixedFileAndMaterializedListsTerminateAtShorterInput\|StdinAndMaterializedListsTerminateAtShorterInput\|StdinAndFileStreamsTerminateAtShorterInput\|ResourceScalarSelectorFailsClosedAsZipInput\|ResourceMatrixSelectorSnapshotsFeedZipBarrier\|ResourceMatrixLatestSelectorFailsClosedAsZipInput)\|StyioSecurityNightlySemantics\.(CharMaterializedListsFeedZipBarrier\|MatrixSelectorSnapshotsFeedZipBarrier\|StdinFeedsStreamZipBarrier\|RejectsDuplicateStdinZipBeforeDriverDecision)\|StyioDiagnostics\.(StreamZipUnsupportedSourceReportsFeatureCode\|DuplicateStdinZipReportsFeatureCode)\|stream_processing_t14_zip_matrix_selector_snapshots)$' --output-on-failure` |
| `RangeLiteralExpressionBoundsMaterializeList` / `RangeLiteralRejectsStepRangeSyntax` / `RangeLiteralRejectsNonIntegerExpressionBounds` / `DynamicRangeLiteralLowersToRuntimeListLoop` | Prove `[start..end]` materializes integer expression bounds as `list[i64]`, rejects reserved `[start..end..step]` source syntax in the parser, rejects non-integer bounds in Sema, and emits the dynamic runtime-list fill loop. | `ctest --test-dir build/default -R '^(StyioParserEngine\.(RangeLiteralExpressionBoundsMaterializeList\|RangeLiteralRejectsStepRangeSyntax\|RangeLiteralRejectsNonIntegerExpressionBounds)\|StyioSecurityNightlyCodegen\.DynamicRangeLiteralLowersToRuntimeListLoop)$' --output-on-failure` |
| `StyioDiagnostics` compile-plan malformed-input hardening tests | Prove explicit malformed `profile.build_mode`, non-object `packages` entries, and missing `entry.package_id` package-list consistency fail with machine-readable CLI diagnostics and `diag_dir` output when available. | `ctest --test-dir build/default -R '^StyioDiagnostics\.CompilePlan(MalformedBuildModeTypeWritesCliDiagnosticToDiagDir\|MalformedPackageEntryWritesCliDiagnosticToDiagDir\|EntryPackageMissingWritesCliDiagnosticToDiagDir)$' --output-on-failure` |
| `StyioDiagnostics` function return fallback tests | Prove scalar and inferred function returns stay executable, explicit matrix function returns apply matrix literal context, flat-list matrix returns fail before runtime with `STYIO_TYPE_MATRIX_LITERAL_INVALID`, tuple return annotations fail closed with `STYIO_TYPE_UNSUPPORTED_TUPLE_RETURN` instead of lowering to `i64`, and empty or tail-less function bodies fail closed with `STYIO_TYPE_FUNCTION_MISSING_RETURN` instead of returning `0`. | `ctest --test-dir build/default -R '^(StyioDiagnostics\.(ScalarAndInferredFunctionReturnsStayExecutable\|Matrix(BindingRaggedLiteralReportsTypeCode\|FunctionReturnFlatListFailsBeforeRuntime)\|TupleFunctionReturnAnnotationReportsTypeCode\|FunctionMissingReturnReportsTypeCode)\|StyioIRContract\.FunctionLoweringDefaultsUntypedParamsAndRejectsEmptyBodies\|StyioSecurityNightly(Semantics\.RejectsMatrixFunctionReturnFlatListBeforeRuntime\|ParserStmt\.RejectsTupleFunctionReturnAnnotationBeforeLoweringFallback))$' --output-on-failure` |
| `StyioTypeInferInternal` session type/symbol side-map tests | Prove attached `TypeTable` records inferred binding types, SemaContext side-map write helpers, ordinary function signature parameter/return types, resource-method parameter/result types, and inferred list/dict literal result types through Sema, and `SymbolInterner` side maps resolve ordinary/native function calls, inferred function-return tracking, `NameAST` local binding reads, self-assign and binary local binding type reads, stdin collect source checks, binding-info resource/final/declared-type checks, fixed/final assignment checks, resource binding types, snapshot declarations, duplicate task/resource-consumption checks, and task outer-resource ownership checks by `SymbolId` after the string fallback map is cleared. | `ctest --test-dir build -R '^StyioTypeInferInternal\.(AttachedTypeTableInterns(BindingTypesThroughSema\|ContainerLiteralTypesThroughSema\|SemaContextSideMapWrites)\|FunctionReturnAndReceiverScanHelpersStayExplicit\|SymbolInternerResolves(FunctionDefinitions\|NativeFunctionDefinitions\|LocalBindingTypes\|BindingInfo\|FixedAssignments\|ResourceBindingTypes\|SnapshotVars\|ConsumedTasks\|ConsumedResources\|OwnedResources)BySymbolId)$' --output-on-failure` |
| `StyioDiagnostics` / `StyioSecurityNightlySemantics` match Sema tests | Prove accepted match expressions and function match sugar infer branch tail values before lowering, keep branch-local bindings isolated, preserve `i64`/`f64`/`bool`/`char`/`string` result families including native `bool`/`char` LLVM PHI widths, keep recursive function match sugar executable, and reject undefined arm tails, branch-local leaks, or unsupported container match results. | `ctest --test-dir build -R '^(StyioDiagnostics\.(FunctionMatchSugarAndTailExpressionsReturnValues\|MatchBoolAndCharTailValuesPreserveScalarFamilies\|MatchBranchLocalTailValuesRemainExecutable)\|StyioSecurityNightlySemantics\.(MatchBoolAndCharLowerToNativeScalarWidths\|RejectsUnsupportedContainerMatchResultBeforeLowering\|RejectsUndefinedMatchArmTailValue\|MatchArmBindingsDoNotLeakAcrossBranches\|FunctionMatchSugarInfersArmBodiesOnCall\|FunctionMatchSugarArmBindingsStayBranchScoped))$' --output-on-failure` |
| `StyioResourceCopy` materialized clone tests | Prove `copy << list_or_dict_or_matrix` emits explicit clone lowering, produces independent runtime containers, and keeps `copy <- list_or_dict_or_matrix` fail-closed because `<-` is acquire/receive or task pull, not bound-resource clone. | `ctest --test-dir build/default -R '^(StyioResourceCopy\.Explicit(ListAndDictCloneAreIndependent\|MatrixCloneIsIndependent)\|StyioSecurityNightlySemantics\.(Rejects(BoundResourceCopyByLeftArrow\|MatrixCopyByLeftArrow)\|AllowsExplicit(CloneFormAndIndexedMutation\|MatrixCloneForm)\|AllowsDictIndexingAttrsAndClone))$' --output-on-failure` |
| `StyioResourceTopology` selector snapshot iterator/copy tests | Prove bounded `@name[...]` and `@name[-n..]` selector iterators run over materialized history values through the runtime-list path, keep scalar latest selector iterators fail-closed as non-iterable with `STYIO_TYPE_ITERATION_UNSUPPORTED_SOURCE`, preserve existing selector-snapshot zip behavior including matrix-valued snapshots, prove bounded list/dict/matrix selector history stores cloned handles instead of aliases, prove explicit `name << @resource[...]` / `name << @resource[-n..]` copies for list/dict/matrix handle snapshots stay materialized after later source writes, and keep unbounded topology snapshot selectors from emitting committed snapshot-read graph edges. | `ctest --test-dir build -R '^(StyioResourceTopology\.(ResourceSelectorSnapshotIteratorUsesMaterializedValues\|ResourceSelectorHandleSnapshotsCloneListAndDictValues\|ResourceSelectorHandleSnapshotCopiesStayMaterialized\|ResourceSelectorMatrixSnapshotsCloneValues\|ResourceScalarSelectorIteratorFailsClosed\|ResourceTopologyRejectsUnboundedSnapshotSelectors)\|StyioSecurity(NightlySemantics\.(ResourceSelectorSnapshotIteratorUsesRuntimeListPath\|MatrixSelectorSnapshotsFeedZipBarrier)\|ResourceTopology\.(LowersBoundedListAndDictResourceSelectors\|LowersBoundedMatrixResourceSelectors\|KeepsUnboundedMatrixResourceSelectorSnapshotFailClosed))\|stream_processing_t1(3_zip_resource_selector_snapshots\|4_zip_matrix_selector_snapshots)\|state_resources_t12_topology_selector_snapshot_matrix\|state_resources_err_e06_selector_snapshot_unbounded_matrix_unsupported)$' --output-on-failure` |
| File resource default-settlement tests | Prove plain file acquire/write/release/direct-iterator failures outside `?|` emit JSONL runtime diagnostics and stop before following statements, successful direct release continues, and explicit resource-effect recovery remains covered separately. | `ctest --test-dir build/default -R '^(StyioDiagnostics\.RuntimeFile(Acquire\|Write\|IteratorOpen\|Release)FailureStopsBeforeNextStatement\|StyioResourceLifecycle\.DirectFileReleaseSuccessContinues)$' --output-on-failure` |
| `StyioResourceLifecycle` file cleanup and alias lifecycle tests | Prove use-after-close remains rejected without a rebind, accepted `name = @file(...)` creates a fresh resource occupant, same-path singleton slots reopen after explicit close, successful rebind releases the old handle and continues with the replacement, cleanup failure during rebind stops before overwrite/following statements, stale same-path aliases fail fast with `STYIO_RUNTIME_INVALID_FILE_HANDLE` instead of EOF, explicit function returns close tracked file handles before returning, loop break/continue exits close tracked file handles before branching, and top-level break/continue fail closed. | `ctest --test-dir build/default -R '^(StyioResourceLifecycle\.(FlexFileRebindReleasesOldFileAndContinues\|FlexFileRebindCleanupFailureStopsBeforeOverwrite\|FlexFileRebindAfterCloseReopensSamePath\|FunctionReturnRunsFileScopeCleanupSmoke\|LoopBreakAndContinueRunFileScopeCleanupSmoke\|FileAliasUseAfterCloseFailsFast)\|StyioSecurityNightlyCodegen\.(RebindRunsFileCleanupBeforeNextAcquire\|ReturnRunsFileScopeCleanupBeforeRet\|BreakRunsFileScopeCleanupBeforeLoopExitBranch\|ContinueRunsFileScopeCleanupBeforeLoopStepBranch\|RejectsBreakAndContinueOutsideLoop)\|StyioResourceTopology\.(ResourceUseAfterCloseStillRequiresRebind\|FlexResourceRebindClearsDestroyedReceiver)\|StyioSafetyRuntime\.ZeroFileHandleReadSetsRuntimeError)$' --output-on-failure` |
| `StyioResourceEffects` cleanup-family tests | Prove explicit file close cleanup failure reports `STYIO_RUNTIME_FILE_CLEANUP_FAILURE`, matches `cleanup`, and is not caught by adjacent `io` handlers. | `ctest --test-dir build/default -R '^(StyioResourceEffects\.(NamedCleanupHandlerRunsForFileCloseFailure\|IoHandlerDoesNotCatchFileCloseCleanupFailure)\|StyioSafetyRuntime\.FileCloseFailureIsCleanupRuntimeEffect)$' --output-on-failure` |
| `StyioResourceEffects` file acquire settlement tests | Prove statement-shaped `?\| f <- @file(...) \| fallback` skips recovery on success and keeps the acquired file handle usable by a following iterator, later resource-effect close method, later guarded `f.write(...)` write method, or later `value = ?\| (<< f) \| fallback` instant-pull expression; recovers file-open read failures through catch-all fallback or matched `io` handlers; fails fast without fallback; fails closed if recovered failed acquire is later used as a zero handle; does not recreate a missing path through later write-method use; keeps close-method receiver invalidation; and remains rejected as a value-producing acquire expression. | `ctest --test-dir build -R '^(StyioResourceEffects\.HandleAcquire(FallbackRecoversFileOpenFailure\|NamedIoHandlerRecoversFileOpenFailure\|NoFallbackStopsFileOpenFailure\|SuccessSkipsFallbackAndKeepsHandleUsable\|SuccessFeedsLaterResourceEffectClose\|SuccessFeedsLaterResourceEffectWrite\|SuccessFeedsLaterResourceEffectInstantPull\|FallbackThenUseFailsClosed\|FallbackThenWriteHandlerRecoversClosedHandle\|FallbackThenWriteNoFallbackStopsClosedHandle\|FallbackThenInstantPullHandlerRecoversClosedHandle\|FallbackThenInstantPullNoFallbackStopsClosedHandle)\|StyioSecurityNightlyParserStmt\.(ParsesResourceEffectHandleAcquireStatement\|ResourceEffectHandleAcquireFeedsLater(Iterator\|CloseMethod\|InstantPull)\|RejectsHandleAcquireResourceEffectExpression)\|StyioSecurityNightlySemantics\.(ResourceEffectAcquireThenCloseConsumesReceiver\|RejectsNonFileHandleInstantPullResourceEffectValue))$' --output-on-failure` |
| `StyioResourceEffects` file iterator settlement tests | Prove statement-shaped `?\| @file(...) >> #(line) => { ... } \| fallback` skips recovery on success, recovers file-open read failures through catch-all fallback or matched `io` handlers, fails fast without fallback, lowers through the resource-effect IR route, and keeps non-file iterators rejected under `?\|`. | `ctest --test-dir build -R '^(StyioResourceEffects\.FileIterator(FallbackRecoversOpenFailureAndContinues\|NamedIoHandlerRecoversOpenFailure\|NoFallbackStopsOpenFailure\|SuccessSkipsFallback)\|StyioSecurityNightlyParserStmt\.ParsesResourceEffectFileIteratorStatement\|StyioSecurityNightlySemantics\.RejectsNonFileIteratorResourceEffectStatement)$' --output-on-failure` |
| `StyioResourceEffects` file rebind settlement tests | Prove statement-shaped `?\| f = @file(...) \| fallback` recovers replacement file-open failures through catch-all fallback or matched `io` handlers, leaves a recovered failed rebind on the closed-handle path for later acquired-handle pulls, keeps scalar flex binds fail-closed, rejects value-required rebind expressions, and emits the cleanup-error branch before replacement open. | `ctest --test-dir build -R '^(StyioResourceEffects\.FlexRebind(Fallback\|NamedIoHandler)RecoversFileOpenFailure\|StyioSecurityNightlyParserStmt\.(ParsesResourceEffectFileRebindStatement\|RejectsScalarFlexBindResourceEffectStatement\|RejectsFileRebindResourceEffectExpression))$' --output-on-failure` |
| `StyioResourceEffects` direct file release settlement tests | Prove statement-shaped `?\| @file(...) -> @() \| fallback` skips recovery on success, recovers file-open read failures through catch-all fallback or matched `io` handlers, fails fast without fallback, lowers acquired-handle release through `SIOHandleRelease`, consumes the receiver for topology, and rejects value-required release expressions. | `ctest --test-dir build -R '^(StyioResourceEffects\.DirectFileRelease(SuccessSkipsFallback\|FallbackRecoversFileOpenFailure\|NamedIoHandlerRecoversFileOpenFailure\|NoFallbackStopsFileOpenFailure)\|StyioSecurityNightlyParserStmt\.(ParsesResourceEffectDirectFileReleaseStatement\|ResourceEffectHandleAcquireFeedsLaterDirectRelease)\|StyioSecurityNightlySemantics\.(RejectsDirectFileReleaseResourceEffectExpression\|ResourceEffectAcquireThenDirectReleaseConsumesReceiver))$' --output-on-failure` |
| `StyioResourceEffects` resource-method settlement tests | Prove statement-shaped `?\| @file(...).close() \| fallback` skips recovery on success, recovers missing-file open failures through catch-all fallback or matched `io` handlers, fails fast without fallback, and rejects non-resource member calls under `?\|`. | `ctest --test-dir build/default -R '^(StyioResourceEffects\.ResourceMethodClose(SuccessSkipsFallback\|FallbackRecoversFileOpenFailure\|NamedIoHandlerRecoversFileOpenFailure\|NoFallbackStopsFileOpenFailure)\|StyioSecurityNightlyParserStmt\.ParsesResourceEffectResourceMethodStatement\|StyioSecurityNightlySemantics\.RejectsNonResourceMethodResourceEffectStatement)$' --output-on-failure` |
| `StyioResourceEffects` value-returning resource-method tests | Prove single-return resource methods such as `@file::answer = () => { <\| 42 }`, statement-preface methods such as `@file::answer = () => { >_("inside") <\| 42 }`, scalar local `=` / `:=` preface methods such as `@file::answer = () => { x := 41 <\| x + 1 }`, and local list/dict/matrix preface methods such as `@file::answer = () => { xs = [40, 2]; <\| xs[0] + xs[1] }`, `@file::list_answer = () => { xs := [41,42]; <\| xs }`, `@file::matrix_cell = () => { m: matrix := [[42,2],[1,3]]; <\| m[0][0] }`, or `@file::matrix_answer = () => { m: matrix := [[40,2],[1,3]]; <\| m }`, return values through direct calls and `result = ?\| log.answer() \| fallback`, preserve bool/f64/char/string scalar, format-string, scalar/string match-expression, local list/dict/matrix container, returned block-form function-call, and explicit matrix-return function result families through direct calls and guarded value paths, return internally recovered values when the returned expression is itself a value-producing `?\| ... \| ...` resource-effect expression, recover returned file instant-pull failures through catch-all fallback or matched `io` handlers, recover returned canonical stdin `(<- @stdin)` parse failures through catch-all fallback or matched `parse` handlers, recover returned list index/list slice, inline dict index, ordered dict value slice, and typed-parameter matrix cell/row or row-range slice bounds failures through catch-all fallback or matched `bounds` handlers, fail fast without fallback, keep returned statement-only function calls, returned resource-effect discard, returned container match results, flat-list matrix returns, local resource binding method bodies, and global matrix capture fail-closed, keep resource method argument mismatches and fallback type mismatches fail-closed with `STYIO_TYPE_RESOURCE_EFFECT_FALLBACK_MISMATCH`, and reject unsupported statement-only called-function returns or local resource binding method bodies before lowering with `STYIO_SEMA_RESOURCE_METHOD_UNSUPPORTED_BODY`. | `ctest --test-dir build -R '^(StyioResourceEffects\.(ValueResourceMethod(Local(FlexBind\|FinalBind)ReturnsFromDirectAndResourceEffectCalls\|Local(ListDictMatrixBindsReturnScalarValues\|ContainerReturnsFromDirectAndFallbackCalls)\|StatementPrefaceReturnsFromDirectAndResourceEffectCalls\|ReturnsFromDirectAndResourceEffectCalls\|ScalarFamiliesReturnFromDirectAndFallbackCalls\|MatchCasesPreserveScalarFamilies\|ReturnedBlockFunction(CallsInferResultFamilies\|MatrixResult)\|ReturnedResourceEffectRecoversInsideMethod)\|ResourceMethod(Value\|FmtStringValue)FallbackTypeMismatchReportsTypeCode\|ResourceMethodReturnedStatementOnlyFunctionReportsSemaCode\|ValueResourceMethod(FallbackRecoversReturnedInstantPullFailure\|NamedIoHandlerRecoversReturnedInstantPullFailure\|NoFallbackStopsReturnedInstantPullFailure\|FallbackRecoversReturnedStdinParseFailure\|NamedParseHandlerRecoversReturnedStdinParseFailure\|NoFallbackStopsReturnedStdinParseFailure\|FallbackRecoversReturnedContainerBoundsFailure\|NamedBoundsHandlerRecoversReturnedDictFailure\|NoFallbackStopsReturnedDictFailure\|FallbackRecoversReturnedMatrixBoundsFailure\|NamedBoundsHandlerRecoversReturnedMatrixFailure\|NoFallbackStopsReturnedMatrixFailure))\|StyioDiagnostics\.MatrixFunctionReturnFlatListFailsBeforeRuntime\|StyioSecurityNightlyParserStmt\.(ParsesResourceMethod(Local(FlexBind\|FinalBind)ReturnExpression\|LocalListDictMatrixBindReturnExpressions\|StatementPrefaceReturnExpression\|Returned((Dict(Index\|Slice)\|ListSlice\|Matrix(Index\|Row\|Slice))\|ScalarFmt\|ResourceEffect\|BlockFunctionCall)FallbackExpression)\|ParsesResourceMethodReturnedMatchCasesScalarFamilies\|RejectsResourceMethodReturnedResourceEffectDiscardExpression)\|StyioSecurityNightlySemantics\.(ResourceMethodLocalContainerReturnsLowerThroughValueScope\|MatrixFunctionReturnFeedsResourceMethodValue\|RejectsMatrixFunctionReturnFlatListBeforeRuntime\|RejectsResourceMethod(ArgumentTypeMismatch\|LocalResourceBindBeforeResourceValueScopeSupport\|GlobalMatrixCaptureBeforeLexicalCaptureSupport\|ReturnedContainerMatchResult\|ReturnedStatementOnlyFunctionCall)))$' --output-on-failure` |
| `StyioDiagnostics` resource method inline-clone tests | Prove accepted single-quoted `char` literals, format strings, receiver-scoped property postfix such as `@file.path`, dynamic range literals, materialized list/list stream-zip statements, match expressions, statement-only, scalar local `=` / `:=`, and local list/dict/matrix `=` / `:=` prefaces before a final scalar/string or local list/dict/matrix container return, returned block-form function calls, and value-producing resource-effect expressions parse inside user-defined resource method bodies, survive `StateExprCloneVisitor` inlining, execute after resource method calls or under `?\| method() \| fallback`, and reject adjacent invalid multi-byte char literals, malformed format strings, top-level receiver-property shorthand, local resource binding method bodies, lexical/global captures, returned container match results, returned statement-only function calls, returned resource-effect discard, unsupported stream-zip sources, or non-integer range bounds before lowering. | `ctest --test-dir build -R '^(StyioDiagnostics\.ResourceMethod(CharLiteralBodyRunsAfterInlineClone\|FmtStringBodyRunsAfterInlineClone\|ReceiverPropertyBodyRunsAfterInlineClone\|RangeBodyRunsAfterInlineClone\|StreamZipBodyRunsAfterInlineClone\|InvalidCharLiteralFailsClosed\|InvalidFmtStringFailsClosed\|InvalidRangeBoundFailsClosed)\|StyioDiagnostics\.ResourceReceiverPropertyOutsideMethodBodyFailsClosed\|StyioResourceEffects\.ValueResourceMethod(Local(FlexBind\|FinalBind)ReturnsFromDirectAndResourceEffectCalls\|Local(ListDictMatrixBindsReturnScalarValues\|ContainerReturnsFromDirectAndFallbackCalls)\|StatementPrefaceReturnsFromDirectAndResourceEffectCalls\|MatchCasesPreserveScalarFamilies\|ReturnedBlockFunctionCallsInferResultFamilies\|ReturnedResourceEffectRecoversInsideMethod)\|StyioSecurityNightlyParserStmt\.(ParsesResourceReceiverPropertyAccessInsideDefinitions\|RejectsResourceReceiverPropertyOutsideDefinition\|ParsesResourceMethodLocal(FlexBind\|FinalBind)ReturnExpression\|ParsesResourceMethodLocalListDictMatrixBindReturnExpressions\|ParsesResourceMethodStatementPrefaceReturnExpression\|ParsesResourceMethodReturned(Range\|ResourceEffect\|BlockFunctionCall)FallbackExpression\|ParsesResourceMethodReturnedMatchCasesScalarFamilies\|RejectsResourceMethodReturnedResourceEffectDiscardExpression)\|StyioSecurityNightlySemantics\.(ResourceMethodLocalContainerReturnsLowerThroughValueScope\|RejectsResourceMethodLocalResourceBindBeforeResourceValueScopeSupport\|RejectsResourceMethodGlobalMatrixCaptureBeforeLexicalCaptureSupport\|RejectsResourceMethod(ReturnedContainerMatchResult\|ReturnedStatementOnlyFunctionCall)))$' --output-on-failure` |
| `StyioLoweringInternal` internal helper clone tests | Prove `StateExprCloneVisitor` clones internal `CondFlowAST` trees, `ParallelAssignAST` targets keep local temp substitutions without accepting parameter/value substitutions as assignment targets, resource selector lowering can read resource-binding types through `SymbolId`, selected lowering write sites fill SymbolInterner side maps, direct ordinary/native call lowering and pulse/state helper lookup can resolve by `SymbolId`, and local binding, binding-info dynamic slot, resource-method dynamic local slot, and iterator collection reads survive after string fallback maps are cleared, while existing source-backed helper clone families remain intact. | `ctest --test-dir build -R '^StyioLoweringInternal\.(CloneResourceMethodAndPulseHelpersStayExplicit\|AdditionalLoweringGuardBranchesStayExplicit\|SymbolInternerRecordsLoweringSideMapWrites)$' --output-on-failure` |
| `StyioResourceEffects` task_await settlement tests | Prove failed task pulls run `?| task -> value: T | fallback` after clearing the materialized task error, no-fallback awaits stop at settlement, non-task await sources fail closed, and bare continuation freeze fallback stays reserved. | `ctest --test-dir build/default -R '^(StyioResourceEffects\.(TaskAwaitFallbackRunsAfterFailedTaskPull\|FailedTaskAwaitWithoutFallbackStopsAtSettlementSite)\|StyioSecurityNightlyParserStmt\.RejectsBareAwaitFreezeFallbackBeforeContinuationLowering\|StyioSecurityNightlySemantics\.RejectsAwaitPipeNonTaskSource)$' --output-on-failure` |
| `StyioResourceEffects` instant-pull value resource-effect tests | Prove `result = ?| (<< @file("data.txt")) | fallback`, acquired-handle `result = ?| (<< f) | fallback` after `?\| f <- @file(...) \| ...`, untyped `result = ?| (<- @stdin) | fallback`, and explicit-target stdin forms such as `result: f64 = ?| (<- @stdin) | fallback` return successful values or fallback values. Matched `io`/`parse` handlers recover file-open read and stdin numeric/list parse failures, matched `closed` handlers recover zero acquired handles, no-fallback expression settlement fails fast, typed stdin string pulls clone the line value, expression discard remains rejected, statement-shaped write operations are not value expressions, non-file acquired-handle pull sources and unsupported stdin target families such as `bool` or `list[bool]` fail closed with `STYIO_TYPE_STDIN_UNSUPPORTED_TARGET`, and fallback type mismatches fail closed. | `ctest --test-dir build -R '^(StyioResourceEffects\.(ValueFallbackRecovers(FileInstantPull\|StdinInstantPullParseFailure)\|ValueNamedHandlerRecovers(FileInstantPull\|StdinInstantPullParseFailure)\|ValueNoFallbackStops(FileInstantPull\|StdinInstantPullParseFailure)\|ValueTyped(F64FallbackAndHandlerRecoverStdinInstantPullParseFailure\|StringReturnsStdinInstantPull\|List(ReturnsStdinInstantPull\|FallbackRecoversStdinInstantPullParseFailure\|NoFallbackStopsStdinInstantPullParseFailure))\|HandleAcquire(SuccessFeedsLaterResourceEffectInstantPull\|FallbackThenInstantPullHandlerRecoversClosedHandle\|FallbackThenInstantPullNoFallbackStopsClosedHandle))\|StyioSecurityNightlyParserStmt\.(ParsesResourceEffectValue(FallbackExpression\|StdinPullFallbackExpression\|TypedStdinPullFallbackExpressions)\|ResourceEffectHandleAcquireFeedsLaterInstantPull\|RejectsResourceEffectDiscardExpression)\|StyioSecurityNightlySemantics\.(RejectsStatementShapedResourceEffectExpression\|RejectsResourceEffectValueFallbackTypeMismatch\|RejectsUnsupportedTypedStdinResourceEffectValue\|RejectsNonFileHandleInstantPullResourceEffectValue)\|StyioDiagnostics\.UnsupportedTypedStdin(ListTarget\|Target)ReportsTypeCode)$' --output-on-failure` |
| `StyioResourceEffects` container-bounds value resource-effect tests | Prove `result = ?| xs[i] | fallback`, `slice = ?| xs[0..] | fallback`, `result = ?| d[key] | fallback`, `values = ?| d[start..end] | fallback`, `result = ?| m[row][col] | fallback`, `row = ?| m[row] | fallback`, and `rows = ?| m[start..end] | fallback` return successful materialized container values, recover `STYIO_RUNTIME_LIST_INDEX`, `STYIO_RUNTIME_DICT_KEY`, or `STYIO_RUNTIME_MATRIX_INDEX` through catch-all fallback or a matched `bounds` handler, fail fast without fallback, and keep plain `xs[i]`, `xs[0..]`, `d[key]`, `d[start..end]`, `m[row][col]`, and `m[start..end]` outside `?|` guarded before following statements. Parser/security coverage proves the value route lowers to `SCListGet`, `SCListSlice`, `SCDictGet`, `SCDictValues`, `SCMatrixGet`, `SCMatrixRow`, or `SCMatrixRowsSlice`, and fallback type mismatches fail closed. Ordered dict value slices reuse `d.values` plus the list-slice bounds path, so out-of-range dict slices report `STYIO_RUNTIME_LIST_INDEX`. | `ctest --test-dir build -R '^(StyioResourceEffects\.(ValueList(Index\|Slice)(SuccessSkipsFallback\|FallbackRecoversBoundsFailure\|NamedBoundsHandlerRecoversBoundsFailure\|NoFallbackStopsBoundsFailure)\|ValueDict(Index\|Slice)(SuccessSkipsFallback\|FallbackRecoversBoundsFailure\|NamedBoundsHandlerRecoversBoundsFailure\|NoFallbackStopsBoundsFailure)\|ValueMatrix(Index\|Slice)(SuccessSkipsFallback\|FallbackRecoversBoundsFailure\|NamedBoundsHandlerRecoversBoundsFailure\|NoFallbackStopsBoundsFailure)\|ValueMatrixRowFallbackRecoversBoundsFailure)\|StyioDiagnostics\.Runtime(List(Index\|Slice)\|Dict(Key\|Slice)\|Matrix(Index\|Slice))FailureStopsBeforeNextStatement\|StyioSecurityNightlyParserStmt\.ParsesResourceEffectValue(List(Index\|Slice)\|Dict(Index\|Slice)\|Matrix(Index\|Slice))FallbackExpression\|StyioSecurityNightlySemantics\.RejectsResourceEffectValue(List(Index\|Slice)\|Dict(Index\|Slice)\|MatrixIndex)FallbackTypeMismatch)$' --output-on-failure` |
| `StyioMainContract.NanoSourceClosureHelpersCoverSuccessAndFailureEdges` | Prove nano source closure seeds include required compiler sources and active prelude files, fail on missing/unreadable sources, and reject sibling-prefix include escapes with component-aware source-root containment. | `ctest --test-dir build -R '^StyioMainContract\.NanoSourceClosureHelpersCoverSuccessAndFailureEdges$' --output-on-failure` |
| `StyioMainContract.NanoArchiveBoundariesRejectTraversalAndSymlinks` | Prove nano package archive listings reject traversal, absolute, drive-qualified, and backslash paths, package trees reject symlinks, and nano subprocess tool lookup resolves through explicit executable paths before argv/exec launch. | `ctest --test-dir build -R '^StyioMainContract\.NanoArchiveBoundariesRejectTraversalAndSymlinks$' --output-on-failure` |
| `StyioNanoPackage` negative-path edge tests | Prove the compiler-side nano handoff rejects malformed static repository entries, malformed cloud manifests, create/publish guard failures, blob integrity failures, and local/static publish boundary violations without adding package-manager lifecycle UX. | `ctest --test-dir build/default -R '^StyioNanoPackage\.(CloudRepositoryRejectsMalformedEntrySchema\|CloudManifestRejectsMalformedPackageFile\|CliRejectsMutuallyExclusiveAndMisdirectedOptions)$' --output-on-failure` |

---

## 4. Recovery Gate

`checkpoint-health.sh` runs the feature catalog as part of the repository health floor through `styio_pipeline`, shadow gates, security, parser diagnostics, soak, docs checks, and the 95% source coverage gate.

```bash
./scripts/checkpoint-health.sh --no-asan --no-fuzz
```

For docs-only changes that still touch this catalog, use the delivery gate with health skipped only when no compiler/test fixture behavior changed:

```bash
./scripts/delivery-gate.sh --skip-health
```
